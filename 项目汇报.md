# NL2SQL 智能查数系统 · 项目汇报

> 汇报日期:2026-06-04
> 一句话定位:**让不懂 SQL 的业务人员用中文直接查数据库** —— 自动判断查哪个库、写 SQL、执行、并给出结果可信度,全程无需接触任何技术细节。

---

## 一、项目概述

### 背景与目标
数据分析的最后一公里卡在"会提问的人不会写 SQL,会写 SQL 的人不在业务一线"。本项目要把"自然语言 → SQL → 执行 → 展示 → 可信度评估"做成一个**安全、可解释、面向业务用户**的完整闭环,而不是一个只能跑 demo 的玩具。

### 一句话能力
输入"梅西的惯用脚是哪只 / 广东 2025 年销量前 5 的电子产品 / 客单价是多少",系统自动选库、生成并执行 SQL、按表格返回结果,并标注一个 0–100% 的可信度评分。

### 技术底座
LangChain + FastAPI + 通义千问(Qwen)+ 向量检索。当前已接入 **12 个数据源**(1 个自建示例电商库 + 11 个 BIRD 国际基准库),用于功能验证与准确率评测。

---

## 二、当前进度

整体处于**核心闭环完整、关键能力成型、基准验证通过**的阶段,具备接入真实业务库投入试用的条件。

| 模块 | 状态 | 说明 |
|---|---|---|
| NL→SQL 生成闭环 | ✅ 完成 | 生成 / 校验 / 执行 / 失败回修 / 结果格式化 |
| 多数据源自动路由 | ✅ 完成 | 按问题自动选库,支持多轮追问留库 / 换话题切库 |
| 知识库检索(schema linking) | ✅ 完成 | 四路召回 + 融合,大库只喂相关表列 |
| 业务语义层(派生指标) | ✅ 完成 | 业务方用自然语言定义指标,系统自动接字段 |
| AI 准确率评估 | ✅ 完成 | 另一个模型当裁判,异步打分 + 业务语言评语 |
| 安全防护 | ✅ 完成 | 三层防御,只读,只跑 SELECT |
| 前端交互 | ✅ 完成 | 单页应用,进度条 / 异步勋章 / 术语表 / 表结构面板 |
| 评测体系 | ✅ 完成 | 支持 BIRD 基准批量评测 + 高风险用例回归 |
| 接入真实业务库 | ⏳ 就绪待启动 | 改配置即可接 SQLite / PostgreSQL,无需改代码 |

### 本期主要进展(近期重点工作)
1. **检索召回准确率工程化升级**:把检索触发从"按表数量"改为"按 schema 体量",启用检索的库从 **2/11 提升到 9/11**;新增宽表列级裁剪、查询侧跨语言术语扩展、融合打分去偏,系统性提升大库召回质量。
2. **业务语义层落地**:实现"派生指标 / 计算口径" —— 业务方用纯自然语言定义指标(如 `客单价 = 总消费金额 / 订单数量`),系统自动把概念匹配到字段;并攻克大库下的"操作数感知召回"难题。
3. **面向业务用户的体验改造**:可信度评语改为业务语言(不暴露表名/列名);新增查询进度条;评估异步化让结果先出、勋章随后补;表结构面板只展示表、不泄露内部规则。
4. **性能与可靠性优化**:评估异步化、路由改用快模型、启动预热索引,缩短用户感知等待;修复多源场景下的路由切库可靠性问题。
5. **质量保障**:设计并执行 27 条高风险测试用例,覆盖去重、歧义、跨语言、多轮、路由、截断等易错点,发现并修复"大结果集静默截断不告警"等问题。

---

## 三、核心设计

### 3.1 数据流(一次提问的完整链路)
```
用户中文提问
   ↓
① 自动路由      —— 判断该查哪个数据库(多轮感知:追问留原库 / 换话题切新库)
② 加载 Schema   —— 读取表结构 + 业务说明 + 自动发现的字段取值
③ 知识库检索    —— 大库只召回相关表/列 + 业务规则(替代整库结构,省 token、提准确率)
④ 生成 SQL      —— 大模型据上下文写 SQL;信息不足时主动澄清反问
⑤ 安全校验      —— 单条 / 只读 / 关键字黑名单 / 表白名单 / 强制行数上限
⑥ 执行          —— 只读连接执行,返回结果
⑦ 准确率评估    —— 另一个模型异步打分(结果先返回,不阻塞)
   ↓
表格结果 + 生成的 SQL + 可信度勋章；失败自动回修最多 2 轮
```

### 3.2 三模型分工(按任务选模型,兼顾质量与成本)
| 角色 | 模型 | 说明 |
|---|---|---|
| 主力生成 SQL | qwen3.7-plus | 核心质量,用最强模型 |
| 裁判 + 路由 + 查询扩展 | qwen3.6-plus | 辅助任务,与生成分离,可独立调优 |
| 向量检索嵌入 | text-embedding-v3 | 语义召回 |

### 3.3 知识库检索:四路召回融合
大库整库结构既费 token 又稀释信号(如足球库单表 115 列)。系统针对每个问题只召回相关内容:

| 检索路 | 信号 |
|---|---|
| 向量 | 语义相似(列描述 / 业务名) |
| 关键字 | 精确词命中(列名 / 枚举值) |
| 业务术语 | 取值映射、计算口径等业务规则 |
| 关系图谱 | 表结构相关性(沿外键扩散)+ JOIN 路径 |

四路经"倒数排名融合"得到相关表,再做列级裁剪,产出精简上下文喂给模型。

### 3.4 两种部署形态
- **轻量模式(local)**:纯进程内,零外部依赖,开箱即用,适合快速验证。
- **生产模式(server)**:接 Milvus + Elasticsearch + PostgreSQL,适合大库 / 生产。
- 后端**无状态**,会话历史由前端持有,服务可水平扩展。

---

## 四、项目亮点

### 亮点 1:真正面向业务用户,而非技术用户
- **不用选库**:问什么自动路由到对应数据库。
- **不用懂字段**:派生指标用自然语言定义(`客单价 = 总消费金额 / 订单数量`),系统自动接到具体字段;查不到的概念会如实说明而非瞎算。
- **看得懂的可信度**:评估评语用业务语言("系统找到了订单和客户数据,结果能对上你想问的"),不堆砌表名列名。
- **不泄露内部**:表结构面板只展示表,业务规则只喂模型、不暴露给用户。

### 亮点 2:检索召回的系统性工程,而非简单 RAG
- 触发条件按"schema 体量"而非表数量,让宽表库也能享受检索;
- 宽表**列级裁剪**(只留命中列 + 主外键),信号更纯、token 更省;
- **跨语言术语扩展**(中文问题自动补英文列名别名),解决"中文提问 vs 英文列名"召回短板;
- **操作数感知召回**:派生指标的公式操作数会主动把对应列拉进上下文,解决"公式在、源列不在"的难题。

### 亮点 3:安全可解释,可放心接生产库
- **三层防御**:Prompt 约束 + SQL 校验器(只允许单条 SELECT、关键字黑名单、表白名单)+ 数据库只读连接,任一层失守后两层兜底。
- **结果可追溯**:每列回传源表达式,用户一眼看出每个数字来自哪张表哪个字段。
- **可信度自评**:每次查询给出 AI 估算的可信度,辅助用户判断结果可不可信。

### 亮点 4:工程质量与可维护性
- Prompt、数据源、业务词表全部外置,改完即生效,无需改代码;
- 检索索引落盘缓存 + 启动预热,消除冷启动;
- 评估异步化、路由用快模型,优化用户感知延迟;
- 完整评测体系:支持 BIRD 国际基准批量评测,并配套高风险用例回归。

---

## 五、质量与测试情况

围绕系统最易出错的环节设计了 **27 条高风险测试用例**(覆盖时序表去重、概念歧义、宽表漏列、跨语言召回、自动路由切库、多轮同源、澄清、截断等),执行结论:

| 测试组 | 结果 |
|---|---|
| A 时序表去重(正/反向) | 4/4 通过 |
| B 概念歧义 / 宽表 / 跨语言 | 5/5 通过 |
| C 派生指标(含大库 + 兜底) | 4/4 通过 |
| D 自动路由 | 3/4 通过(1 项为含糊问题的设计权衡) |
| E 多轮追问 / 同源原则 | 7/7 通过 |
| F 澄清 / 缺字段 / 截断 | 4/5 通过(1 项已修复) |

- **整体通过率高**,核心能力(去重、跨语言召回、派生指标、多轮、安全)全部达标;
- 测试中发现"大结果集被静默截断却不告警"的问题,已通过执行层探测机制修复;
- 同时具备 BIRD 国际基准的批量准确率评测能力,可量化迭代效果。

---

## 六、后续规划

| 方向 | 内容 |
|---|---|
| 接入真实业务库 | 选取 1–2 个真实业务场景试点,验证端到端效果 |
| 派生指标体系化 | 支持指标嵌套引用、口径治理与统一管理(向轻量"指标平台"演进) |
| 路由与召回持续调优 | 针对真实库的领域词、歧义概念做专项优化 |
| 评估闭环 | 用线上反馈反哺 prompt / 词表 / 检索参数,形成自迭代 |
| 权限与审计 | 接入数据权限、查询审计,满足生产合规 |

---

## 七、风险与边界(如实说明)

- **大模型非确定性**:同一问题可能生成不同 SQL,已用低温度 + 校验兜底,但不能 100% 保证。
- **概念歧义天花板**:一个中文概念对应多个候选字段时,模型倾向自信选择而非追问 —— 这是 LLM 的固有局限,靠词表口径明确可降低概率,无法根除。
- **依赖业务知识沉淀**:复杂业务口径仍需人工维护业务词表 / 派生指标,系统提供机制、不替代领域知识。
- **当前为基准与示例数据验证阶段**:尚未接入真实生产数据,投产前需完成权限、审计、真实库适配。

---

## 八、一句话总结

NL2SQL 已完成从"能跑"到"好用、可信、面向业务"的关键跨越:**核心闭环完整、检索与语义层成型、安全可解释、基准验证通过**,具备接入真实业务库试点的条件;下一步聚焦真实场景落地与持续调优。
