# 教学系统数据源注册表
# 默认运行时只连接教学业务库。需要接入其他教学业务数据库时，
# 应通过数据接入页面或在此文件中增加经过审核的 SQLite/PostgreSQL 数据源。

sources:
  - name: teaching
    label: 教学数据管理平台 (SQLite)
    url: sqlite:///data/teaching.db
    glossary: data/glossaries/teaching.md
    schema_profile: data/schema_profiles/teaching.yaml

  # 教学业务 PostgreSQL 接入示例：
  # - name: teaching_archive
  #   label: 教学历史库 (PostgreSQL)
  #   url: postgresql+psycopg://readonly_user:password@127.0.0.1:5432/teaching_archive
  #   glossary: data/glossaries/teaching_archive.md
