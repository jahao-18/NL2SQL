# 项目功能总览与维护基线

> 最后核对日期：2026-07-22
> 核对范围：当前仓库中的后端路由、核心服务、前端页面、数据模型、配置、脚本与自动化测试
> 文档性质：本文件是“当前已经实现什么”的唯一总览，不是产品愿望清单或开发计划

## 1. 文档状态说明

本文使用以下状态，避免把可选能力或未来计划误写成现成功能：

- **已实现**：当前代码已有完整入口，满足对应权限和运行条件即可使用。
- **条件可用**：代码已经实现，但依赖 API Key、可选服务、外部数据集或特定配置。
- **仅后端**：已有后端接口，当前网页没有对应操作入口。
- **未实现**：当前不存在可用的完整产品流程，仅用于说明边界。

本文不记录任何真实或演示账号的明文密码。测试账号及实验身份信息只维护在不提交版本库的 `docs/demo_accounts_private.md` 中。

## 2. 项目定位与总体架构

本项目是面向高校教学管理场景的智能问数与教学业务平台。它不只提供自然语言转 SQL，还包含账号注册与组织权限、教学作业、课程分析、学生支持、知识治理和数据接入等完整业务工作台。

系统由以下部分组成：

- FastAPI 后端统一提供认证、权限、教学业务、NL2SQL、治理和数据维护接口。
- 原生 HTML、CSS、JavaScript 单页前端，根据后端返回的角色能力动态展示导航与页面。
- 默认运行时只注册 SQLite 教学演示库；管理员仍可接入经过审核的教学业务 SQLite/PostgreSQL 数据源。
- Qwen/DashScope 用于数据源路由、查询扩展、SQL 生成、失败修复和结果裁判。
- Schema 检索先应用当前角色的表/字段授权：小型授权视图直接使用完整授权 Schema；大型授权视图再使用本地关键词/BM25 与向量并行召回、授权关系图补全，且可切换到 Elasticsearch、Milvus、PostgreSQL/pgvector 组成的服务端检索。
- JSON、JSONL、YAML 与 SQLite 共同保存画像、示例、反馈、治理项、审计和教学业务数据。

应用启动时会执行幂等数据迁移，补齐账号、组织、岗位、审核队列和教学工作流所需结构；可配置在后台预热检索索引。B.6 身份岗位迁移使用一次性迁移标记，首次完成旧账号转换后不会在后续启动中依据旧角色字段重新授权，也不会把已经撤销的正式岗位重新初始化。教师和辅导员权威目录仍会安全地增量投影到统一职工目录。V3 起新增的结构变更统一通过有序、带校验值的 `v3_schema_migration` 账本执行，并写入 SQLite schema version；待执行迁移处于同一事务，失败会整体回滚，禁止用不可追踪的手工改库代替正式迁移。前端静态站点挂载在根路径，API 统一位于 `/api`。

## 3. 用户、身份与权限体系

### 3.1 当前角色

| 角色 | 主要工作入口 | 数据范围与关键限制 |
|---|---|---|
| 学生 `student` | 我的首页、智能问数、我的课程、作业、课程分析、本人考勤、课程答疑、通知、学生支持、个人中心 | 仅本人数据；不能读取教师/学生名单、他人考勤、私密问题及全校汇总 |
| 教师 `teacher` | 教师首页、我的课程、作业管理、考勤管理、课程答疑、成绩提交、课程分析、个人中心 | 仅本人授课班级；问数可显示本人授课班学生姓名，但内部学生 ID、原始评教和全校数据不可输出 |
| 辅导员 `counselor` | 辅导员首页、智能问数、学生支持、审批中心、个人中心 | 仅被分配的行政班；可读取所带学生必要考勤事实但不读取教师备注、课程名单或答疑正文 |
| 学院负责人 `college_manager` | 学院首页、教学运行及异常助手、成绩审批、汇总问数、审批中心、组织管理、个人中心 | 仅所负责学院；问数仅开放课程运行、工作量和质量汇总表，异常助手不能打开其他学院事项 |
| 教务人员 `academic_office` | 教务首页、教学任务、教学异常助手、成绩发布、汇总问数、组织管理、个人中心 | 全校教学管理范围；问数仅开放教学运行汇总表，不浏览作业正文 |
| 身份审核员 `identity_reviewer` | 审批中心、个人中心 | 仅授权学院或队列中的注册申请 |
| 平台管理员 `admin` | 管理首页、智能问数、助手运营、知识库、治理、数据接入、审批、组织管理、个人中心 | 平台配置与高风险管理能力；助手运营仅展示脱敏聚合；操作写入审计 |
| 普通职工 `staff` | 个人中心 | 已核验职工身份，但尚未取得具体业务岗位 |
| 个人自助 `self_service` | 个人中心 | 隐藏基础身份；仅在没有可用业务身份时进入，不包含教学或管理数据权限 |
| 待审核用户 `pending` | 注册进度 | 尚无业务数据访问权 |

权限不是单一角色字符串。系统把“人员身份、在岗任职、角色绑定、数据作用域、当前登录工作身份”拆开保存，因此同一个人可以同时担任教师和辅导员，也可以在多个合法身份之间切换。

`app_user.role` 仅作为历史兼容字段保留，不再用于运行时授权、默认身份排序、数据范围兜底或注册审核人选择。运行时权限的唯一事实源是账号状态、已核验人员身份、当前有效的 `user_role_binding`、对应 `role_scope_binding` 以及仍在有效期内的岗位任职。

### 3.2 登录、注册与账号生命周期

已实现能力：

- 使用学号或工号作为登录标识，不要求额外记忆一套业务账号。
- 学生和职工可在登录页自助注册，填写官方姓名、学号或工号及密码。
- 注册信息必须与知识库/教学库中的已有人员身份匹配；注册者不能自行选择管理员、学院负责人等高权限角色。
- 学生申请进入所属班级/学院审核队列；职工申请进入其组织范围内的审核队列。
- 待审核用户可以查询自己的申请状态。
- 审核员可以按条件查看申请，逐条通过/驳回，也可以勾选后批量通过或批量驳回。
- 审核通过后建立人员身份绑定和基础角色；学生获得学生身份，普通职工先获得职工身份，教师基础身份可获得教师角色。
- 防止同一学号/工号重复注册、身份被重复占用或申请重复提交。
- 登录返回服务端生成的导航、功能和数据范围，前端不自行推断权限。
- 一个用户有多个有效任职时，登录后可选择工作身份，也可以在系统内切换身份。
- 切换身份会签发新会话并使旧令牌失效，避免旧权限继续使用。
- 用户可在响应式个人中心查看当前账号、已核验状态、岗位身份和数据范围，并在独立的账号安全卡片中修改密码；修改后撤销既有会话。
- 连续登录失败达到 5 次会锁定 15 分钟。
- 会话令牌包含签发时间、12 小时有效期、会话版本、当前角色绑定和签名校验。
- 已结束或到期的任职会自动使关联角色失效并撤销相关会话。
- 有效账号如果没有任何有效角色绑定，会被拒绝进入业务系统；修改旧账号表中的角色或范围字段不能产生权限。

### 3.3 尚未实现的账号边界

- 没有完整的“忘记密码—短信/邮件验证—重置密码”自助流程。数据库中的重置申请结构不等于产品功能已完成。
- 没有短信、邮件、企业微信或统一身份认证平台的真实集成。
- 没有开放注册高权限岗位；管理员、教务人员、学院负责人、辅导员和审核员必须通过组织岗位任命获得权限。
- 当前认证适合项目演示和内部验证，生产部署仍需 HTTPS、密钥托管、密码策略强化、集中审计和安全评审。

### 3.4 生命周期基础、影响预览与账号安全冻结

B.7-0 已实现账号、人员身份、校内关系和岗位退出的只读基础能力：

- 为每个已核验自然人建立 `person_affiliation` 学籍/任职关系基线。
- 建立生命周期事件、账号状态历史、责任交接和销户申请数据结构。
- 为已核验人员建立隐藏的 `self_service` 基础绑定，只包含个人中心权限，不包含教学或管理数据。
- 正常存在业务身份时，`self_service` 不出现在身份选择列表；所有业务绑定暂停后才作为最低权限入口。
- 提供本人生命周期详情、管理范围内人员详情和事件影响预览。
- 影响计算覆盖角色、范围、岗位、会话撤销要求、已领取审核任务、教学班、辅导员个案、学生选课/未交作业、最低岗位覆盖阻塞和交接要求。
- 学生、辅导员、学院负责人、身份审核员、教务人员和管理员按照本人、行政班或学院范围访问，不能跨范围查看人员生命周期。
- 除账号安全冻结/恢复外，B.7 生命周期接口仍为只读预览，响应明确返回 `execution_available = false`，不会真正执行休学、毕业、调岗、离职或销户。
- 平台管理员可在组织教职工目录对已核验账号执行安全冻结/恢复，均须填写依据并完成当前管理员密码二次验证。
- 冻结和恢复都会递增 `session_version` 并使旧令牌立即失效；冻结账号不能登录或签发新令牌。
- 冻结不删除人员、岗位或角色绑定，恢复只恢复账号状态，因此冻结期间已撤销、结束或到期的岗位不会被复活。
- 系统禁止冻结当前管理员自身，并确保冻结后仍至少保留两名正式平台管理员；成功、错误二验和保底拦截均留有审计记录。
- 教务处可在组织与岗位页面办理学生休学、复学、毕业、退学，并按筛选后的在读学生批量办理毕业；平台管理员不替代教务决定学籍事实。
- 休学暂停学生业务绑定、撤销旧会话并保留 `self_service` 最低权限；复学按当前学籍创建新的学生绑定，不重新启用历史绑定。
- 毕业/退学撤销学生业务绑定、归档账号并禁止重新登录，成绩、作业、考勤、选课和支持记录不删除；批量毕业先校验整批对象并以单事务执行。
- 职工跨学院调动、离职和退休使用演示阶段的“双具名管理员确认”流程：首位管理员发起并复验密码，另一位管理员确认执行。
- 调动关闭旧学院关系、旧岗位和旧范围，建立新学院关系但不自动授予新岗位；离职/退休撤销业务角色、归档账号并使旧会话失效。
- 已领取审核任务返回原组织队列；教学班和未关闭辅导个案写入责任交接异常清单，保留业务历史并等待教务或学院明确接任。

### 3.5 通知中心与课程空间（阶段 C）

- 通知中心提供当前账号的未读数、加载/空/错误态、列表和已读标记；后端按资源类型返回安全业务页面目标，用户点击“查看详情”时先标记本人通知已读，再跳转到课程、作业、答疑、支持或教学运行页面。
- 任课教师可向本人授课班发布或删除课程公告；系统只为该班已选课且账号正常的学生生成送达记录与站内通知。删除采用软删除，公告及对应学生通知立即下线，但送达记录与操作审计继续保留。
- 学生只能读取本人选课课程空间中的已发布公告和在可见时间内的资料；教师只能维护本人任课班的公告、资料和公告送达汇总。
- 课程空间后端聚合课程基本信息、公告与资料；作业和课程分析仍复用既有受课程成员关系约束的接口。
- 教师和学生导航已接入“我的课程”与“通知中心”：教师可在课程空间发布公告、查看并发布本人未发布公告草稿，上传不超过 20MB 的受控格式附件或登记 HTTP(S) 外部资料链接；课程成员可按权限下载真实附件，学生只能查看已发布课程内容和标记通知已读。附件使用随机存储键保存，下载时再次校验课程成员关系，不暴露服务器文件路径。
- “我的课程”采用独立的响应式课程工作台：顶部切换当前课程，侧栏展示课程概览和业务入口，主区分别呈现公告时间线与资料卡片，教师发布公告和资料使用分离的编辑卡片；窄屏下自动切换为单列布局。

### 3.6 课程场次、考勤与答疑（阶段 D）

- 任课教师可以为本人课程建立真实课程场次，记录日期、时间、教室和主题；旧考勤数据在幂等迁移中关联到对应场次，不再只依赖抽象课次编号。
- 教师按场次获取实际选课名单，批量登记出勤、迟到、请假或缺勤；再次保存属于带审计记录的修正，场次同步进入已完成状态。
- 学生只能查看本人各课程场次及本人考勤状态和教师可见备注，不能读取同班名单或其他学生考勤。
- 辅导员只能通过本人负责行政班关系查看所带学生的课程、课次、日期和考勤状态，不返回教师考勤备注或课程名单；这些事实继续供规则化学习支持使用。
- 学生可在本人课程发起公开或私密问题。公开问题对课程成员可见但学生端隐藏提问者身份字段；私密问题只对本人和任课教师可见。
- 教师可回复、关闭、重新打开和置顶本人课程问题；学生可对本人未关闭问题追问，状态按 `open -> answered -> closed` 流转，追问使 `answered -> open`。
- 新问题通知任课教师，教师回复通知提问学生；答疑和通知不依赖智能问数服务。

### 3.7 教务与学院工作台（阶段 E）

- 教学任务台账展示课程、学院、教师、学年学期、教室、容量、选课人数和成绩提交状态；台账与成绩提交列表均可收起/展开，并使用内部滚动避免页面过长。两类列表均支持课程/教师关键词搜索，以及按权限范围动态提供的学年、学院下拉选项，可与学期、成绩状态组合筛选、一键重置并显示结果数。教师仅看本人授课班，学院仅看本学院，教务查看全校管理范围。
- 教务可按确定性规则刷新未排教室、超容量和低选课教学异常；教务或所属学院可标记处理中或填写说明解决，跨学院处理被拒绝。
- 学院负责人和教务人员可从教学运行页携带当前学年、学期、学院和异常状态上下文进入统一助手；询问逾期或待处理异常时使用服务端固定查询，不调用模型生成 SQL。当前“逾期”按未解决异常自最后状态更新时间起停留至少 72 小时计算。
- 异常回答展示课程、学院、教师、规则依据、处理状态和停留小时；可确认定位当前范围内最优先异常，也可确认回到教学运行页应用“未解决/逾期”安全筛选。动作创建与确认均重新校验学院/教务范围，跨学院异常不能打开。
- 成绩状态按 `not_submitted -> submitted -> approved/returned -> published` 流转：教师提交，学院或教务审批/退回，教务发布；退回必须填写原因，状态变化通知教师。
- 学院工作台聚合本学院课程运行、教师班级数/学生人次和课程质量；成绩样本少于 5 时不展示均分和通过率排名。
- 教务智能问数只开放 `academic_teaching_operations_summary`；学院智能问数只开放本学院教学运行、教师工作量和质量汇总表，并继续强制 `college_id` 行级条件。

## 4. 组织、岗位与审核队列

### 4.1 组织模型

已实现学校、教务处、学院、班级等组织单元，并维护职工目录、行政班、岗位槽位、岗位任职、角色绑定、数据作用域、审核队列和审核任务。

演示数据包含多个学院。学院负责人不是全系统共用的单一 `college` 账号：每个学院可以拥有独立的负责人岗位和人员，权限自动限制在该学院。

### 4.2 岗位任职全生命周期

组织管理页面及接口支持：

- 按组织单元、岗位类型和状态查看岗位槽位、当前任职人及缺岗/多人占岗等覆盖问题。
- 搜索职工并创建正式、临时或代理任职。
- 为辅导员选择一个或多个负责行政班，形成班级级数据范围。
- 更新任职范围、任职类型和有效期。
- 对现有任职续期。
- 在结束任职前预览影响，包括将失去的角色、范围、队列和会话。
- 结束任职并同步撤销角色绑定、范围权限和会话。
- 原子化转任：在同一操作内结束原任职并创建接任关系，减少权限空档或重叠。
- 临时/代理任职必须设置到期时间；到期后自动失效。
- 对新增管理员、敏感岗位变更、结束和转任要求当前管理员再次输入密码确认。
- 至少保留两名实名正式管理员，阻止导致管理员人数跌破下限的操作。
- 阻止管理员给自己授予、撤销或转移高权限岗位。
- 高风险操作写入审计记录。

### 4.3 审核队列

- 支持查看当前用户可管理的注册审核队列和队列任务。
- 队列按组织和职责分配，不依赖一个全校通用学院账号。
- 学院审核员只处理被授权学院的申请；管理员和相应管理岗位按角色规则访问。
- 审核中心支持条件筛选、全选、批量通过、批量驳回和单条处理。

## 5. 角色首页与教学数据看板

登录后首先进入角色化工作台。后端返回当前角色可见的模块、摘要、快捷操作和业务状态；没有数据的普通区块可隐藏，重要状态区块会保留。

已实现角色首页：学生、教师、辅导员、教务人员、学院负责人和管理员。

教学看板支持：

- 按学期、学院、专业、课程类型等条件筛选。
- 展示学生规模、教师/课程/教学班、成绩、考勤、作业、评教、预警等汇总卡片或分布。
- 展示教师工作量、出勤情况、风险提示和可继续追问的快捷问题。
- 学生和教师数据自动收敛到本人/本人授课范围。
- 学院负责人自动限定本学院；教务和管理员按授权范围查看。

## 6. 智能问数（NL2SQL）

### 6.1 完整问数链路

一次问数会执行以下已实现流程：

1. 普通教学角色固定使用教学业务数据源；管理员可根据手动选择、当前上下文、提问和历史对话选择已配置的外部只读数据源。
2. 加载数据源结构、字段信息、样例值、枚举值、业务词表和已发布 Schema 画像。
3. 对大库/宽表执行 Schema linking，筛选相关表和列，并补齐连接所需桥接表。
4. 使用最近对话、业务词表、指标定义和 Few-shot 示例构造提示词。
5. 调用 Qwen 生成只读 SQL。
6. 校验 SQL 安全性、可访问表/字段和当前用户行级范围。
7. 以只读连接执行，并限制超时和最大返回行数。
8. 对可修复失败调用模型重写 SQL 后重试。
9. 返回表格、SQL、解释、数据源、检索轨迹和置信度任务信息。
10. 后台异步裁判召回质量及 SQL/结果匹配度；失败时可用规则分数兜底，不阻断查询结果。

### 6.2 自适应检索与知识增强

- 检索触发条件以已经完成角色过滤的授权 Schema 为准，不再以整库表数/列数决定普通角色是否检索；当前本地推荐阈值为授权后不超过 15 张表且不超过 120 列时直接使用完整授权结构。
- 大型授权视图并行使用关键词/BM25 与向量相似度两路召回，多路结果使用带权 RRF 融合，按表聚合后限制表数和宽表列数。
- 关系图作为召回后的结构信号，只能排序授权表，并且只允许通过授权表补齐 JOIN 桥接路径；召回原子、外键列和最终上下文同样再次按授权表/字段过滤。
- 本地业务词表不作为独立召回路，只在问题明确命中术语、条目引用表全部授权且与选中表相关时条件注入；指标公式仍按问题命中注入。
- 查询扩展可从中文问题中提取实体、属性及英文列名别名；当前本地运行配置关闭该额外模型调用以降低延迟，需要跨语言召回时可显式开启。
- 可缓存原子信息、索引和关系图，也可在启动后后台预热。
- **条件可用**：服务端检索可连接 Elasticsearch（含可选 IK 中文分词）、Milvus、PostgreSQL/pgvector；任一路不可用时相应检索器降级，不应导致主查询崩溃。

### 6.3 查询交互

智能问数页面支持：

- 普通教学角色固定使用 `teaching`；管理员可自动或手动选择已配置数据源。
- 保留浏览器内多轮对话历史并用于追问。
- 为每个数据源维护浏览器本地业务术语和指标表达式。
- 查看生成 SQL、结果表格、自然语言解释、检索/执行轨迹和置信度。
- 对结果提交有帮助/无帮助反馈。
- 低置信结果进入治理复核队列（受治理阈值和设置控制）。
- 将结果导出为 CSV，并对可能触发电子表格公式的单元格做转义保护。
- 对适合的数据生成简单图表。
- 查看查询历史与反馈状态。

### 6.4 SQL 安全与权限

- 只允许单条 `SELECT` 查询，禁止写入、DDL 和危险语句。
- 按角色白名单限制可访问表，按敏感字段策略限制输出列。
- 受限表禁止使用无法确认字段安全性的 `SELECT *`。
- 学生、教师、辅导员和学院角色的查询必须包含系统要求的行级范围条件。
- 行范围在每个 CTE/子查询块分别校验，范围等式必须作为独立顶层合取条件；范围查询保守禁止 `OR`、`UNION`、`INTERSECT` 和 `EXCEPT`，避免布尔或集合绕过。
- 敏感输出字段在外层查询、CTE 和子查询中统一检查，支持识别表别名、未限定列和带引号标识符。
- 禁止 SQL 注释、数据库文件读取/输出函数、延时函数和其他高风险只读副作用能力。
- 没有 `ask` 功能的身份不能读取数据源目录或调用问数；空表权限不会回退为全表。
- 执行器使用只读数据库连接，并限制查询时长及最大返回行数（默认 5 秒、200 行，可配置）。
- 问题中的越权意图、受保护资源和字段别名也会参与拦截，但数据库和部署层仍需保留纵深防护。

### 6.5 V3 统一助手基础

**后端、API 与学生、教师、辅导员业务入口已实现；学院和教务页面融合尚未实现**：

- 已定义冻结契约中的 12 个首版页面标识以及页面到角色功能的服务端映射。
- 页面上下文模型支持课程、学生、学院、学年、学期、带时区时间范围和不超过 4 KB 的筛选条件。
- 解析器在每次使用时重新确认账号状态、会话版本、当前角色绑定及关联岗位有效期；旧工作身份上下文不能在角色切换后继续使用。
- 课程上下文重新验证学生选课、教师任课、学院范围或教务/管理员范围；学生上下文重新验证本人或辅导员行政班关系；学院上下文重新验证当前身份所属范围。
- 无权页面、伪造对象、互相冲突的课程/学生/学院组合及在筛选条件中夹带权限字段会被明确拒绝。
- 与当前页面无关的字段会进入 `ignored_fields`，不会参与后续问数；最终上下文不包含核验过程中读取的教师、行政班等内部字段。
- 解析结果继续引用原始 `AuthContext`，不会增加表、字段、功能或行范围权限。
- 服务端会话严格绑定 `user_id + role_binding_id`，支持创建、列表、重命名、收藏、软删除和轮次分页读取；跨用户、跨工作身份和失效账号统一不可读取。
- 创建会话和追加轮次可使用客户端请求 ID 去重；重复删除返回稳定状态，软删除后不再出现在列表且不能继续追加轮次。
- 每轮只保存问题、回答类型、执行状态、经过验证的上下文、安全回答摘要、错误码和时间，不保存查询结果行、附件或完整回答正文。
- `assistant_saved_query` 已建立受当前工作身份隔离的数据结构，但收藏查询的服务方法、API 和页面入口尚未实现。
- `POST /api/assistant/query` 已将固定工作台计数、业务状态、页面导航、现有 NL2SQL 和不支持问题统一为同一响应结构，并自动创建或续用当前身份会话、保存轮次。
- 确定性计数和待办优先使用现有范围化工作台服务，不调用模型；带具体课程、学生或学院对象的统计不会走全范围工作台捷径。
- NL2SQL 分支继续使用现有表/字段/行级白名单、只读执行和失败回修链路；验证后的课程、学生和学院页面上下文会合并进 SQL 行级范围，不能只停留在展示层。
- 模型、检索、SQL 或编排器异常转换为结构化 `failed` 响应，不中断 FastAPI；权限型旧问数错误转换为 `rejected`；整个 NL2SQL 旧问数调用受 `llm_timeout_seconds` 硬超时约束，外部模型内部重试也不能让统一助手无限等待。
- 主 SQL 生成默认使用低延迟文本模型 `qwen-plus`，路由与查询扩展默认使用 `qwen-turbo`；Qwen3 多模态模型仍可通过 `QWEN_MODEL` 选择，且可用 `LLM_ENABLE_THINKING` 控制思考模式，默认关闭以避免 SQL 生成链路产生不必要的长延迟。
- 普通角色即使请求详细 trace 也只能获得安全摘要，管理员可以显式请求更多治理 trace；`include_sql` 和 `max_rows` 只能隐藏 SQL 或收紧返回结果。
- 原 `POST /api/ask` 保持兼容，基础教学业务接口与统一助手失败相互隔离。
- 已开放会话创建、列表、详情、重命名/收藏和软删除 API；跨用户或跨工作身份详情统一返回 404。

`assistant-view` 已改用 V3 通用助手组件，并在当前浏览器会话中续用服务端 `session_id`。学生和教师可从角色首页、课程空间、作业中心、本人/课程考勤和课程分析页打开同一组件；前端只提交当前页面和已选择教学班 ID，服务端仍对课程成员关系与当前工作身份求交。页面尚未提供历史会话列表、重命名、收藏、删除或跨刷新恢复；删除助手会话会同时取消该会话尚未确认的动作草稿。

### 6.6 V3 认证指标语义层

**仅后端与 API，尚未接入前端页面**：

- 版本控制的 `data/semantic_metrics.yaml` 已定义 21 个首批认证指标，覆盖作业、考勤、成绩、教学运行、学生支持和平台问数运维。
- 每项指标明确公式、业务来源、时间语义、适用角色、页面、内部行范围、最小样本量、下钻能力和单位；对外定义不暴露内部角色清单、行范围、物理表或 SQL。
- `GET /api/assistant/metrics` 按当前工作身份和已验证页面上下文返回可见指标，可进一步按 `code` 获取单项；未知或不可见指标返回 404。
- 统一助手优先识别认证指标名称和别名，固定接口与自然语言问法共用同一求值器，返回一致的值、样本量、分项、更新时间和定义。
- 指标计算继续服从学生本人、教师授课班、辅导员带班、学院和教务范围；伪造课程、学生或学院参数在计算前被上下文解析器拒绝。
- 零分母百分比返回空值；低于定义最小样本量时返回结构化小样本标记与警告，不把无数据伪装成 0%。
- 作业完成/迟交/未交/待批，考勤出勤/缺勤/连续缺勤，成绩提交/发布/分布/通过，教学异常和学习支持指标均已实现。
- 管理员问数成功率和降级率已实现；低置信率和阶段耗时已接入 V3-2.2 脱敏执行轨迹，按当前治理阈值和真实阶段样本计算。

当前没有独立前端指标卡或指标目录；通用助手可以展示自然语言命中的指标定义、结果和质量提示。跨周、跨月和跨学期趋势尚未建立历史快照，因此本节不能描述为完整的指标运营产品流程。

### 6.7 V3 回答证据与执行轨迹

**后端与 API 已实现，独立智能问数页已接入安全展示**：

- 统一助手响应会返回最终角色/页面/课程/学生/学院/学期范围摘要；页面时间范围包含带时区起止时间和口径说明。
- 认证指标证据补充指标编码、业务来源、时间语义和可取得的数据更新时间；NL2SQL 与固定业务统计返回数据来源及查询/生成时间。
- `warnings` 已使用 `code / severity / message` 结构化表示小样本、空结果、截断、低置信、检索降级和被忽略上下文。
- Schema 检索异常但安全回退后仍完成查询时，统一助手返回 `status=degraded`，并明确说明降级原因。
- 普通用户只看到上下文、路由、检索、模型、校验、执行等阶段的成功/失败/跳过状态；平台管理员仅在显式请求详细 trace 时看到阶段耗时、检索器名称、回修次数和安全降级原因。
- 对任何身份都不会返回完整 Prompt、密钥、连接地址、Schema 上下文片段、原始堆栈或未脱敏内部日志。
- V3 schema version 3003 新增 `query_execution_trace`，与助手轮次一对一保存路由、阶段聚合耗时、检索状态、降级、置信度、行数和截断状态；schema version 3005 为轨迹补充实际检索后端 `none / local / server`，供质量运营聚合使用。
- 执行轨迹表不保存问题正文、回答正文、完整页面上下文、SQL、结果行、附件正文或私密业务记录；轨迹写入故障不会使已完成回答失败。

通用助手组件已展示最终范围、指标口径、结果表、证据来源、结构化质量提示、SQL 折叠区和普通用户安全执行摘要，并区分空结果、低置信与检索降级状态。异步裁判生成的后续置信度尚不回写既有轨迹；本能力也不等同于生产级分布式链路追踪或监控告警。

### 6.8 V3 白名单动作草稿

**后端、API 与独立智能问数页的预览/确认交互已实现**：

- V3 schema version 3004 新增 `assistant_action_draft`，按 `user_id + role_binding_id` 隔离草稿，并可关联助手会话和轮次。
- 服务端动作类型严格限制为 `open_assignment_roster`、`open_course`、`open_support_case`、`open_teaching_issue`、`apply_safe_filter`、`draft_course_notification`、`submit_governance_feedback` 和 `export_current_result`；额外类型与额外参数均被拒绝。
- 草稿默认 15 分钟有效，保存服务端规范化参数、安全预览、状态和确认结果；客户端请求 ID 可用于同一工作身份内的幂等创建。
- 创建时校验页面能力、课程任课/选课、辅导员个案、学院教学异常、助手会话和轮次归属；确认时基于当前账号、会话版本和工作身份重新鉴权，不信任创建时结论。
- 草稿采用 `pending / confirmed / expired / failed / canceled` 状态约束；确认使用数据库写锁和条件更新，同一草稿不能重复执行，过期、取消及切换工作身份后的草稿不能确认。
- 导航类动作确认后只返回当前角色可访问的安全页面和范围化参数；当前结果导出确认后返回前端 CSV 导出指令，不在草稿表保存结果行。
- 课程通知动作在确认前不写业务公告；确认后只创建 `course_announcement.status=draft` 的通知草稿，不创建送达记录或站内通知。教师可在对应课程空间的“待发布草稿”区明确发布本人草稿，发布时再次校验任课关系和草稿归属，并创建送达记录与站内通知。
- 治理反馈确认后调用现有反馈与治理入队服务；动作草稿本身不扩展调用者的数据域、表、字段或行级权限。
- 统一助手会把服务端生成的页面导航建议和有结果的固定统计/NL2SQL 导出建议物化为真实动作草稿，响应只返回可确认的草稿标识、预览和过期时间，不再把未校验的客户端动作当成可执行指令。
- 教师在作业、考勤、课程空间或课程分析入口携带已验证课程上下文查询未交、未完成或缺勤学生名单且结果非空时，同一回答下会自动提供“保存全班课程提醒草稿（未发送）”；普通统计、空名单、学生身份和无课程上下文不会生成该动作。
- 教师询问“哪些人/哪些同学没交或未完成作业”等名单问题时，统一助手使用服务端固定查询本人授课班的作业提交记录，不使用模型生成 SQL；结果只显示学生姓名、课程、作业和截止时间，仍受当前课程上下文进一步收窄。

通用助手组件会展示动作类型、服务端预览、确认边界与过期/失败结果；确认时只提交 `draft_id`，导航使用服务端返回的目标页面，CSV 使用服务端确认后才导出组件当前持有的结果。教师既可从课程空间明确提出“生成提醒草稿”，也可在范围化风险名单查询后使用结果联动动作；确认后会自动带到对应课程空间的“待发布草稿”，仍须单独点击发布才会通知学生。当前提醒草稿是当前课程全体已选学生可见的课程公告草稿，并非只面向查询结果名单的定向提醒；系统也没有主动倒计时或过期刷新。

### 6.9 V3 通用前端助手组件

**已在独立智能问数页及学生、教师、辅导员、学院负责人和教务业务入口复用**：

- 新增独立 `assistant-panel.js` 与完全作用域化的 `assistant-panel.css`，未继续把 V3 交互堆入既有单体问数函数；旧问数 DOM 暂时保留为隐藏兼容结构，不再作为页面可见入口。
- 组件覆盖初始推荐问题、提问中、澄清、成功、空结果、低置信、检索降级、无权限、服务异常及动作草稿预览/确认状态。
- 学生和教师可从角色首页、课程空间、作业中心、考勤和课程分析页进入同一组件；切换入口时组件重置会话并上报来源页面，课程相关入口仅携带已选教学班 ID，不在客户端判断权限。
- 组件调用统一助手 API，当前页面内续用服务端会话；切换工作身份时由应用事件清空前端会话和当前结果，避免旧身份草稿或上下文继续显示；请求超过 50 秒会由浏览器主动中止并退出加载状态。
- 回答区域支持最终范围、时间范围、认证指标口径、结果表格、证据来源、结构化警告、SQL、执行摘要、推荐追问和动作草稿。
- 前端不读取角色能力、表白名单、字段黑名单或行范围，不根据当前用户推断授权；只提交页面上下文，并消费服务端返回的回答和动作。
- 动态内容统一通过 DOM `textContent` 写入，不使用动态 `innerHTML`；输入限制为 500 字，支持表单提交与 `Ctrl/Command + Enter`，状态区使用 `aria-live`，并提供窄屏和减少动态效果样式。
- 动作确认只使用服务端草稿 ID；安全导航由主应用接收服务端目标页面，导出动作在确认成功后将当前结果生成为 CSV，业务草稿和治理反馈展示服务端确认结果。
- 辅导员可从学习支持工作台携带 `support_workbench` 上下文进入助手，入口预填“本周有哪些待复查事项”；待复查与学生预约使用服务端固定范围查询，不调用模型生成 SQL。
- 待复查回答展示学生姓名、行政班、支持事项、事实依据、复查时间和逾期状态，并可确认“打开最优先个案”；服务端在动作创建与确认时均校验个案归属，确认后页面直接选中该个案，实际联系、复查和状态变更仍由辅导员人工记录。
- 学院负责人和教务人员可从教学运行页询问逾期/待处理教学异常；回答由服务端按当前学院或教务范围固定查询，并可确认定位最优先异常或应用异常状态筛选。确认导航后页面会加载现有异常处理工作台、高亮目标异常，但不会自动改变异常状态或填写处理说明。

当前组件仍集中显示在独立智能问数页，而角色业务页通过受控入口带入上下文；尚没有服务端历史会话管理、附件输入、语音输入或浏览器端端到端自动化验收。

### 6.10 V3 助手质量运营

**管理员页面与只读聚合 API 已实现**：

- 管理员导航新增“助手运营”，支持近 7、30、90 天周期切换和手动刷新；其他角色既不获得导航能力，调用聚合接口也会返回 403。
- 页面展示查询总量、成功率、P50/P95 响应耗时、轨迹覆盖率、越权拒绝、低置信查询、负向反馈和待治理数量。
- 失败按路由、检索、模型、SQL 校验、执行和其他阶段聚合；检索按本地、服务端、未使用和降级统计，并展示本地/服务端占比及降级率。
- 每日趋势、高频类别和低置信类别只按页面、回答类型与内部路由聚合；负向反馈只按受控数据源类别与反馈类别聚合，待治理事项只按受控治理类型聚合。
- API 不查询或返回问题正文、生成 SQL、结果行、反馈原因和治理标题；未知或自定义反馈类别、来源、治理类型会归入“其他”，避免借分类字段带出私密文本。
- 历史轨迹在 3005 迁移前没有实际检索后端信息，统一记作 `none`；当前统计来自单机 SQLite 轨迹及 JSONL 治理数据，不等同于生产级可观测平台、告警或分布式追踪。

## 7. 作业全流程

### 7.1 教师侧

- 查看本人授课班级及班级详情。
- 创建作业草稿，设置标题、说明、截止时间等内容。
- 修改草稿并发布给教学班。
- 查看作业提交名单和每位学生的提交状态。
- 查看提交详情、历史版本和附件。
- 将提交退回修改并填写说明。
- 录入成绩和评语；未发布成绩对学生不可见。
- 批量发布该作业的已评分成绩。

### 7.2 学生侧

- 查看与本人选课对应的作业列表和作业详情。
- 提交文本内容和/或文件附件。
- 在规则允许时重新提交，系统保存版本历史和原附件。
- 查看教师退回原因并再次提交。
- 只有教师发布成绩后才能看到分数和评语。
- 只能下载本人提交版本的附件。

### 7.3 共同规则

- 教师只能管理本人实际授课班级，学生只能操作本人提交。
- 作业、提交、退回、评分、成绩发布均有状态校验。
- 关键动作写入教学审计并生成站内通知数据。
- 到期作业的状态由迁移/维护逻辑自动更新。

## 8. 课程分析

- 教师可按本人授课班级查看分析上下文；学生只看到本人课程上下文。
- 提供预计算的课程摘要和指标定义，降低重复分析开销。
- 支持模板化教学问题，也支持在权限范围内回退到 NL2SQL 分析。
- 保存分析查询日志、结果状态和错误信息。
- 展示个人分析历史。
- 用户可以对分析结果标记“有帮助/无帮助”。
- 分析刷新或失败与作业提交流程隔离，不会因为统计失败而破坏作业业务数据。

## 9. 学生支持与辅导员工作台

### 9.1 规则化支持个案

- 根据透明规则从考勤、作业等证据生成支持个案，例如连续/累计缺勤或多次未交作业。
- 辅导员只看到本人负责行政班的学生个案。
- 个案展示触发原因、证据、建议行动、跟进时间、联系人记录和学生反馈。
- 状态流转覆盖待处理、已联系、跟进中、已改善和已关闭等业务阶段。
- 可控制部分支持信息是否对学生可见，保护内部工作记录。
- 刷新个案、状态变化和跟进记录进入日志、通知和审计。

### 9.2 学生求助/预约

- 学生可以创建咨询或预约请求。
- 请求按班级关系路由到对应辅导员。
- 学生查看自己的请求，辅导员查看权限范围内的请求。
- 辅导员可以回复、安排和更新请求状态。
- 学生不能查看其他学生的个案、请求或辅导员内部信息。
- 辅导员助手已接入本工作台，可确定性查询本人范围内本周应复查、已逾期复查和待处理预约；回答展示已有事实依据但不会据模糊行为自动创建正式风险标签。
- 助手的个案定位动作只打开本人负责个案，确认后进入现有详情与人工跟进表单，不自动写入联系记录、学生反馈、复查时间或状态。

## 10. 知识库、Schema 画像与质量治理

### 10.1 知识库与 Schema 控制台

- 查看已注册数据源、数据源概况、健康/质量信息、示例和反馈。
- 搜索、展开并查看表和字段结构。
- 编辑表业务名、表说明、字段业务名、字段说明、枚举、单位、语义类型、聚合方式和默认过滤条件。
- 标记字段启用状态、敏感状态和废弃状态。
- 维护表关系和业务指标定义。
- 自动合并数据源原始 Schema、业务词表和结构化画像。
- 生成画像质量报告。
- 保存画像、创建版本快照、发布版本和回滚到历史版本。
- 更新或删除允许操作的版本记录。

### 10.2 治理复核队列

- 由用户负反馈、低置信问数、画像发布以及字段/关系/指标变更形成治理项。
- 按数据源、状态和类型筛选治理项。
- 设置状态、优先级、负责人和处理说明。
- 接受治理项时可沉淀为 Few-shot 示例、字段画像、表关系、指标定义或画像发布。
- 可填写原因驳回治理项。
- 治理设置包括：发布是否必须复核、错误反馈是否自动入队、低置信阈值、发布说明是否必填、默认状态筛选和页面条数。
- 治理复核和设置页面仅管理员可用。

### 10.3 反馈与示例

- 保存问数正向/负向反馈并支持列表和删除。
- 保存、更新、列出和删除 Few-shot 问答/SQL 示例。
- 治理接受动作可自动写入示例或画像；删除相关反馈时会清理关联治理项。

## 11. 数据源接入与数据维护

管理员数据接入页支持：

- 查看 `data_sources.yaml` 中注册的数据源及其只读/可写状态。
- 使用数据库 URL 测试连接。
- 注册已有 SQLite 文件路径。
- 粘贴/导入 CSV 内容并生成受管理的 SQLite 数据源。
- 扫描数据源结构和表。
- 分页浏览表数据。
- 对明确标记为 `writable` 的 SQLite 数据源执行受控新增、修改和删除。
- 查看数据维护审计日志。
- 校验数据源名、表名、文件名和上传大小；默认上传上限为 20 MB，可配置。

**仅后端**：`POST /api/data-access/sources/db-file` 支持以 Base64 内容导入 SQLite 数据库文件，当前网页只展示 SQLite 路径注册和 CSV 两个接入标签。

数据维护不是任意 SQL 控制台：只有管理员、只有允许写入的数据源、只有受控的行级 CRUD 接口可以修改数据。

## 12. 前端页面与通用交互

当前单页应用包含以下可路由工作视图，是否显示由当前角色的服务端导航决定：

| 页面 | 主要用途 |
|---|---|
| 登录/注册/进度/身份选择面板 | 登录、自助注册、申请状态、选择工作身份 |
| 角色首页 `dashboard-view` | 角色摘要、待办、快捷入口和教学看板 |
| 课程分析 `course-analytics-view` | 课程上下文、摘要问数、历史和反馈 |
| 作业中心 `assignment-workflow-view` | 作业创建、发布、提交、退回、评分和成绩发布 |
| 课程空间 `course-space-view` | 课程公告、真实资料附件及作业/考勤/答疑入口 |
| 课程考勤 `attendance-view` | 教师建立场次、按名单点名和修正；学生查看本人记录 |
| 课程答疑 `course-questions-view` | 学生公开/私密提问与追问；教师回复、关闭和置顶 |
| 教学运行 `teaching-operations-view` | 教师成绩提交、学院审批与聚合、教务任务/异常/成绩发布；学院与教务可询问逾期/待处理异常并确认筛选或定位 |
| 学生支持 `support-workbench-view` | 支持个案、跟进流转、咨询/预约请求；辅导员可携带页面上下文询问待复查/预约并安全定位本人个案 |
| 组织管理 `organization-view` | 组织、岗位覆盖、任职、续期、结束和转任 |
| 身份审批 `identity-approval-view` | 注册申请筛选、单条和批量审核 |
| 个人中心 `profile-view` | 响应式身份概览、账号状态、当前角色/数据范围和分区式修改密码表单 |
| 通知中心 `notifications-view` | 当前账号站内通知、未读状态、标记已读和安全业务页面跳转 |
| 智能问数 `assistant-view` | V3 统一助手问答、范围/口径/证据/质量状态、安全执行摘要、动作草稿确认和确认后 CSV 导出；历史会话管理尚未接入 |
| 知识库列表 `kb-list-view` | 数据源及知识资产入口 |
| 知识库概览 `kb-overview-view` | 单数据源质量、示例和反馈概览 |
| Schema 控制台 `schema-console-view` | 表字段、关系、指标、画像和版本管理 |
| 治理队列 `governance-queue-view` | 复核、分派、接受或驳回治理项 |
| 治理设置 `governance-settings-view` | 入队、发布、阈值和列表设置 |
| 助手运营 `assistant-quality-view` | 管理员查看脱敏的问数成功率、耗时、失败阶段、检索使用、反馈与治理聚合 |
| 数据接入 `data-access-view` | 数据源注册、扫描、表数据维护和审计 |

通用交互还包括动态面包屑和页标题、服务端权限导航、加载/空状态/错误提示、弹窗表单、分页、筛选、批量选择、角色切换、退出登录，以及浏览器本地保存的部分问数偏好。

## 13. 数据模型与演示数据

默认教学库覆盖：

- 学院、专业、行政班、学生、教师。
- 课程、先修关系、教学班、选课。
- 成绩、评教、真实课程场次、场次化考勤、学习活动。
- 作业、提交、提交版本、评分和通知。
- 奖学金与学业预警。
- 学生支持个案、跟进日志和咨询请求。
- 应用用户、身份申请、人员身份、职工目录。
- 组织单元、岗位槽位、任职、角色绑定、数据范围、审核队列和任务。
- 人员学籍/任职关系、生命周期事件、账号状态历史、责任交接和销户申请。
- 课程公告、资料、课程问题、答疑回复、课程分析缓存、查询日志、业务审计和安全审计。
- 教学异常、成绩提交状态、教务教学运行汇总、学院教师工作量和质量汇总。
- V3 迁移账本、按用户和工作身份隔离的助手会话、助手轮次、收藏查询结构、记录实际检索后端的脱敏执行轨迹和短期动作草稿。

仓库提供教学演示数据生成/补种脚本。默认 `data_sources.yaml` 只注册 `teaching` 教学业务库，不再包含外部评测数据库。

## 14. 运维、配置与评测工具

### 14.1 主要配置

- DashScope API Key、认证签名密钥、演示模式和 Qwen 模型。
- 查询最大行数、SQL 超时、LLM 超时和上传大小。
- 枚举发现上限。
- 置信度裁判开关、模型、权重、样本行数、兜底时间和线程数。
- 检索开关、本地/服务端后端、授权后表/列触发阈值、召回/保留数量、查询扩展开关、ES/Milvus/pgvector 地址、融合权重和预热开关；仓库本地配置启用自适应 `local` 检索、关闭查询扩展，教师/辅导员等小型授权视图通常不会调用 embedding。
- 数据源连接和只读/可写属性维护在 `data_sources.yaml`。

### 14.2 工具与部署支持

- `scripts/seed_teaching_db.py` 和 `scripts/seed_db.py` 用于生成/补充演示数据库。
- `scripts/eval.py` 用于教学场景 NL2SQL 评测。
- `scripts/eval_v3_scenarios.py` 读取版本化的 `tests/v3_scenario_eval_cases.yaml`，校验并执行六角色 V3 助手场景；默认在临时教学库运行确定性用例，`--include-model` 才执行依赖当前模型/检索配置的场景，JSON 报告只保存场景 ID、类别、角色、状态和失败原因，不保存问题或回答正文。
- `scripts/verify_server_backend.py` 使用教学库验证服务端检索依赖。
- `scripts/run_stage_f_regression.py` 在独立临时数据库中一键运行六角色权限矩阵和公告、作业、考勤、答疑、支持、异常及成绩审批演示链路；`docs/stage_f_demo_runbook.md` 提供手工演示顺序。
- `scripts/start_lan_server.ps1` 用于局域网启动。
- `docker-compose.yml` 与 `docker/es/Dockerfile` 提供可选的 Elasticsearch、Milvus、etcd、MinIO 和 PostgreSQL/pgvector 检索组件。
- FastAPI 自带 OpenAPI 文档；`GET /api/health` 提供运行状态、模型/检索等基础信息。

## 15. API 完整清单

以下清单以当前代码路由为准。除登录、注册及必要的状态查询外，业务接口均要求有效会话，并由后端继续检查角色和数据范围。

### 15.1 认证、组织与工作台

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/health` | 健康和运行配置摘要 |
| GET | `/api/auth/options` | 登录页可用配置/演示选项 |
| POST | `/api/auth/login` | 学号/工号登录 |
| POST | `/api/auth/switch-role` | 切换当前工作身份 |
| POST | `/api/auth/register` | 学生或职工自助注册 |
| GET | `/api/auth/session` | 校验和恢复会话 |
| GET | `/api/auth/application` | 查询本人的注册申请状态 |
| GET | `/api/auth/applications` | 查看权限范围内申请 |
| POST | `/api/auth/applications/batch-review` | 批量审核申请 |
| POST | `/api/auth/applications/{application_id}/review` | 单条审核申请 |
| GET | `/api/auth/me` | 当前用户、角色、范围和导航 |
| POST | `/api/auth/password` | 修改当前用户密码 |
| GET | `/api/organization/units` | 组织单元列表 |
| GET | `/api/organization/staff` | 职工目录搜索 |
| GET | `/api/organization/class-groups` | 行政班列表 |
| GET | `/api/organization/position-slots` | 岗位槽位和任职覆盖 |
| POST | `/api/organization/position-assignments` | 创建岗位任职 |
| GET | `/api/organization/position-assignments/{assignment_id}/impact` | 结束任职影响预览 |
| PATCH | `/api/organization/position-assignments/{assignment_id}` | 更新或续期任职 |
| POST | `/api/organization/position-assignments/{assignment_id}/end` | 结束任职 |
| POST | `/api/organization/position-assignments/{assignment_id}/transfer` | 原子转任 |
| GET | `/api/organization/review-queues` | 可管理审核队列 |
| GET | `/api/organization/review-queues/{queue_id}/tasks` | 队列任务 |
| GET | `/api/lifecycle/me` | 本人的生命周期详情（只读） |
| GET | `/api/lifecycle/me/impact` | 本人账号事件影响预览（只读） |
| GET | `/api/lifecycle/people/{person_identity_id}` | 权限范围内人员生命周期详情（只读） |
| GET | `/api/lifecycle/people/{person_identity_id}/impact` | 离校、离职、调岗、停用或销户影响预览（只读） |
| POST | `/api/lifecycle/people/{person_identity_id}/security-suspend` | 平台管理员安全冻结账号（依据与二次验证必填） |
| POST | `/api/lifecycle/people/{person_identity_id}/security-restore` | 平台管理员恢复安全冻结账号（依据与二次验证必填） |
| GET | `/api/lifecycle/students` | 教务处学生学籍生命周期清单 |
| POST | `/api/lifecycle/students/{person_identity_id}/{event_type}` | 教务处办理休学、复学、毕业或退学 |
| POST | `/api/lifecycle/students/batch-graduation` | 教务处批量毕业（整批预校验、最多 500 人） |
| POST | `/api/lifecycle/staff/{person_identity_id}/events/{event_type}/request` | 发起职工离职或退休双确认事项 |
| POST | `/api/lifecycle/staff/{person_identity_id}/transfer/request` | 发起跨学院调动双确认事项 |
| POST | `/api/lifecycle/staff/events/{event_id}/confirm` | 另一名具名管理员确认并执行人事事项 |
| GET | `/api/notifications` | 当前账号通知列表、未读数和安全业务页面跳转目标 |
| POST | `/api/notifications/{notification_id}/read` | 标记本人通知已读 |
| GET | `/api/teaching/classes/{teaching_class_id}/space` | 课程成员读取课程空间公告与资料 |
| DELETE | `/api/teaching/announcements/{announcement_id}` | 任课教师软删除本课程公告并撤下对应通知 |
| GET | `/api/teaching/resources/{resource_id}/file` | 课程成员鉴权下载课程资料附件 |
| POST | `/api/teaching/classes/{teaching_class_id}/announcements` | 任课教师发布课程公告并送达学生 |
| POST | `/api/teaching/announcements/{announcement_id}/publish` | 任课教师显式发布本人课程公告草稿，并创建送达记录与学生通知 |
| GET | `/api/teaching/announcements/{announcement_id}/receipts` | 任课教师查看公告送达/已读汇总 |
| POST | `/api/teaching/classes/{teaching_class_id}/resources` | 任课教师上传课程附件或登记外部链接 |
| GET | `/api/business-domains` | 当前角色可用业务域与限制 |
| GET | `/api/teaching/dashboard` | 角色范围内教学看板 |
| GET | `/api/workbench` | 角色首页聚合数据 |

### 15.2 智能问数、知识库与画像

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/sources` | 当前角色可见数据源 |
| GET | `/api/schema` | Schema 信息（管理员） |
| GET | `/api/profile` | 获取 Schema 画像（管理员） |
| PUT | `/api/profile` | 保存 Schema 画像 |
| GET | `/api/profile/versions` | 画像版本列表 |
| POST | `/api/profile/publish` | 发布画像 |
| POST | `/api/profile/rollback` | 回滚画像 |
| PUT | `/api/profile/versions/{version_id}` | 修改版本元数据 |
| DELETE | `/api/profile/versions/{version_id}` | 删除允许删除的版本 |
| GET | `/api/quality` | Schema 画像质量报告 |
| POST | `/api/ask` | 自然语言问数主接口 |
| POST | `/api/assistant/query` | V3 统一助手入口：固定计数、业务状态、导航、NL2SQL 与不支持响应 |
| GET | `/api/assistant/metrics` | 当前工作身份与页面上下文可见的认证指标及统一求值结果（仅后端） |
| GET | `/api/assistant/quality-operations` | 管理员按 1～90 天窗口读取不含原始问题、SQL、结果行和反馈原因的助手质量聚合 |
| POST | `/api/assistant/action-drafts` | 创建经过白名单参数校验和首次鉴权的短期动作草稿（仅后端） |
| GET | `/api/assistant/action-drafts/{draft_id}` | 读取当前用户当前工作身份的动作草稿、预览和状态（仅后端） |
| POST | `/api/assistant/action-drafts/{draft_id}/confirm` | 重新鉴权并单次确认执行动作草稿（仅后端） |
| GET | `/api/assistant/sessions` | 当前用户当前工作身份的助手会话分页列表 |
| POST | `/api/assistant/sessions` | 创建带服务端验证上下文的助手会话 |
| GET | `/api/assistant/sessions/{session_id}` | 读取当前工作身份会话及分页轮次 |
| PATCH | `/api/assistant/sessions/{session_id}` | 重命名或收藏当前工作身份会话 |
| DELETE | `/api/assistant/sessions/{session_id}` | 软删除当前工作身份会话 |
| POST | `/api/judge` | 查询结果置信度裁判/查询 |
| POST | `/api/feedback` | 新增结果反馈 |
| GET | `/api/feedback` | 反馈列表 |
| DELETE | `/api/feedback/{item_id}` | 删除反馈 |
| GET | `/api/examples` | Few-shot 示例列表 |
| POST | `/api/examples` | 新增或更新示例 |
| DELETE | `/api/examples/{item_id}` | 删除示例 |

### 15.3 教学作业、课程分析与学生支持

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/teaching/analytics/contexts` | 可分析课程上下文 |
| GET | `/api/teaching/analytics/summary` | 课程分析摘要 |
| POST | `/api/teaching/analytics/ask` | 权限范围内课程问数 |
| GET | `/api/teaching/analytics/history` | 分析历史 |
| PATCH | `/api/teaching/analytics/history/{log_id}/feedback` | 分析结果反馈 |
| GET | `/api/teaching/classes/{teaching_class_id}/sessions` | 课程成员查看课程场次及本人/汇总考勤状态 |
| POST | `/api/teaching/classes/{teaching_class_id}/sessions` | 任课教师建立课程场次 |
| GET | `/api/teaching/sessions/{session_id}/attendance` | 教师读取点名名单或学生读取本人场次记录 |
| PUT | `/api/teaching/sessions/{session_id}/attendance` | 任课教师批量登记或修正场次考勤 |
| GET | `/api/teaching/attendance/students/{student_id}` | 学生本人或所属辅导员读取范围收敛的考勤事实 |
| GET | `/api/teaching/classes/{teaching_class_id}/questions` | 课程成员按公开/私密规则读取答疑列表 |
| POST | `/api/teaching/classes/{teaching_class_id}/questions` | 学生在本人课程发起问题 |
| GET | `/api/teaching/questions/{question_id}` | 按可见范围读取问题与回复 |
| POST | `/api/teaching/questions/{question_id}/replies` | 教师回复或提问学生追问 |
| PATCH | `/api/teaching/questions/{question_id}` | 任课教师关闭、重新打开或置顶问题 |
| GET | `/api/teaching/operations/tasks` | 按教师/学院/教务范围读取教学任务台账 |
| GET | `/api/teaching/operations/filter-options` | 读取当前角色数据范围内可用的学年和学院筛选项 |
| POST | `/api/teaching/operations/issues/refresh` | 教务刷新确定性教学异常 |
| GET | `/api/teaching/operations/issues` | 教务或学院读取范围内异常 |
| PATCH | `/api/teaching/operations/issues/{issue_id}` | 标记异常处理中或填写说明解决 |
| GET | `/api/teaching/grade-submissions` | 按关键词、学年、学院、学期和状态读取范围内成绩提交列表 |
| POST | `/api/teaching/classes/{teaching_class_id}/grade-submissions` | 任课教师提交课程成绩状态 |
| POST | `/api/teaching/grade-submissions/{submission_id}/review` | 学院/教务审批退回或教务发布 |
| GET | `/api/teaching/operations/summary` | 教务运行或学院工作量/质量聚合 |
| GET | `/api/teaching/classes` | 可访问教学班 |
| GET | `/api/teaching/classes/{teaching_class_id}` | 教学班详情 |
| GET | `/api/teaching/classes/{teaching_class_id}/assignments` | 班级作业列表 |
| GET | `/api/teaching/classes/{teaching_class_id}/submissions` | 班级提交列表 |
| POST | `/api/teaching/classes/{teaching_class_id}/assignments` | 创建作业 |
| GET | `/api/teaching/assignments/my` | 我的作业 |
| GET | `/api/teaching/assignments/{assignment_id}` | 作业详情 |
| PUT | `/api/teaching/assignments/{assignment_id}` | 修改作业 |
| POST | `/api/teaching/assignments/{assignment_id}/submit` | 学生提交/重新提交 |
| POST | `/api/teaching/assignments/{assignment_id}/publish` | 发布作业 |
| GET | `/api/teaching/assignments/{assignment_id}/roster` | 作业名单与状态 |
| POST | `/api/teaching/assignments/{assignment_id}/publish-grades` | 批量发布成绩 |
| GET | `/api/teaching/submissions/{submission_id}` | 提交详情 |
| GET | `/api/teaching/submissions/{submission_id}/versions/{version_no}/file` | 下载授权附件 |
| POST | `/api/teaching/submissions/{submission_id}/return` | 退回修改 |
| POST | `/api/teaching/submissions/{submission_id}/grade` | 评分与评语 |
| GET | `/api/teaching/support/cases` | 支持个案列表 |
| POST | `/api/teaching/support/cases/refresh` | 按规则刷新个案 |
| GET | `/api/teaching/support/cases/{case_id}` | 个案详情 |
| POST | `/api/teaching/support/cases/{case_id}/transition` | 个案状态与跟进流转 |
| GET | `/api/teaching/support/requests` | 咨询/预约请求列表 |
| POST | `/api/teaching/support/requests` | 学生创建请求 |
| PATCH | `/api/teaching/support/requests/{request_id}` | 辅导员回复或更新请求 |

### 15.4 治理与数据接入

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/governance/review-items` | 治理项列表 |
| PATCH | `/api/governance/review-items/{item_id}` | 更新治理项 |
| POST | `/api/governance/review-items/{item_id}/accept` | 接受并沉淀治理项 |
| POST | `/api/governance/review-items/{item_id}/reject` | 驳回治理项 |
| POST | `/api/governance/review-items/low-confidence` | 创建低置信复核项 |
| GET | `/api/governance/settings` | 获取治理设置 |
| PUT | `/api/governance/settings` | 保存治理设置 |
| GET | `/api/data-access/sources` | 接入数据源列表 |
| POST | `/api/data-access/test` | 测试连接 |
| POST | `/api/data-access/sources/sqlite` | 注册 SQLite |
| POST | `/api/data-access/sources/csv` | 导入 CSV |
| POST | `/api/data-access/sources/db-file` | 导入 DB 文件（仅后端） |
| POST | `/api/data-access/sources/{source}/scan` | 扫描数据源 |
| GET | `/api/data-access/sources/{source}/tables/{table}/rows` | 分页查看表数据 |
| POST | `/api/data-access/sources/{source}/tables/{table}/rows` | 新增数据行 |
| PATCH | `/api/data-access/sources/{source}/tables/{table}/rows/{pk}` | 修改数据行 |
| DELETE | `/api/data-access/sources/{source}/tables/{table}/rows/{pk}` | 删除数据行 |
| GET | `/api/data-access/audit` | 数据维护审计日志 |

## 16. 自动化测试与当前质量基线

当前测试集覆盖：

- 注册匹配、重复保护、申请状态、单条和批量审核。
- 多角色登录、角色切换、令牌失效和密码修改。
- 旧 `app_user.role`/范围字段篡改不影响权限、无绑定账号拒绝访问及迁移重复执行不重新授权。
- V3 迁移首次执行、重复执行、旧教学库升级、历史校验漂移拒绝及任意中途失败的完整事务回滚。
- 角色表/字段/行级权限、越权问数和 SQL 安全。
- 智能问数主链路成功/回修/失败隔离，以及注释、字符串、CTE、子查询、集合查询、布尔反转、危险函数、跨数据源和空权限攻击回归。
- 自适应检索按角色过滤后的表/列数量决定是否启用；召回候选、图排序、桥接路径、外键列和条件术语均不能重新引入未授权表或敏感非关联字段；受限主外键只可用于 JOIN/WHERE，模型误选未授权表或受限输出字段时必须先安全回修且校验通过前不得执行。
- 任课教师可在本人授课班范围内问数查看学生姓名，查询必须落实 `teacher_id`/课程关系，内部 `student_id` 仍禁止作为结果输出。
- 助手页面上下文模型、页面功能复核、课程/学生/学院范围校验、无关字段忽略、权限筛选拦截及角色切换后的旧上下文失效。
- 助手会话跨用户/跨身份隔离、冻结账号拒绝、创建和轮次幂等、列表及轮次分页、重命名收藏、软删除与删除后写入拒绝。
- 统一助手固定计数、业务状态、页面导航、不支持和 NL2SQL 分流，旧问数响应转换、结构化失败、上下文 SQL 范围落实、trace 最小化、结果收紧、旧 `/api/ask` 兼容及基础业务隔离。
- 认证指标目录安全输出、角色/页面可见性、固定接口与智能问法口径一致、课程越权拒绝、小样本提示、条件不可用和未知指标处理。
- 回答范围与证据补全、结构化空结果/截断/低置信/降级提示、普通用户与管理员 trace 差异、敏感内容不落轨迹表、阶段耗时持久化及轨迹驱动指标。
- 管理员助手运营聚合、P50/P95、失败阶段、实际检索后端、越权拒绝、低置信/负向反馈/待治理类别，以及非管理员拒绝和私密问题/原因不进入响应。
- V3 场景评测集契约、六角色覆盖、固定指标、NL2SQL、真实会话追问、模糊澄清、越权、空结果、小样本和检索降级类别，以及回答范围、禁止内容、SQL 约束和动作草稿策略；确定性评测在临时数据库运行，检索降级使用受控故障注入。
- 动作类型和参数白名单、对象范围篡改拒绝、确认时重新鉴权、过期与重复确认拒绝、跨工作身份隔离、会话删除取消草稿，以及课程通知未经显式业务发布不发送。
- 辅导员待复查与预约固定查询不调用模型、只返回本人带班范围；个案动作确认后只定位现有详情，不能自动创建风险标签或代替人工写入跟进记录。
- 通用助手组件静态挂载、统一 API 调用、全部 V3-3.1 状态、身份切换重置、动作确认只提交草稿 ID、动态内容安全写入、窄屏布局和减少动态效果契约。
- 组织岗位创建、范围更新、影响预览、结束、转任、到期和管理员安全约束。
- 生命周期表结构、隐藏基础自助身份、只读影响预览、访问范围，以及安全冻结/恢复的会话撤销、审计与授权不复活。
- 角色首页和产品入口权限。
- 教师/学生作业全流程及附件权限。
- 教师/学生课程分析权限、历史和反馈。
- 辅导员个案、学生请求、隐私和状态流转。
- 课程场次、批量考勤登记与修正审计、学生本人考勤、辅导员必要事实范围。
- 公开/私密课程答疑、教师回复、学生追问、关闭置顶、通知与跨课程越权拒绝。
- 教学任务范围、确定性异常、成绩提交审批发布、学院聚合和汇总表问数白名单，以及学院/教务逾期异常固定查询、跨学院动作拒绝、状态筛选和页面定位契约。
- 阶段 F 六角色菜单/接口允许拒绝矩阵、会话字段最小化、临时数据库隔离、未登录状态、通知跳转和移动端断点契约。
- 治理复核、治理设置、反馈和知识沉淀。

截至 2026-07-22，当前测试集共收集并完整通过 **250 项**；本次 V3 场景评测相关定向回归 **38 项通过**。V3-5.1 默认评测结果为 **8 通过、0 失败、4 个模型场景跳过**；跳过项不计作通过，需在模型与检索服务可用时使用 `--include-model` 执行。当前环境没有可用浏览器实例，因此未执行真实点击验收；自动化用例通过也不代表已完成生产环境性能、安全、兼容性和浏览器端端到端验收。

## 17. 当前明确边界

以下内容不能描述为已经完成：

- 生产级短信/邮件验证、找回密码、统一身份认证和多因素认证。
- 面向真实学校主数据系统的自动人员同步和离职/转岗消息集成。
- 主动销户；当前账号安全冻结/恢复、学生休学/复学/毕业/退学及职工调岗/离职/退休已可执行。正式人事角色、权威人事主数据同步以及教学/个案接任人的完整分派仍待后续阶段完成。
- 审批消息的真实短信、邮件或企业消息推送；当前为站内数据和页面待办。
- 完整的移动端适配、无障碍合规和多浏览器自动化验收。
- 大规模并发、灾备、集中日志、监控告警、密钥管理和正式等保方案。
- 任意数据库写入；数据维护只开放管理员受控接口和显式可写 SQLite 源。
- 默认数据源清单之外的数据库；新增教学数据源必须经管理员显式接入并配置权限。
- 未启动或未正确配置的 Elasticsearch、Milvus、pgvector 服务端检索能力。
- 不经过人工确认就自动把所有低置信结果写入知识库；是否入队和如何接受由治理规则控制。
- V3 服务端历史会话管理和真正利用既有轮次恢复 NL2SQL 多轮语义；学院/教务异常助手当前使用固定 72 小时停留口径，尚无按异常类型配置 SLA 的管理页面。
- V3 独立认证指标目录/卡片和制度知识问答；当前通用组件已展示回答证据和动作草稿，管理员可查看单机脱敏质量聚合，但 CSV 动作仍依赖前端当前结果，异步置信度回写、自动告警、分布式追踪和生产监控仍未实现。
- V3-5.1 评测集中的制度知识和数据/制度混合问题因 V3-4 尚未实现而显式排除；真实模型 NL2SQL、上下文追问、空结果和小样本场景默认跳过，不能把离线确定性评测结果视为模型准确率结论。

## 18. 文档持续维护规则

从本文件建立之日起，任何改变用户可见能力或系统边界的代码变更，都必须在同一开发任务内更新本文。包括但不限于：

- 新增、删除或修改页面、按钮、业务流程和状态机。
- 新增、删除或改变 API 的方法、路径、权限、参数语义和结果语义。
- 新增角色、岗位、导航、表/字段/行级权限或审核范围。
- 新增数据模型、外部依赖、配置项、脚本或部署方式。
- 将“仅后端”变为页面可用，或将“条件可用”改为默认可用。
- 修复导致原功能描述不再准确的重要缺陷。

每次更新至少完成以下检查：

1. 修改本文相关功能章节，而不只追加更新日志。
2. 同步 API 清单、角色矩阵、页面清单、配置或边界说明。
3. 更新顶部“最后核对日期”。
4. 在下方记录一条简短变更摘要。
5. 用当前后端路由和前端页面标识反查，确保没有漏项或把规划写成实现。
6. 若测试数量或质量结论发生变化，同步更新质量基线。

### 变更记录

| 日期 | 变更摘要 |
|---|---|
| 2026-07-22 | 完成 V3-5.1 场景评测集：新增 12 个版本化场景，覆盖六角色及固定指标、NL2SQL、会话追问、模糊澄清、越权、空结果、小样本和检索降级；每条记录回答类型、范围、禁止内容、SQL 约束和动作策略。新增临时数据库执行器、受控检索降级故障注入、隐私安全 JSON 报告和契约回归；默认 8 个确定性场景通过、4 个模型场景明确跳过，V3-4 制度/混合类别显式排除。全量 **250 项通过**，相关定向回归 38 项通过。 |
| 2026-07-22 | 完成 V3-3.5 管理员助手质量运营：新增管理员专属“助手运营”页面与近 7/30/90 天脱敏聚合 API，展示总量、成功率、P50/P95、失败阶段、本地/服务端检索和降级、越权拒绝、低置信、负向反馈、高频类别及治理积压；schema 升级至 3005 并记录每次实际检索后端。接口不查询或返回原始问题、SQL、结果行、反馈原因和治理标题，未知分类统一归入“其他”。运行时 API 为 141 个，全量 **246 项通过**；当前环境无浏览器实例，未完成真实点击验收。 |
| 2026-07-22 | 完成 V3-3.4 学院与教务场景：教学运行页接入统一助手；逾期/待处理教学异常走服务端确定性范围查询，以最后状态更新时间停留 72 小时作为逾期口径；回答可确认定位最优先异常或应用安全状态筛选，页面随后高亮目标，且不自动改变异常状态。学院范围、教务筛选和跨学院动作在创建与确认时均重新校验。当前收集 239 项测试，本次相关 43 项通过；当前环境无浏览器实例，未做真实点击验收。 |
| 2026-07-22 | 将 Schema linking 改为授权后的自适应检索：教师、辅导员等小型授权视图直接使用完整授权 Schema；大型视图并行使用本地 BM25 与向量召回，再以仅含授权表的关系图排序和补 JOIN；本地术语改为条件注入，查询扩展在当前配置关闭。召回候选、桥接表和术语引用均不能扩大角色权限。当前收集 238 项测试，本次相关 13 项通过。 |
| 2026-07-22 | 完成 V3-3.3 辅导员场景：学习支持工作台接入统一助手；“本周待复查”和“待处理学生预约”走服务端确定性范围查询，回答展示事实依据和复查状态；可确认打开本人最优先个案并直接定位现有人工跟进表单。全过程不自动创建风险标签、不写跟进记录，也不返回非带班学生。当前收集 233 项测试，本次相关 44 项通过。 |
| 2026-07-22 | 修复教师“哪些人没交作业”问数失败：该高频名单问题改为服务端确定性查询本人授课班作业提交记录，不再依赖模型生成 SQL，因此不会因子查询遗漏 `teacher_id` 行级条件而在安全回修后失败；结果仅返回学生姓名、课程、作业和截止时间。当前收集 231 项测试。 |
| 2026-07-22 | 补齐教师提醒草稿发布闭环：确认助手草稿后自动打开对应课程空间；教师可在“待发布草稿”查看并显式发布本人草稿，发布时再次校验任课关系与草稿归属，随后创建本课程学生送达记录和站内通知。草稿确认本身仍不会发送。 |
| 2026-07-22 | 补齐 V3-3.2 查询结果联动：教师从作业、考勤、课程空间或课程分析入口在当前任课班查询非空的未交、未完成或缺勤名单后，同一回答直接提供“保存全班课程提醒草稿（未发送）”；无课程上下文、空结果、普通统计和非教师不生成。当前动作仍只创建全班课程公告草稿，不会定向发送给名单学生。当前收集 230 项测试，本次相关 29 项通过。 |
| 2026-07-22 | 系统性修复角色问数误报无权：检索候选不能重新引入授权 Schema 外的表或敏感字段；主外键可作为 JOIN/WHERE 键但继续禁止结果输出；模型首次生成未授权表或受限输出字段时进入安全回修而非直接把模型错误归因于用户。任课教师现在可在本人授课班范围内查询学生姓名，仍须落实教师/课程行范围且不能输出内部学生 ID。当前收集 228 项测试，本次相关 73 项通过。 |
| 2026-07-22 | 完成 V3-3.2 学生与教师场景融合：角色首页、课程空间、作业中心、考勤和课程分析页均可携带受控页面/课程上下文进入统一助手；切换入口会重置会话，前端不判断权限。教师在当前课程空间请求提醒草稿时，服务端只生成可确认的课程公告草稿，确认后不发送通知且仍须在课程页显式发布。新增前后端回归覆盖；辅导员、学院和教务融合仍未实现。 |
| 2026-07-21 | 修复真实 NL2SQL 持续超时：SQL 主模型恢复为低延迟文本模型 `qwen-plus`，路由恢复为 `qwen-turbo`；当前教学库授权 schema 较小，本地运行环境关闭会触发远端嵌入调用的检索增强，直接使用完整授权 schema，表/字段/行级白名单与只读校验保持不变。新增 `LLM_ENABLE_THINKING` 配置并在 Qwen3 多模态调用中默认关闭思考，同时保留 45 秒服务端硬超时和 50 秒前端中止保护。测试基线更新为 221 项。 |
| 2026-07-21 | 修复统一助手 NL2SQL 分支无限等待：为整个旧问数调用增加可配置硬超时，超时返回 `ASSISTANT_NL2SQL_TIMEOUT` 和安全失败阶段；前端统一助手请求增加 50 秒中止保护，确保外部模型或检索服务不返回时退出加载状态。测试基线更新为 220 项。 |
| 2026-07-21 | 修复 V3-3.1 静态资源版本不一致：为新增统一助手 API 的 `api.js` 和负责页面联动的 `app.js` 更新缓存标识，并在组件侧增加旧客户端检测，避免浏览器混用新助手组件与旧 API 客户端时出现 `queryAssistant is not a function`。测试基线仍为 219 项。 |
| 2026-07-21 | 完成 V3-3.1 通用前端助手组件：独立新增作用域化 JS/CSS，在智能问数页接入统一助手会话，覆盖推荐、加载、澄清、成功、空结果、低置信、检索降级、无权限、异常和动作确认状态；展示范围、口径、证据、质量提示与安全 trace，身份切换会重置会话。运行时 API 仍为 139 个，全量测试更新为 219 项；角色业务页融合留待 V3-3.2～3.4。 |
| 2026-07-21 | 完成 V3-2.3 后端白名单动作草稿：新增 schema 3004 动作表及创建、读取、确认 3 个 API，严格限制 8 类动作，按当前工作身份隔离并在确认时重新鉴权；覆盖参数篡改、越权、过期、重复确认、角色切换、会话删除和通知不直发。运行时 API 更新为 139 个，全量测试更新为 213 项；前端预览/确认组件尚未接入。 |
| 2026-07-19 | 完成 V3-2.2 后端回答证据与执行轨迹：统一返回最终范围、来源、口径、结构化质量提示和分级 trace；新增 schema 3003 脱敏轨迹表，不保存问题、SQL 或结果行；低置信率与阶段耗时升级为真实轨迹指标。运行时 API 仍为 136 个，全量测试更新为 205 项；前端证据卡片尚未接入。 |
| 2026-07-19 | 完成 V3-2.1 后端认证指标语义层：以版本化 YAML 定义 21 个首批指标，新增按角色、页面和服务端上下文范围化的统一求值器及 `GET /api/assistant/metrics`；自然语言命中认证指标时与固定接口共用同一结果，低置信率和阶段耗时明确为条件不可用。运行时 API 更新为 136 个，全量测试更新为 202 项；前端尚未接入。 |
| 2026-07-18 | 完成 V3-1.3：新增统一助手编排器与 6 个助手 API，将固定计数、业务状态、导航、NL2SQL 和不支持问题统一响应并写入身份隔离会话；异常结构化返回，旧 `/api/ask` 保持兼容，课程上下文落实到 SQL 行范围。运行时 API 更新为 135 个，全量测试更新为 197 项；前端尚未接入。 |
| 2026-07-18 | 完成 V3-1.2 后端基础：通过 V3 迁移新增助手会话、轮次和收藏查询表，实现按用户及当前角色绑定隔离的会话创建、列表、分页、重命名、收藏、软删除和轮次幂等写入；冻结或失效账号不可读取，全量测试更新为 187 项。API 和前端入口仍未实现。 |
| 2026-07-18 | 完成 V3-1.1 后端基础：新增助手页面上下文模型及服务端权限求交，复核页面能力、会话/角色绑定、课程成员与任课关系、辅导员行政班和学院范围；覆盖六类必测越权场景，全量测试更新为 181 项。统一助手 API 和前端接入仍未实现。 |
| 2026-07-18 | 完成 V3-0.3：新增有序、带 SHA-256 校验值和 SQLite schema version 的 V3 迁移账本，应用启动自动执行待处理迁移；覆盖首次执行、幂等重复执行、旧库升级、历史漂移拒绝及失败整体回滚，全量测试更新为 173 项。 |
| 2026-07-18 | V3-0.1 基线审计：运行时 129 个 API 与目录清单一致，10 个运行时角色与角色表一致；补记已实现但页面表此前遗漏的通知中心 `notifications-view`。完整测试仍为 168 passed。 |
| 2026-07-17 | 移除 BIRD 外部评测库连接：默认数据源收敛为 `teaching`，删除 BIRD 环境变量、专用评测脚本和连接测试，并将服务端检索验证改为教学场景。 |
| 2026-07-17 | 阶段 C 后端：新增通知列表/未读/已读、课程公告送达与汇总、课程资料元数据和按课程成员关系读取的课程空间；公告只通知本课程已选学生，全量测试更新为 116 项。 |
| 2026-07-17 | 重构“我的课程”前端：新增课程概览侧栏、公告时间线、资料卡片、教师内容编辑区和移动端响应式布局，并补充课程区块定位与前端结构回归测试；全量测试更新为 117 项。 |
| 2026-07-17 | 课程资料从文件名元数据升级为真实附件：教师可上传受控格式的 20MB 以内文件，课程成员可鉴权下载，同时保留 HTTP(S) 外部链接方式。 |
| 2026-07-17 | 完善课程公告生命周期：任课教师可二次确认后软删除本人课程公告，对学生同步撤下公告通知，并保留送达记录和审计日志。 |
| 2026-07-17 | 完成阶段 D：新增真实课程场次、批量点名与留痕修正、学生本人/辅导员必要考勤事实，以及公开/私密课程答疑、回复追问、关闭置顶和通知；新增课程考勤与课程答疑页面，全量测试更新为 120 项。 |
| 2026-07-17 | 修复阶段 D 角色界面隔离：避免布局样式覆盖 `hidden` 属性，教师端不再显示学生提问表单；后端继续强制拒绝教师创建课程问题，并增强旧考勤关联迁移在重复事实下的幂等性。 |
| 2026-07-17 | 完成阶段 E：新增教学任务台账、确定性异常待办、教师提交—学院审批—教务发布的成绩状态流、学院课程运行/工作量/质量聚合页面，并将教务和学院问数收敛到范围化汇总表；全量测试更新为 122 项。 |
| 2026-07-17 | 完善教学任务台账：新增课程/教师/学院关键词、学年、学期和成绩提交状态的后端范围化组合筛选，以及前端结果计数和一键重置。 |
| 2026-07-17 | 优化教学运行长页面：台账和成绩列表支持独立折叠、内部滚动与独立组合筛选；学年和学院改为范围化下拉选项，并修复筛选主按钮的文字与悬停样式。 |
| 2026-07-17 | 重构个人中心页面：统一身份概览、账号状态、资料卡片和密码表单的信息层级，使用完全作用域化样式修复通用表单规则造成的组件错位，并补齐桌面、平板和手机响应式布局。 |
| 2026-07-17 | 完成阶段 F：建立六角色产品回归矩阵和独立临时数据库断言，增加一键演示回归脚本与手工演示手册；补齐学生首页入口、通知业务跳转以及空/错误/无权/移动端契约检查，全量测试更新为 138 项。 |
| 2026-07-17 | 完成智能问数全面回归与安全加固：修复行范围、嵌套敏感字段、空权限、辅导员班级范围和跨数据源等绕过风险，增加危险函数与输入大小限制，并通过学生、教师、辅导员、学院四角色真实模型问数；全量测试更新为 168 项。 |
| 2026-07-17 | 完成 B.7-3：以两名具名管理员双确认临时替代人事角色，支持跨学院调动、离职和退休；统一结束旧岗位/角色、撤销会话、归档离职账号、释放审核任务并生成教学和个案交接清单，全量测试更新为 115 项。 |
| 2026-07-17 | 完成 B.7-2：教务处可办理学生休学、复学、毕业、退学及批量毕业；休学保留最低权限自助身份，复学创建当前绑定，毕业/退学归档账号且不删除业务历史；新增学籍状态、会话撤销、事件、状态历史和审计回归，全量测试更新为 113 项。 |
| 2026-07-17 | 完成 B.7-1：平台管理员可在组织目录安全冻结/恢复已核验账号；操作采用二次密码验证、会话版本立即撤销、正式管理员保底、状态历史、生命周期事件与安全审计，恢复不复活已撤销授权；新增专项回归，全量测试更新为 111 项。 |
| 2026-07-17 | 完成 B.7-0：新增人员关系、生命周期事件、账号状态历史、交接与销户申请模型，增加隐藏 `self_service` 基础绑定和 4 个只读详情/影响预览接口；真实库 23 个已核验身份全部建立基线，全量测试更新为 108 项。 |
| 2026-07-17 | 完成 B.6-5：现有账号一次性迁移到角色/范围绑定模型，停止使用 `app_user.role` 作为最终权限源，增加迁移完整性报告及旧字段篡改、无绑定拒绝、重复迁移回归；全量测试更新为 103 项。 |
| 2026-07-16 | 首次建立全项目功能基线，纳入 B.6-0 至 B.6-4 的注册、组织岗位、多角色与管理员全生命周期能力，并核对现有全部后端路由和前端工作视图。 |
