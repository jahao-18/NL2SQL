# zjh-dev 修改与测试记录

更新日期：2026-07-31

## 当前说明

本文件只记录 `zjh-dev` 实际执行的文档建立、测试活动和获批修改，不把 `9527-test-optimize` 的修改写成本分支成果。

- 已建立并持续更新：
  - `PROJECT_ISSUES.md`
  - `PROJECT_CHANGES.md`
  - `PROJECT_PROGRESS.md`
- `ZJH-DOC-001/002` 与 `ZJH-FIX-001～010` 均已在原记录后追加文件级清单，明确新增文件、修改文件、原内容、替换/新增内容及用途；纯测试记录继续按测试证据记录，不虚构产品文件变化。
- 已获批完成：
  - 第一批 `ZJH-006`；
  - 第二批 A `ZJH-002`；
  - 第二批 B `ZJH-005`；
  - `ZJH-004` 测试基线修正；
  - `ZJH-010` 助手会话测试隔离修正；
  - 第三批 A `ZJH-003` 合法 OR 查询兼容修正；
  - 第三批 B `ZJH-001` 未完成作业 Schema 画像最小修正；
  - 独立 `ZJH-001` 收口修复；
  - `ZJH-007` 日期边界查询最小修正；
  - `ZJH-011` 敏感身份拒绝与无法回答占位状态最小修正；
  - 外部数据源接入闭环最小修正 `ZJH-014 + ZJH-013`；
  - 外部数据源前端闭环修正 `ZJH-018 + ZJH-019`；
  - `ZJH-016` server 关系图兼容最小修正；
  - `ZJH-021` PostgreSQL 只读表预览最小修正；
  - 统一助手结果反馈与安全 Few-shot 闭环修正；
  - 旧管理员教学驾驶舱适用性浏览器收口核对。
- 其余问题只登记，尚未获批修改。
- 当前业务代码、测试和数据库均有前述批次留下的未提交变化；ZJH-001 收口在第三批 B 的 Schema 画像基础上增加教学提示词和执行前语义保护，真实模型测试使用隔离副本，不修改原数据库，也不清理、不覆盖、不提交其他变化。

## 记录格式

每条记录按适用情况包含：批准与对应问题、修改前证据、涉及文件、核心变化、调用链、测试结果、实际运行、未修改范围、潜在影响和精确回退。测试记录与代码修改记录分开，测试发现的问题以 `PROJECT_ISSUES.md` 为准。

## 记录总览

| 编号 | 日期 | 类型 | 主要内容 | 结果 |
|---|---|---|---|---|
| ZJH-DOC-001 | 2026-07-26 | 文档 | 建立本分支问题、修改和进度记录 | 已完成 |
| ZJH-DOC-002 | 2026-07-27 | 文档 | 按测试优化版格式整理已有问题与修改记录 | 已完成 |
| ZJH-DOC-003 | 2026-07-27 | 文档 | 为已有文档和十批修正补充文件级变更清单 | 已完成 |
| ZJH-DOC-004 | 2026-07-29 | 文档与交付审查 | 同步状态矛盾、确认交付边界并进入代码冻结 | 已完成；未修改产品、测试或数据库 |
| ZJH-DOC-005 | 2026-07-29 | 项目文件审查 | 建立逐文件分类、职责、关系、测试覆盖和修改映射目录 | 已完成；未修改产品、测试、配置或数据库 |
| ZJH-DOC-006 | 2026-07-29 | 测试交付文档 | 建立11阶段人工测试向导和96条可复现结果工作簿 | 已完成；未修改产品、测试、配置或数据库 |
| ZJH-TEST-001 | 2026-07-26 | 自动测试 | 环境健康、依赖和 250 项 pytest | 249 通过、1 个 ZJH-004 失败 |
| ZJH-TEST-002 | 2026-07-26 | 真实 HTTP | 六角色、权限、会话隔离和新增业务流程 | 计划内流程通过 |
| ZJH-TEST-003 | 2026-07-26 | 浏览器 | 页面资源、角色切换清理、学生菜单和网络 | 计划内检查通过 |
| ZJH-TEST-004 | 2026-07-26 | 真实模型 | 未完成作业稳定性初测 | 发现 ZJH-001/ZJH-003 |
| ZJH-TEST-005 | 2026-07-26 | 模型专项 | 追问、反向、日期、安全、教师和专业 | 登记 ZJH-002/005/006/007/008 |
| ZJH-TEST-006 | 2026-07-27 | 隔离真实模型 | ZJH-001 天然数据与已截止无提交边界复现 | 天然 9/9；边界 4/9，更新 ZJH-001/008 |
| ZJH-TEST-007 | 2026-07-27 | 真实浏览器 | 旧管理员教学驾驶舱适用性与网络请求收口 | 角色工作台通过；旧筛选不再适用 |
| ZJH-TEST-008 | 2026-07-27 | 隔离真实模型与浏览器 | 敏感字段和缺失字段响应类型专项复现 | 登记 ZJH-011；未发现敏感事实泄露 |
| ZJH-TEST-009 | 2026-07-27 | 最终演示回归 | 全量、阶段 F、六角色 HTTP、真实模型与 Edge 固定矩阵 | 283/283、22/22、8/8、6/6、3/3；登记 ZJH-012 |
| ZJH-TEST-010 | 2026-07-28 | server 真实验收 | Milvus、Elasticsearch、pgvector 和图关系四路检索 | 四路均实际命中；首次 95.646 秒，缓存 4.603 秒 |
| ZJH-TEST-011 | 2026-07-28 | 外部双库验收 | 外部 SQLite 上传接入与独立业务 PostgreSQL 配置、扫描、查询和权限 | 主链可用；接口原始 17/20、NL2SQL 严格 12/14；登记 ZJH-013～015 |
| ZJH-TEST-012 | 2026-07-28 | BIRD server 验收 | 官方 Mini-Dev 单库、四路预热、固定15题、权限和只读 | 前置6/6、可执行14/15、严格EX 11/15；登记 ZJH-016～017 |
| ZJH-TEST-013 | 2026-07-28 | 真实浏览器 | 外部 PostgreSQL 页面注册、Schema、统一助手问数、学生权限和安全 | 注册POST 200但页面误报；Schema 6/24通过；外部问数误用teaching；登记 ZJH-018～019 |
| ZJH-FIX-001 | 2026-07-26 | 代码修正 | 多表学生范围连接 | 已验证 |
| ZJH-FIX-002 | 2026-07-26 | 代码修正 | 服务端同会话历史 | 已验证 |
| ZJH-FIX-003 | 2026-07-27 | 代码修正 | 具体业务问题默认路由 | 已验证 |
| ZJH-FIX-004 | 2026-07-27 | 测试修正 | 课程空间有效成员与角色绑定夹具 | 已验证；另登记 ZJH-010 |
| ZJH-FIX-005 | 2026-07-27 | 测试修正 | 助手会话测试使用前后基线差值 | 已验证；全量 262/262 |
| ZJH-FIX-006 | 2026-07-27 | 代码修正 | 合法括号业务 OR 与顶层范围证明 | 部分整改；全量 269/269 |
| ZJH-FIX-007 | 2026-07-27 | 配置与测试修正 | 未完成作业状态、连接和截止语义写入教学 Schema 画像 | 部分整改；全量 270/270 |
| ZJH-FIX-008 | 2026-07-27 | 提示词与代码修正 | ZJH-001 教学提示词和执行前语义保护收口 | 已验证；全量 274/274 |
| ZJH-FIX-009 | 2026-07-27 | 提示词与代码修正 | ZJH-007 SQLite ISO 时间日期边界保护 | 已验证；全量 278/278 |
| ZJH-FIX-010 | 2026-07-27 | 代码与前端状态修正 | ZJH-011 敏感身份拒绝及无法回答占位转不支持 | 已验证；全量 283/283 |
| ZJH-FIX-011 | 2026-07-28 | 代码、前端与测试修正 | ZJH-014 PostgreSQL 页面/API 注册闭环与 ZJH-013 外部 Schema 展示 | 已验证；专项 8/8、相关 76/76、真实闭环 13/13、全量 291/291 |
| ZJH-FIX-012 | 2026-07-29 | 代码、前端与测试修正 | ZJH-018 PostgreSQL 表单稳定引用与 ZJH-019 外部源助手锁源 | 已验证；相关55/55、真实浏览器闭环通过、全量296/296 |
| ZJH-FIX-013 | 2026-07-29 | 代码与测试修正 | ZJH-016 server 关系图表/列规范化及无效关系过滤 | 已验证；BIRD 7节点31有效关系、四路通过、全量298/298 |
| ZJH-FIX-014 | 2026-07-29～30 | 代码、前端与测试修正 | ZJH-021 PostgreSQL 只读表预览与 SQLite 维护不回归 | 已验证；专项12/12、API 9/9、真实Edge通过、全量302/302 |
| ZJH-FIX-015 | 2026-07-31 | 代码、前端与测试修正 | 统一助手点赞/点踩、轮次反馈治理与服务端安全 Few-shot | 定向41/41、相关106/106、全量309/309及真实浏览器闭环通过 |

## 记录 ZJH-DOC-001：建立本分支持续记录

- 日期：2026-07-26
- 用户批准：为 `zjh-dev` 建立自身的问题登记、修改记录和任务进度文件。
- 修改原因：持续记录本分支真实测试结果，不照搬 `9527-test-optimize` 的问题正文或结论。
- 新增文件：
  - `PROJECT_ISSUES.md`：问题表现、证据、状态和交叉影响；
  - `PROJECT_CHANGES.md`：测试活动与获批修改；
  - `PROJECT_PROGRESS.md`：阶段结果、阻塞和下一步。
- 记录边界：只写当前分支实际测试和源码证据；未验证判断单独标注；不记录真实密钥。
- 产品影响：无业务、API、页面、权限、配置或数据库结构变化。
- 验证：三份文件存在且编号、状态和测试数字可相互对应。
- 回退范围：仅删除三份记录文件；不得回退任何业务代码或测试。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件共 3 个，没有修改既有产品文件：

| 新增文件 | 内容介绍 | 与其他记录的关系 |
|---|---|---|
| `PROJECT_ISSUES.md` | `zjh-dev` 自身的问题台账，记录复现证据、源码证据、状态、影响与待验证边界 | 问题编号作为本文件修正记录的索引 |
| `PROJECT_CHANGES.md` | 测试活动、获批修正、文件级变化、验证结果和精确回退范围 | 当前文件；只记录实际发生的工作 |
| `PROJECT_PROGRESS.md` | 强制工作原则、总体进度、阶段结论、阻塞和下一步 | 汇总状态，不替代问题和修改细节 |

- 替换内容：无；三份文件均为新增，不是从 `9527-test-optimize` 复制或覆盖形成。
- 产品影响：没有新增 Python、HTML、CSS、JavaScript、配置、迁移或测试文件。

## 记录 ZJH-DOC-002：已有问题与修改记录格式整理

- 日期：2026-07-27
- 用户批准：将已有问题记录和修改记录改为较详细版本；随后明确要求控制篇幅，参照 `9527-test-optimize` 的格式即可。
- 涉及文件：`PROJECT_ISSUES.md`、`PROJECT_CHANGES.md`。
- 修改内容：
  - 问题记录增加“发现/更新时间、文件位置、问题情况、影响功能、状态”总览，并为 ZJH-001～009 补齐必要的实际证据、原因、验证边界和修改状态；
  - 修改记录增加总览表，对早期文档、环境、HTTP、浏览器和模型测试补齐测试范围、结果、隔离方式和未覆盖项；
  - 三批修复继续保留批准、代码位置、调用链、测试、影响和精确回退，不照搬测试优化版的问题事实。
- 产品影响：无业务代码、测试代码、配置、数据库或运行行为变化。
- 验证方式：核对问题状态、测试数量、会话范围、日期和三批修复记录一致，并执行 Markdown/差异检查。
- 回退范围：只恢复两份 Markdown 到本次整理前文字；不得回退 ZJH-FIX-001～003 的代码、测试或真实运行数据。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无。

| 修改文件 | 原有内容 | 追加或替换内容 | 用途 |
|---|---|---|---|
| `PROJECT_ISSUES.md` | 以简要问题描述为主 | 增加问题总览表；为 ZJH-001～009 补齐复现、原因、实际/源码证据、影响和状态 | 让问题结论可追溯且区分已验证与待验证 |
| `PROJECT_CHANGES.md` | 早期记录较简略、缺少统一结构 | 增加记录总览；补齐测试范围、隔离方式、调用链、验证、未修改范围和回退说明 | 形成与本分支事实一致的修改审计记录 |

- 替换边界：只调整和扩充 Markdown 结构与文字，没有删除问题事实，也没有替换任何产品实现或测试断言。
- 本次没有修改 `PROJECT_PROGRESS.md`、`docs/project_feature_catalog.md` 或任何运行文件。

## 记录 ZJH-DOC-003：已有修改记录文件级清单补全

- 日期：2026-07-27
- 用户批准：保留已有修改内容，在每次修改记录后追加更全面的文件、添加内容和替换内容说明。
- 补充范围：
  - `ZJH-DOC-001/002`：说明新增或修改了哪些记录文件；
  - `ZJH-FIX-001～010`：逐项增加“文件级修改补充”，列出仓库内新增文件、修改文件、原逻辑、替换/新增内容和用途；
  - `ZJH-FIX-007～010`：同时介绍仓库外隔离启动、矩阵、日志和报告文件；
  - 纯 `ZJH-TEST-*` 记录本身不声称修改产品代码，继续保留原有“产品文件修改/证据路径/隔离方式”说明，不伪造代码变化。
- 保留原则：没有删除、重写或缩短任何已有记录；所有内容均追加在对应记录末尾。

| 修改文件 | 修改性质 | 新增内容 | 用途 |
|---|---|---|---|
| `PROJECT_CHANGES.md` | 文档 | 新增总览编号 `ZJH-DOC-003`，并为 2 个文档记录和 10 个修正记录追加文件级清单 | 让每批变化可直接回答“新增/修改了什么文件、替换了什么、为什么” |
| `PROJECT_PROGRESS.md` | 文档 | 增加本次记录完整性补充的完成状态 | 保持任务进度与修改记录一致 |

- 仓库内新增文件：无。
- 产品影响：没有修改 Python、提示词、Schema、前端、测试、数据库或依赖。
- 精确回退范围：只删除各记录标题为“文件级修改补充（后补，不替换原记录）”的追加段落、当前 `ZJH-DOC-003` 条目及进度文件对应一行；不得删除原始修改记录。

## 记录 ZJH-TEST-001：环境健康与分组自动测试

- 日期：2026-07-26
- 目标：确认当前环境可运行、依赖完整，并建立修改前自动测试基线。
- Python：3.12.10
- 依赖检查：`pip check` 通过。
- pytest 收集：250 项。
- 结果：249 通过，1 失败，2 条弃用警告。
- 唯一失败：课程空间测试硬编码旧 `teacher` 账号，前置查询返回 `None`，详见 `ZJH-004`。
- 隔离措施：pytest 使用临时数据库副本，未修改工作区现有数据库。
- 结论：环境与绝大多数自动测试健康；不能把 249/250 写成全量通过。
- 产品文件修改：无。

## 记录 ZJH-TEST-002：隔离服务真实 HTTP 测试

- 日期：2026-07-26
- 目标：不依赖浏览器，验证当前后端角色、权限、会话隔离和新增业务闭环。
- 运行地址：本机临时端口 8011。
- 数据：从 HEAD 数据库提取后在临时目录迁移。
- 上传与课程资料：临时目录。
- 身份与权限：
  - 六角色登录、本人信息、会话、工作台；
  - 每个角色允许接口为 200、禁止接口为 403；
  - 角色切换后旧令牌 401、新令牌 200；
- 会话隔离：
  - 助手会话本人可见、其他用户 404；
  - 删除后不能继续读取。
- 新增业务流程：
  - 作业创建、提交、退回、重交、评分、发布；
  - 公告通知与删除；
  - 考勤记录、修正与本人范围；
  - 公开/私密问答和回复通知；
  - 学生支持请求和辅导员处理。
- 证据边界：这些是隔离服务 HTTP 结果，不等同于所有前端页面点击验收或真实模型准确率。
- 数据影响：业务写流程只作用于隔离数据和临时上传目录。
- 产品文件修改：无。

## 记录 ZJH-TEST-003：真实浏览器测试

- 日期：2026-07-26
- 目标：验证前端资源、身份切换清理和学生可见菜单。
- 浏览器：临时配置目录的无头 Microsoft Edge。
- 实际结果：
  - 页面和全部本地脚本、样式加载成功；
  - 教师双角色选择成功；
  - 教师切换为辅导员后，助手输入清空、旧结果 DOM 移除、欢迎状态恢复；
  - 学生只显示本人业务菜单；
  - 学生登录阶段未发现本地 4xx 网络请求；
  - 未发现浏览器控制台异常。
- 隔离：未使用或修改用户日常浏览器配置。
- 未覆盖：没有据此宣称全部页面、全部筛选组合和移动端均已验收。
- 产品文件修改：无。

## 记录 ZJH-TEST-004：真实模型 NL2SQL 测试

- 日期：2026-07-26
- 身份与问题：学生，“我有哪些作业未完成”。
- 首次验证：成功，正确包含 `student_id=900001`。
- 连续 5 次：4 成功、1 失败。
- 失败链：OR 被拒绝 → OR 修复仍被拒绝 → `UNION ALL` 被拒绝。
- 发现问题：
  - 相同问题结果不稳定，登记 ZJH-001；
  - 保守 OR/集合规则阻断正常查询，登记 ZJH-003。
- 后续限制：DashScope 免费额度耗尽，停止继续调用。
- 排除项：额度耗尽导致的 403 不计作产品失败。
- 产品、测试和配置文件修改：无。

## 记录 ZJH-TEST-005：额度恢复后的模型专项测试

- 日期：2026-07-26
- 目标：补完额度不足中断的追问、反向、边界、安全及历史问题测试。
- 数据环境：重新从 HEAD 提取数据库并在全新临时目录执行迁移。
- 额度验证：“我有哪些作业未完成”成功，确认 DashScope 已恢复。
- 同会话追问：
  - “按课程分组”3/3 错误统计教学班数量；
  - “只看已经截止的”3/3 被判为不支持。
- 完整句：
  - 作业按课程分组3/3被误判为通用待办指标；
  - 已截止未完成作业3/3返回 0，与数据库基准一致。
- 反向与边界：
  - 已完成作业3次中2次返回正确的2行，1次因提交表缺少学生连接条件返回3行；
  - 2020年空结果2/2返回空并带 `EMPTY_RESULT`；
  - 2026年12月31日边界2/2错误返回空，数据库基准为1；
  - 提示注入被直接拒绝；
  - 跨学生范围查询未执行成功；
  - 显式“未提交或者迟交”被模型改写为 `IN`，但遗漏无提交记录。
- 历史回归：
  - 默认路由将教师统计误判为通用系统指标；
  - 默认路由将专业名称查询判为不支持；
  - 强制 NL2SQL 时，零班级教师数量2/2正确为1；
  - 强制 NL2SQL 时，专业名称2/2正确返回18条；
  - 每位教师列表一次超时，一次返回100行并截断。
- Judge：
  - 对缺少提交表学生范围的错误结果给出总分88、正确性85并放行。
- 问题映射：
  - 追问不继承前文 → ZJH-002；
  - 具体业务问题误路由 → ZJH-005；
  - 提交表学生范围遗漏 → ZJH-006；
  - 日期当天遗漏 → ZJH-007；
  - Judge 错误高分 → ZJH-008；
  - OR/集合失败继续计入 ZJH-003。
- 结论边界：强制 NL2SQL 结果只用于证明底层查询能力，不代表默认前端路由已经正确。
- 产品、测试和配置文件修改：无。

## 修改审批状态

第一批 ZJH-006、第二批 A ZJH-002、第二批 B ZJH-005 已分别获批并完成。其余问题尚未获得代码或测试修正批准，任何后续修改仍需要追加记录：

- 修改原因和问题编号；
- 文件及具体位置；
- 调用链变化；
- 验证结果；
- 潜在影响；
- 精确回退范围。

## 记录 ZJH-FIX-001：多表学生范围连接修正

- 日期：2026-07-26
- 对应问题：ZJH-006。
- 用户批准：明确批准执行第一批 ZJH-006 修复。
- 修改原因：原校验器在一个查询块中发现任意一个正确的 `student_id` 条件后即整体放行，导致 `enrollment` 已限定本人时，未限定的 `assignment_submission` 仍可能混入其他学生提交记录。
- 产品代码：
  - `app/core/validator.py`
  - `_enforce_row_scope` 改为逐个范围承载表别名检查；
  - 新增谓词提取、范围字段直接匹配和范围节点等值连接判断；
  - 连接关系只从当前查询块的 `JOIN ... ON` 与 `WHERE` 独立合取谓词中读取；
  - 所有范围节点必须能直接或经等值连接到当前身份的范围值。
- 测试代码：
  - `tests/test_security.py` 新增错误 SQL 拒绝、正确学生连接接受、结果列表达式不能伪造连接 3 个用例；
  - `tests/test_ask_regression.py` 新增错误模型 SQL 必须先修复再执行的调用链用例。
- 调用链变化：
  - 模型生成 SQL；
  - `service.ask` 调用 `validate_and_fix`；
  - 校验器逐别名验证范围；
  - 不完整 SQL 抛出 `SQLValidationError` 并进入既有 `repair_sql`；
  - 只有补齐提交表本人条件或安全等值连接后才允许执行。
- 自动验证：
  - 新增定向用例 4/4 通过；
  - `test_security.py + test_role_permissions.py + test_ask_regression.py` 共 58/58 通过；
  - 全量收集 254 项，253 项通过、1 项失败；
  - 唯一失败仍为 ZJH-004 的旧教师测试数据假设，与本次修改无关。
- 真实模型验证：
  - 数据库基准为本人已完成作业 2 条；
  - 在独立端口 `8012` 启动当前工作区代码，启动日志确认应用完整加载；
  - 连续 3 次真实 HTTP/模型请求均返回 2 条；
  - 三次 SQL 的提交表均直接限定本人或与已限定选课表按 `student_id` 等值连接。
- 运行测试副作用：
  - `data/teaching.db` 在本批开始前已经是有改动状态，本批没有重置或覆盖；
  - 初次请求误连到旧端口进程，结果不作为修复证据；发现后改用独立端口重新验证；
  - 两组请求按正常业务流程共新增助手会话 `8`～`13`、轮次 `33`～`38`、对应执行轨迹及 6 条待确认 CSV 导出草稿，用于保留实际测试证据；
  - 原 8000 端口旧进程未能在本轮授权命令中退出，因此没有继续反复强制终止；当前修复版服务保留在 `http://127.0.0.1:8012`，启动与健康检查均通过；
  - 未修改数据库结构或演示教学事实；未擅自清理上述记录。
- 潜在影响：模型生成包含多个范围承载表却未连接其范围字段时，将多进行一次或多次安全修复，极端情况下会安全失败而不执行。
- 未包含内容：ZJH-001/002/003/004/005/007/008 均未在本批修改。
- 精确回退范围：
  - `app/core/validator.py` 中本记录新增的范围节点、谓词及等值连接逻辑；
  - `tests/test_security.py` 的 3 个 ZJH-006 用例；
  - `tests/test_ask_regression.py` 的 1 个 ZJH-006 用例；
  - 本记录及对应问题、进度、功能目录文字。
  - 如需回退本批真实测试数据，先删除会话 `8`～`13` 对应的 6 条导出草稿，再删除会话 `8`～`13`；轮次 `33`～`38` 和执行轨迹会按外键级联删除。该数据清理不属于当前已批准范围，因此本批未执行。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；本批是在现有校验器和测试文件中局部增加逻辑。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `app/core/validator.py` | 产品代码 | 一个查询块只要出现任意正确 `student_id` 条件，就可能整体通过范围检查 | 在 `_enforce_row_scope` 中改为逐个范围承载表别名证明；增加当前查询块谓词提取、直接范围条件识别、范围字段等值连接图和可达性检查，保证提交表自身也受本人范围约束 |
| `tests/test_security.py` | 测试代码 | 缺少“选课表已受限但提交表未受限”的多表对照 | 新增错误连接拒绝、正确学生范围连接接受、结果列表达式不能伪造范围连接 3 类回归 |
| `tests/test_ask_regression.py` | 测试代码 | 缺少模型先生成错误多表 SQL 时的完整回修链断言 | 新增错误 SQL 不得执行、必须进入 `repair_sql` 并在补齐范围后执行的调用链测试 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-006 尚无修正后证据 | 追加修正文件、真实 3/3 结果、状态与对 ZJH-008 的边界 |
| `PROJECT_PROGRESS.md` | 记录 | 第一批状态未收口 | 更新第一批完成状态、自动测试和真实模型结果 |
| `docs/project_feature_catalog.md` | 功能目录 | 行级范围说明未覆盖逐别名连接证明 | 补充多表范围承载表必须直接受限或通过安全等值连接受限，并同步测试基线 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-001` 记录及本文件级补充 |

- 关键替换可概括为：`查询块存在一个正确范围条件即可` → `查询块内每个实际承载范围字段的表别名都必须可证明到当前身份`。
- 没有新增数据库迁移、配置、前端文件或依赖；真实运行新增的是业务证据记录，不是源码文件。

## 记录 ZJH-FIX-002：服务端同会话追问历史接入

- 日期：2026-07-26
- 对应问题：ZJH-002。
- 用户批准：明确批准执行“第二批 A：ZJH-002 追问上下文修复”。
- 修改原因：统一助手此前只用 `get_session` 校验会话页面，没有读取历史轮次；分类与 `ask_service` 均只收到当前问题，服务日志持续为 `history=0`。
- 产品代码：
  - `app/core/assistant_sessions.py` 新增当前身份拥有会话的最近有效轮次读取，限制 1～20，编排器固定使用最近 5 轮；
  - 查询仅选择 `success`、`clarify`、`degraded`，按轮次正序返回，失败和拒绝轮次排除；
  - `app/core/assistant_orchestrator.py` 将问题和安全摘要转换为旧问数 `Turn`；
  - 当前问题无法独立识别数据主题时，分类器参考历史问题做 NL2SQL 兜底；
  - `_nl2sql` 将同一份服务端历史传给 `ask_service`。
- 安全与隐私边界：
  - 历史读取继续经过账号状态、用户 ID、角色绑定和会话软删除校验；
  - 页面仍须与会话页面一致；
  - 不读取或新增完整 SQL、结果行、附件存储；
  - 客户端附加的 `history` 字段被请求模型忽略，不能替换服务端历史；
  - 不改变角色表权限、字段权限或行级范围。
- 测试代码：
  - `tests/test_assistant_api.py` 新增同会话追问、首次空历史和客户端伪造历史无效用例；
  - `tests/test_assistant_sessions.py` 新增最近 5 个有效轮次、时间顺序、失败轮次排除和跨身份拒绝用例。
- 自动验证：
  - 新增定向用例 2/2 通过；
  - 助手 API、会话及旧问数回归 35/35 通过；
  - 全量 256 项中 255 项通过、1 项失败；
  - 唯一失败仍是 ZJH-004 的旧教师数据假设。
- 真实验证：
  - 当前代码在 `http://127.0.0.1:8012` 完成启动和健康检查；
  - 一个完整原问题序列成功：未完成作业 1 条 → 按课程分组“数据库系统 1” → 已截止 0 条；
  - 截止追问在 3 个会话中 3/3 正确进入 NL2SQL 并返回 0；
  - 服务日志记录 `history=0 → 1 → 2`；
  - 4 次分组追问均保留作业语义，其中 3 次被 ZJH-003/ZJH-009 的独立校验限制阻止执行。
- 新发现问题：登记 ZJH-009，本批未修改。
- 运行测试副作用：
  - 正常业务流程新增助手会话 `14`～`18`、轮次 `39`～`50`、对应 12 条执行轨迹及 5 条待确认 CSV 导出草稿；
  - `data/teaching.db` 在本批开始前已经有改动，本批未重置、覆盖或清理既有测试证据；
  - 当前修复版服务继续运行在 8012，旧 8000 进程状态未改变。
- 潜在影响：同一会话的省略式问题可能更多进入 NL2SQL；历史只用于数据主题兜底和模型生成，固定指标、导航、权限检查仍优先执行。
- 未包含内容：ZJH-001、ZJH-003、ZJH-004、ZJH-005、ZJH-007、ZJH-008、ZJH-009 均未在本批修正。
- 精确回退范围：
  - `app/core/assistant_sessions.py` 的 `recent_turns`；
  - `app/core/assistant_orchestrator.py` 的最近历史读取、转换、分类兜底和 `ask_service` 传参；
  - `tests/test_assistant_api.py`、`tests/test_assistant_sessions.py` 的两个 ZJH-002 用例；
  - 本记录及对应问题、进度和功能目录文字。
  - 如需回退本批真实测试数据，先删除会话 `14`～`18` 的 5 条导出草稿，再删除会话 `14`～`18`；轮次 `39`～`50` 和执行轨迹会按外键级联删除。该清理不属于当前批准范围，因此未执行。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；使用既有会话表、轮次表和统一助手模块完成接入。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `app/core/assistant_sessions.py` | 产品代码 | 只有会话创建、读取等能力，编排器没有安全的最近轮次读取接口 | 新增 `recent_turns`：复核当前用户和角色绑定，只读取未删除会话中 `success/clarify/degraded` 轮次，限制数量并恢复正序 |
| `app/core/assistant_orchestrator.py` | 产品代码 | `_classify` 和 `_nl2sql` 只处理当前问题，`ask_service` 收到空历史 | 读取最近 5 轮，将问题与安全摘要转换为旧问数 `Turn`；当前问法省略主题时用历史做数据主题兜底，并把同一服务端历史传给 `ask_service` |
| `tests/test_assistant_api.py` | 测试代码 | 只覆盖单轮统一助手 | 新增同会话追问、首轮空历史和客户端伪造历史不能覆盖服务端历史的 API 回归 |
| `tests/test_assistant_sessions.py` | 测试代码 | 未验证最近有效轮次筛选 | 新增最近 5 轮、时间顺序、失败/拒绝排除和跨身份拒绝测试 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-002 只有失败表现 | 追加调用链原因、修正位置、真实 `history=0 → 1 → 2` 和剩余 ZJH-009 边界 |
| `PROJECT_PROGRESS.md` | 记录 | 第二批 A 未收口 | 更新追问修正、真实截止追问和新发现问题状态 |
| `docs/project_feature_catalog.md` | 功能目录 | 只描述前端续用会话 ID | 增加服务端最近安全轮次恢复语义、失败/拒绝轮次不参与及客户端历史不可覆盖 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-002` 记录及本文件级补充 |

- 关键替换可概括为：`只校验 session_id、history 始终为空` → `校验会话归属后读取最近安全轮次，并用于分类兜底和 SQL 生成`。
- 没有新增历史字段、数据库表或迁移，也没有把 SQL、结果行或完整回答写入会话历史。

## 记录 ZJH-FIX-003：具体业务数据问题默认路由修正

- 日期：2026-07-27
- 对应问题：ZJH-005。
- 用户批准：明确批准执行“第二批 B：ZJH-005 具体业务查询误路由修复”，并要求问题记录和修改记录详细、可参考 `9527-test-optimize` 的记录深度，但不得照搬非本分支事实。
- 修改前状态与证据：
  - 真实前端等价请求不指定 `preferred_answer_type` 时，作业分组、教师班级数量和零班级教师问题被路由为工作台 `metric`，专业列表被路由为 `unsupported`；
  - 强制指定 NL2SQL 后零班级教师数和专业列表能够得到正确数据库基准，表明数据与底层查询能力存在；
  - 本批先增加 4 个调用链回归并在旧代码上运行，实际得到 4/4 失败：3 个 `metric`、1 个 `unsupported`，没有用修改后的期望倒推旧行为。
- 根因与调用链：
  - `POST /api/assistant/query`
  - `query_assistant` 完成当前身份、页面上下文和会话处理；
  - `_dispatch` 先处理专用业务流程与认证语义指标，再调用 `_classify`；
  - 旧 `_classify` 先匹配泛化数量词，导致含“作业/教师”的完整问题在数据主题识别前返回 `metric`；
  - “专业、班级”不在数据主题集合，专业列表最终返回 `unsupported`；
  - 误入 `_workbench_answer` 后，问题未匹配当前身份摘要标签，于是返回全部工作台摘要。
- 产品代码修改：
  - 文件：`app/core/assistant_orchestrator.py`；
  - 函数：`_classify`；
  - 保留导航、显式首选类型、对象上下文、业务状态、认证语义指标和追问数据主题兜底的既有处理；
  - “未读、待批阅”仍在具体数据主题前命中确定性工作台计数；
  - 数据主题新增“专业、班级”，并在泛化“多少、几个、数量、总数”前判断；
  - 只有无具体数据主题且无对象上下文时，泛化数量问法才回退到工作台 `metric`。
- 测试代码修改：
  - 文件：`tests/test_assistant_api.py`；
  - 新增参数化回归 `test_concrete_business_data_questions_route_to_nl2sql`，覆盖学生作业按课程分组、每位教师班级数、零班级教师数和学校专业列表；
  - 测试替换的只有模型调用边界，断言真实编排响应为 `answer_type=nl2sql`、`route=nl2sql`，且问题确实传入 `ask_service`，没有修改实现错误来迎合测试；
  - 在既有确定性路由测试中补充“未读通知”和“当前工作台状态”控制问法，连同原有待批阅、待办、导航和不支持问法验证路由边界。
- 调用链变化：
  - 认证语义指标、专用业务流程和显式工作台词仍按原顺序处理；
  - 具体业务数据主题现在先于泛化数量词返回 `nl2sql`；
  - `_dispatch` 随后进入既有 `_nl2sql`，继续使用当前角色表/字段/行范围、服务端历史、只读校验、失败回修和总超时；
  - 本批没有改变任何 SQL 生成、校验、执行或裁判逻辑。
- 自动验证：
  - 修复前新增问题用例：0/4 通过，失败值与历史现象一致；
  - 修复后问题用例与确定性控制组：10/10 通过；
  - 助手 API、会话、认证语义指标及旧问数相关回归：46/46 通过；
  - 全量测试：262 项中 261 项通过、1 项失败，耗时约 4 分 48 秒；
  - 唯一失败为 `tests/test_course_space.py::test_announcement_notification_and_course_resource_are_member_scoped`，仍因硬编码 `teacher` 账号没有可解包的有效课程而失败，即 ZJH-004，与本次分类修改无调用链关系。
- 真实 HTTP/模型验证：
  - 精确确认旧 8012 服务为 PID `30964` 后结束该进程；第一次受限启动因 SQLite 只读而退出，未把其健康响应计入当前代码证据；
  - 以可写权限启动当前工作区，PID `29884`，日志 `D:\NL2SQL2\zjh005-current-8012-20260727-001528.err.log` 明确记录 application startup complete，`/api/health` 返回数据库正常且千问已配置；
  - 不传 `preferred_answer_type` 连续执行 4 个历史问题，4/4 均实际返回 `answer_type=nl2sql`、`route=nl2sql`；
  - 零班级教师数返回 1，专业列表返回 18 条，分别与直接数据库基准 1 和 18 一致；
  - 作业分组请求进入 NL2SQL 后三次因 OR 被 ZJH-003 拒绝；本批只登记交叉影响，没有顺带放宽安全校验；
  - 每位教师列表的 HTTP 请求达到统一助手总超时；服务日志稍后显示 SQL 得到 200 行且被截断，但由于响应已失败，本记录不把后台完成写成前端成功；
  - 待批阅、未读通知、工作台状态三个控制请求均成功，路由分别为 `semantic_metric`、`deterministic_metric`、`deterministic_business_state`。
- 运行测试副作用：
  - 正常业务流程新增助手会话 `19`～`25`、轮次 `51`～`57` 和对应 7 条脱敏执行轨迹；
  - 会话 `21`、`22`、`24`、`25` 各新增 1 条待确认 `export_current_result` 草稿，共 4 条；没有确认或执行任何草稿；
  - `data/teaching.db` 在本批开始前已经有改动，本批没有重置、覆盖或清理历史证据，也没有修改教学事实或数据库结构；
  - 当前代码服务继续运行在 `http://127.0.0.1:8012`（PID `29884`）；8000 端口不在本批操作范围。
- 安全、权限与产品影响：
  - 变化仅决定“确定性工作台摘要”与“既有 NL2SQL 安全链路”之间的默认入口，不扩大任何角色可见表、字段、页面或行范围；
  - 具体数据问题会更多调用外部模型，可能暴露既有模型超时或保守校验失败，但失败仍为结构化安全失败，不会回退成越权执行；
  - 明确工作台问法继续由服务端固定规则计算，不因本批修改而依赖模型。
- 未包含内容：
  - ZJH-001 查询稳定性、ZJH-003 OR/集合安全兼容、ZJH-004 旧测试数据、ZJH-007 日期边界、ZJH-008 Judge 误判和 ZJH-009 子查询范围传播均未修改；
  - 未修改提示词、检索、SQL 校验器、Judge、数据库、前端组件或历史轮次实现。
- 精确回退范围：
  - `app/core/assistant_orchestrator.py::_classify` 中“未读/待批阅”显式工作台计数、具体数据主题前置、“专业/班级”主题和泛化数量词后置的本批差异；
  - `tests/test_assistant_api.py` 的 4 个 ZJH-005 参数化问题用例及 2 个新增控制参数；
  - 本记录、ZJH-005 问题状态、进度和功能目录中的对应文字；
  - 如需回退本批真实测试数据，应先精确删除会话 `19`～`25` 关联的 4 条待确认动作草稿，再删除会话 `19`～`25`；轮次 `51`～`57` 和对应执行轨迹会按外键关系处理。该数据清理不属于当前批准范围，因此本批未执行。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；本批仅调整既有分类顺序并增加回归参数。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `app/core/assistant_orchestrator.py` | 产品代码 | `_classify` 在具体数据主题前匹配“多少/数量”等泛化词；“专业/班级”不属于数据主题 | 保留明确待办/未读/待批阅确定性路径；加入“专业、班级”主题，并把具体业务数据主题判断前移到泛化数量回退之前 |
| `tests/test_assistant_api.py` | 测试代码 | 缺少误路由的四个真实业务问法和确定性控制组 | 新增 4 组参数化 NL2SQL 路由回归，并在原确定性路由测试中加入未读通知、工作台状态等控制问法 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-005 的多个误路由表现分散 | 汇总四类复现、强制 NL2SQL 能力证据、修正结果和仍受 OR/超时影响的边界 |
| `PROJECT_PROGRESS.md` | 记录 | 第二批 B 未收口 | 更新默认路由 4/4 进入 NL2SQL、控制组和未处理问题 |
| `docs/project_feature_catalog.md` | 功能目录 | 默认分类说明未区分具体主题和泛化数量 | 补充具体课程/教师/专业/班级等问题优先 NL2SQL，明确工作台摘要仍走固定服务 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-003` 记录及本文件级补充 |

- 关键替换可概括为：`泛化数量词优先，命中后返回工作台摘要` → `明确工作台词优先；具体数据主题其次；无具体主题的数量词最后才回退工作台`。
- 没有修改 SQL 提示词、校验器、Judge、数据库、前端或外部依赖。

## 记录 ZJH-FIX-004：课程空间测试基线修正

- 日期：2026-07-27
- 对应问题：ZJH-004；测试中另发现并登记 ZJH-010。
- 用户批准：明确批准执行“ZJH-004 测试基线修正”，并要求及时更新问题记录和修改记录。
- 修改前证据：
  - 目标用例 `tests/test_course_space.py::test_announcement_notification_and_course_resource_are_member_scoped` 单独运行 0/1；
  - 前置 SQL 固定 `tea.username='teacher'`，在 pytest 隔离数据库中返回 `None`，解包产生 `TypeError`；
  - 失败发生在 API 调用之前，不能据此判断课程空间产品流程失败。
- 测试代码修改：
  - 文件：`tests/test_course_space.py`；
  - 将单行硬编码查询改为动态选择有效的任课教师、教学班和在班学生；
  - 要求教师、学生账号处于 active、身份已 verified；
  - 进一步要求 `user_role_binding` 有效，并由 `role_scope_binding` 分别证明教师范围等于教学班 `teacher_id`、学生范围等于选课 `student_id`；
  - 将查询得到的角色绑定 ID 显式传给 `token_for`，保证多角色教师使用 teacher 身份、学生使用 student 身份；
  - 当不存在有效关系时给出明确夹具断言，不再以 `None` 解包掩盖原因。
- 调整过程：
  - 第一次动态选择只证明人员身份，选中多角色教师后默认令牌进入学院负责人身份，公告创建实际返回 403；
  - 随后补齐角色绑定和范围绑定条件并显式选择令牌身份，目标用例通过；
  - 原有公告、通知、回执、删除、附件、非成员和危险链接断言均未删除或放宽。
- 调用链与产品影响：
  - 变化只发生在 pytest 前置夹具和请求头生成；
  - 产品仍按 `token_for → user_from_token → require_teacher_of_class/课程成员校验 → course_space` 原链路执行；
  - 未修改后端 API、权限规则、课程空间服务、数据库迁移、前端或真实业务数据。
- 自动验证：
  - 目标用例：1/1 通过；
  - `tests/test_course_space.py`：2/2 通过；
  - `tests/test_stage_d.py + tests/test_stage_f_product_regression.py`：19/19 通过；
  - `tests/test_action_drafts.py`：9/9 通过；
  - 全量：262 项中 260 项通过、2 项失败，耗时约 4 分 50 秒；ZJH-004 原失败用例已通过。
- 新问题边界：
  - 全量剩余两项均在 `tests/test_assistant_sessions.py`，单独复跑仍失败；
  - pytest 临时库复制了真实专项测试保留的 4 条管理员会话，而用例只清理自身前缀数据却断言全部会话总数；
  - 已登记 ZJH-010，本批没有修改助手会话实现、测试清理逻辑或真实数据库。
- 运行副作用：所有自动测试使用 `tests/conftest.py` 创建的临时数据库与临时上传目录；本批没有新增、删除或清理 `data/teaching.db` 中的证据。
- 精确回退范围：
  - `tests/test_course_space.py` 中目标用例的前置关系查询、角色绑定 ID 解包和复用的 `teacher_headers`/`student_headers`；
  - `PROJECT_ISSUES.md`、本文件、`PROJECT_PROGRESS.md` 和 `docs/project_feature_catalog.md` 中本批记录；
  - 不涉及任何产品代码或数据库回退。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；本批只修正既有测试夹具，没有修改产品实现。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `tests/test_course_space.py` | 测试代码 | 前置 SQL 固定 `tea.username='teacher'`，并依赖默认角色生成令牌；当前数据下查询为空或多角色身份不正确 | 用动态关系查询选择 active、verified 且真实任课/选课的教师、教学班和学生；同时取得有效 `user_role_binding`/`role_scope_binding`，显式传入教师和学生角色绑定 ID |
| `PROJECT_ISSUES.md` | 记录 | ZJH-004 被笼统记为课程空间失败 | 明确失败发生在 API 前置夹具、修正后产品流程通过，并登记独立 ZJH-010 |
| `PROJECT_PROGRESS.md` | 记录 | 测试基线仍显示旧失败 | 更新目标、相关流程结果及全量剩余两项的真实归属 |
| `docs/project_feature_catalog.md` | 功能目录 | 质量基线未反映课程空间夹具修正 | 同步课程空间相关覆盖和当时全量结果，不把 ZJH-010 写成本批通过 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-004` 记录及本文件级补充 |

- 关键替换可概括为：`硬编码旧账号 + 默认身份令牌` → `从当前数据中选择可证明成员关系 + 显式工作身份令牌`。
- 原公告、通知、回执、删除、附件、非成员和危险链接断言全部保留；没有通过删除断言让测试通过。

## 记录 ZJH-FIX-005：助手会话测试隔离修正

- 日期：2026-07-27
- 对应问题：ZJH-010。
- 用户批准：明确批准执行“ZJH-010 助手会话测试隔离修正”。
- 修改前证据：
  - ZJH-004 完成后的全量测试为 262 项中 260 项通过、2 项失败；
  - 两个失败用例单独复跑仍为 0/2；
  - 创建/分页用例期望总数 3、实际 7，软删除用例期望总数 0、实际 4；
  - pytest 临时数据库中已有 4 条从真实模型专项测试复制来的管理员会话，证明失败不是一次性测试顺序波动。
- 根因与测试链：
  - `tests/conftest.py` 将当前演示数据库复制到 pytest 私有目录，这是保护真实数据的既有机制；
  - `tests/test_assistant_sessions.py::_clean_sessions` 只清理当前测试前缀会话；
  - 用例通过 `list_sessions(auth)` 查询当前管理员全部未删除会话，却把总数写死为 3 或 0；
  - 因此真实运行证据增加后，产品返回正确总数，测试反而失败。
- 测试代码修改：
  - 文件：`tests/test_assistant_sessions.py`；
  - 创建测试会话前保存 `baseline_total`，创建 3 个唯一会话后断言 `total == baseline_total + 3`；
  - 保留分页每页数量和相邻页 ID 不重叠检查，并确认本测试创建的 3 个会话 ID 均在列表中；
  - 软删除用例在创建前保存基线，重复删除后断言总数恢复为原基线；
  - 没有清空其他会话，也没有修改 `list_sessions`、`create_session`、`delete_session` 等产品实现。
- 验证结果：
  - 两个原失败目标用例：2/2 通过；
  - `test_assistant_sessions.py + test_assistant_api.py + test_assistant_context.py + test_ask_regression.py`：49/49 通过；
  - 全量自动测试：262/262 通过，耗时约 4 分 49 秒；
  - 仍有 Starlette TestClient 和 `langchain-community` 各 1 条弃用警告，本批未把警告登记为功能失败。
- 数据与安全影响：
  - 所有 pytest 写入仍发生在临时数据库和临时上传目录；
  - `data/teaching.db` 中管理员会话 20、21、22、25 等真实测试证据未删除、未改写；
  - 测试仍验证当前身份隔离、分页、重复创建幂等、软删除幂等和删除后拒绝，不通过放宽产品断言掩盖实现问题。
- 未包含内容：未修改助手会话产品代码、API、前端、数据库迁移或其他已登记问题。
- 精确回退范围：
  - `tests/test_assistant_sessions.py` 两个用例中的 `baseline_total`、动态总数/页长断言和新建 ID 集合检查；
  - `PROJECT_ISSUES.md`、本文件、`PROJECT_PROGRESS.md` 与 `docs/project_feature_catalog.md` 中本批状态和质量基线；
  - 不涉及产品代码或真实数据库回退。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；本批只修改既有会话测试中的总数口径。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `tests/test_assistant_sessions.py` | 测试代码 | 创建后绝对断言 `total == 3`，删除后绝对断言 `total == 0`，隐含管理员初始无会话 | 两个用例执行前记录 `baseline_total`；创建后断言基线加 3，并验证新建 ID 均在列表；删除后断言恢复基线，继续保留分页和幂等验证 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-010 只有全量失败数字 | 补充临时库复制真实会话、绝对总数假设、目标/相关/全量验证和数据未清理边界 |
| `PROJECT_PROGRESS.md` | 记录 | ZJH-010 尚未收口 | 更新会话测试隔离已完成和全量 262/262 基线 |
| `docs/project_feature_catalog.md` | 功能目录 | 质量基线未反映会话测试修正 | 同步会话测试隔离说明和当时测试总数 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-005` 记录及本文件级补充 |

- 关键替换可概括为：`依赖数据库绝对总数` → `只断言本用例造成的前后差值，同时验证新建对象身份`。
- 没有新增清库函数，没有删除真实会话，也没有修改 `assistant_sessions.py`、会话 API 或数据库结构。

## 记录 ZJH-FIX-006：合法 OR 查询兼容修正

- 日期：2026-07-27
- 对应问题：ZJH-003；真实复测同时更新 ZJH-001 证据。
- 用户批准：明确批准执行“第三批 A：ZJH-003 合法 OR 查询兼容修复”。
- 审批边界：
  - 只允许身份范围位于独立顶层 AND、业务 OR 位于括号内部的查询；
  - 顶层 OR 和利用运算符优先级绕过身份范围仍须拒绝；
  - 集合查询继续保守拒绝，因此 ZJH-003 本批完成状态只能是“部分整改”。
- 修正前证据：
  - `app/core/validator.py::_enforce_row_scope` 在逐查询块和逐别名证明前看到任意 OR 即失败；
  - 新增正反测试先在旧实现执行，得到 4/6 通过、2/6 失败；
  - 合法 `scope AND (未提交 OR 未完成状态)` 被全局禁令拒绝；
  - 未受限提交表用例也只得到泛化 OR 错误，尚未进入 ZJH-006 的逐别名诊断。
- 产品代码修改：
  - 文件：`app/core/validator.py`；
  - 函数：`_enforce_row_scope` 移除全局 OR 禁令，但保留 `UNION/UNION ALL/INTERSECT/EXCEPT` 禁令；
  - 函数：`_top_level_conjuncts` 新增顶层 OR 检测；
  - 当表达式存在顶层 OR 时，将整个表达式视为一个不可拆分条件，不能从 `scope AND 条件 OR 1=1` 中抽取 scope 作为安全证明；
  - 括号内 OR 不属于顶层 OR，因此 `scope AND (业务条件 A OR 业务条件 B)` 可继续进入原有逐别名范围传播。
- 测试代码修改：
  - 文件：`tests/test_security.py`；
  - 新增 1 个合法多表 OR 用例；
  - 新增未受限提交表、OR 内伪造范围连接 2 个多表拒绝用例；
  - 新增 4 个顶层 OR/无括号优先级/其他学生范围绕过用例；
  - 既有注释、字符串、子查询、NOT、CASE 和集合查询绕过用例全部保留。
- 调用链变化：
  - `validate_and_fix → _enforce_row_scope → _query_blocks → _direct_predicate_conjuncts`；
  - 集合查询仍在 `_enforce_row_scope` 前置拒绝；
  - OR 查询进入每个查询块后，只有独立顶层 scope 等式和独立 scope 字段连接可建立范围可达图；
  - OR 内的 scope 等式或 scope 字段连接均不能建立安全证明。
- 自动验证：
  - 新增 OR 正反控制：修正后 7/7；
  - 完整 `tests/test_security.py`：47/47；
  - `tests/test_role_permissions.py + tests/test_ask_regression.py`：18/18；
  - 全量测试新增 7 项后为 269/269 通过，耗时约 5 分 03 秒；
  - 仍有 Starlette TestClient 和 `langchain-community` 各 1 条既有弃用警告。
- 真实 HTTP/模型验证：
  - 首次受限启动因 SQLite 只读而失败，没有把该进程计为运行成功；
  - 随后在独立 8013 端口启动当前代码并确认 `/api/health` 数据库正常、DashScope 已配置；
  - 数据库基准：`stu_zhang` 未完成作业 1 条，课程分组为“数据库系统=1”，当前日期前已截止 0；
  - 完整会话 26、轮次 58～60：首问 1 条、分组 1、截止 0，三步均正确；
  - 另执行首问 2 次和分组 2 次；首问合计 3/3 正确，分组合计 2/3 正确；
  - 日志确认 4 条成功 SQL 实际包含 `scope AND (asub.id IS NULL OR asub.status ...)`，均通过校验并返回正确基准；
  - 错误分组 SQL使用内连接并仅筛 `missing/late`，遗漏无提交记录；Judge 指出结果可能偏少但请求仍以成功响应返回，归入 ZJH-001/ZJH-008 的既有边界，不反向否定 OR 修正；
  - 截止追问首次 SQL 未限定提交表 student_id，被 ZJH-006 正确拒绝，回修后补齐范围并返回 0；
  - 临时 8013 测试进程已结束，没有替换或停止原 8000/8012 服务。
- 运行数据副作用：
  - 正常业务流程新增助手会话 26～30、轮次 58～64及对应 7 条脱敏执行轨迹；
  - 会话 26、27、28、30共新增 5 条待确认 `export_current_result` 草稿，均未确认或执行；
  - `data/teaching.db` 中既有证据未清理，本批新增记录继续保留为真实测试证据。
- 安全与产品影响：
  - 放宽的是可证明身份范围之外的括号业务 OR，不扩大角色表、字段、页面或行范围；
  - 恶意顶层 OR、无括号优先级绕过、其他学生范围、未受限提交表和 OR 内伪连接均保持拒绝；
  - 集合查询和 ZJH-009 子查询范围传播未修改。
- 精确回退范围：
  - `app/core/validator.py` 中移除全局 OR 禁令及 `_top_level_conjuncts.has_top_level_or`；
  - `tests/test_security.py` 本批新增的 7 个 OR 正反用例；
  - `PROJECT_ISSUES.md`、本文件、`PROJECT_PROGRESS.md`、`docs/project_feature_catalog.md` 的本批状态和质量基线；
  - 如需回退本批真实测试数据，应先删除会话 26～30关联的 5 条待确认动作草稿，再删除会话 26～30；轮次 58～64及执行轨迹按外键关系处理。该清理未获批准，本批未执行。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；本批在已有范围校验框架内替换 OR 的判定粒度。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `app/core/validator.py` | 产品代码 | `_enforce_row_scope` 发现任意 OR 就直接拒绝，合法括号业务 OR 无法进入逐别名证明 | 移除全局 OR 禁令；在 `_top_level_conjuncts` 增加顶层 OR 标记，顶层 OR 整体不可拆分，括号内业务 OR 则继续进入原有范围可达性检查；集合查询禁令保留 |
| `tests/test_security.py` | 测试代码 | 缺少合法括号 OR 与恶意顶层 OR 的系统对照 | 新增 7 个正反用例：合法多表业务 OR、未受限提交表、OR 内伪连接、顶层 OR、无括号优先级和其他身份范围绕过 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-003 只有“OR 被禁”的表现 | 更新合法 OR 已验证、集合查询仍未整改的“部分整改”状态，并记录与 ZJH-001/006/008 的边界 |
| `PROJECT_PROGRESS.md` | 记录 | 第三批 A 未收口 | 更新定向、安全、全量及真实 OR SQL 结果 |
| `docs/project_feature_catalog.md` | 功能目录 | 边界描述为笼统 OR 禁止 | 改为允许身份范围之外的括号业务 OR，顶层 OR、伪范围和集合查询继续拒绝 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-006` 记录及本文件级补充 |

- 关键替换可概括为：`SQL 中出现任何 OR 都拒绝` → `只接受独立顶层身份范围 AND 括号业务 OR；顶层 OR 仍不能提供范围证明`。
- 没有新增 SQL 改写器，没有放宽集合查询，也没有改变表、字段和角色授权。

## 记录 ZJH-TEST-006：ZJH-001 修复前专项审查与隔离复现

- 日期：2026-07-27
- 对应问题：ZJH-001；错误结果的 Judge 评分同时补充 ZJH-008 证据。
- 用户批准：明确批准“执行 ZJH-001 修复前专项审查、隔离复现和证据登记，不修改产品实现”。
- 审批边界：
  - 只读审查当前作业状态口径、模型调用链和 Schema 画像；
  - 使用数据库副本和独立端口执行真实模型复现；
  - 只更新项目问题、测试记录和进度，不修改 Python/提示词/Schema 画像/自动测试/原数据库；
  - ZJH-007、ZJH-008、ZJH-009、ZJH-003 集合查询均不在本批修正。
- 源码直接证据：
  - `app/core/workbench.py`、`app/core/assignment_workflow.py` 和 `app/core/semantic_metrics.py` 对无提交、`returned`、无提交时间的处理不完全一致；
  - `data/schema_profiles/teaching.yaml` 只说明 `submitted/late/missing`，没有规定未完成作业必须从本人选课与作业出发并 `LEFT JOIN` 本人提交；
  - `app/service.py::ask` 将模型 SQL 依次交给校验、执行和异步 Judge；执行成功后不会用业务基准复核结果。
- 隔离方法：
  - 使用 SQLite `backup` API 从被占用的 `data/teaching.db` 建立一致副本，`PRAGMA integrity_check=ok`；
  - 通过仓库外临时启动模块将 `teaching_migrations.DB_PATH` 指向副本，并用独立 `DATA_SOURCES_FILE` 让 NL2SQL 执行器读取同一副本；
  - 服务仅监听 `127.0.0.1:8014`，健康检查确认数据库正常、DashScope 已配置、检索预热关闭；
  - 临时服务测试后已停止。
- 天然数据基准与结果：
  - `stu_zhang/student_id=900001` 有 3 个作业：2 个已提交并关闭，1 个已发布且完全无提交，截止时间为 `2026-12-31T23:59:00`；
  - 直接数据库基准：未完成 1 条、课程分组“数据库系统=1”、当前日期前已截止 0 条；
  - 3 个独立完整会话、每个依次执行首问/分组/截止，共 9 次请求；
  - 首问 3/3、分组 3/3、截止 3/3 与天然基准一致，9/9 均走默认 NL2SQL；
  - 三个截止 SQL 都改成提交表内连接；当前正确 0 行属于数据恰好未触发漏数，不能写成结构已经正确。
- 合成边界基准与实际复现：
  - 只在隔离副本新增作业 990001：已发布、已截止、本人无提交，标题明确标注为隔离边界；
  - 直接数据库基准变为未完成 2 条、课程分组“数据库系统=2”、已截止分组“数据库系统=1”；
  - 再执行 3 个独立完整会话共 9 次请求：首问 1/3、分组 3/3、截止 0/3 正确，合计 4/9；
  - 两个错误首问增加 `due_time >= 当前日期`，遗漏已截止但未完成的作业；
  - 三个错误截止追问均使用提交表内连接，遗漏完全无提交的已截止作业；
  - 9 次响应均为 HTTP `success`，其中两个错误首问仍获 Judge 90/88 分，ZJH-001 和 ZJH-008 均保持“未整改且已实际复现”。
- 数据隔离核验：
  - 原库测试前后保持会话 30、轮次 64、执行轨迹 64、动作草稿 32、作业 4155，且不存在作业 990001；
  - 隔离副本新增会话 31～36、轮次 65～82、18 条执行轨迹和 12 条未确认动作草稿；
  - 原 `data/teaching.db` 的文件大小和最后修改时间保持不变。
- 本地证据：
  - `D:\NL2SQL2\zjh001-review-20260727\review-results-natural.json`；
  - `D:\NL2SQL2\zjh001-review-20260727\review-results-expired-no-submission.json`；
  - `D:\NL2SQL2\zjh001-review-20260727\server.clean.err.log`；
  - 隔离数据库和启动辅助文件仅用于本地审查，不纳入项目提交；结果文件不含令牌或真实密钥。
- 自动测试边界：本批没有修改产品代码或测试实现，因此没有用旧自动测试结果宣称修复，也没有重新定义当前 269/269 基线。
- 尚待验证：
  - `returned/not_submitted` 的统一产品口径；
  - 补充 Schema 画像能否稳定纠正模型；
  - 修改后首问、分组和截止追问能否在天然与边界数据同时重复通过。
- 产品影响：无；没有修改 API、路由、权限、校验器、提示词、Schema 画像、测试代码、前端或原数据库。
- 精确回退范围：
  - 项目内只需回退 `PROJECT_ISSUES.md`、本文件和 `PROJECT_PROGRESS.md` 中 `ZJH-TEST-006` 的新增证据；
  - 仓库外隔离目录可整体弃用，不涉及组长代码回退；
  - 不得以删除既有 ZJH-001/ZJH-008 历史证据替代本批记录回退。

## 记录 ZJH-FIX-007：未完成作业 Schema 画像最小修正

- 日期：2026-07-27
- 对应问题：ZJH-001。
- 用户批准：明确要求进入“第三批 B：ZJH-001 未完成作业口径最小修正”。
- 修改前证据：
  - 修复前天然数据 9/9 正确，但所有截止 SQL结构均无法证明会保留无提交行；
  - 加入一条已截止、无提交作业后，首问 1/3、分组 3/3、截止 0/3，合计 4/9；
  - 现有 Schema 画像只列 `submitted/late/missing`，没有说明无提交行、退回待重交、迟交已有提交和省略式截止追问的口径。
- 获批业务口径：
  - 未完成包括无提交行、`missing`、`not_submitted`、`returned`；
  - 已有效提交包括 `submitted`、`late`、`late_submitted`、`resubmitted`、`graded_unpublished`、`graded_published`；
  - 普通未完成查询不得自动增加截止过滤；“只看已经截止的”只在原未完成条件上叠加时间条件；
  - 学生查询必须从本人选课和作业出发，按作业 ID 与本人学生 ID `LEFT JOIN` 提交表。
- 修改文件与位置：
  - `data/schema_profiles/teaching.yaml`：在 `assignment.notes` 增加时间口径，在 `assignment_submission.notes` 增加本人范围 `LEFT JOIN` 和状态口径；
  - `data/schema_profiles/teaching.yaml`：将 `assignment_submission.status` 从 3 个旧状态补齐为 9 个旧/新工作流状态，并注明 `late` 已有提交；
  - `tests/test_retrieval_adaptive.py::test_compact_teaching_profile_keeps_unfinished_assignment_semantics`：验证角色裁剪后的精简 Schema 上下文仍保留连接、状态和截止规则。
- 调用链变化：
  - Python 调用链没有改变；
  - `load_schema/retrieve_context → profile_context → generate_sql/repair_sql` 仍走原路径，仅传入的教学业务画像增加明确语义；
  - 未修改路由、身份范围校验、SQL 执行或 Judge。
- 自动验证：
  - 画像直接解析通过并读到 9 个提交状态；
  - 精简检索测试 5/5，旧问数回归 10/10；
  - 全量 270/270，2 条既有弃用警告，耗时约 5 分 15 秒；
  - 默认 pytest 缓存插件在当前环境的 `.pytest_cache` 临时目录创建阶段阻塞，经线程栈确认后使用 `-p no:cacheprovider` 重跑；断言、fixture 和产品实现均未为绕过失败而修改。
- 隔离真实模型验证：
  - 使用原库 SQLite 在线备份建立 `D:\NL2SQL2\zjh001-fix-20260727\teaching-isolated.db`，独立 8015 服务健康；
  - 首次服务受沙箱套接字策略阻断千问 API，3 个会话的 6 次 NL2SQL 失败和 3 次无前文“不支持”单独保存，不计为业务验证；
  - 允许模型网络后，天然数据 3 个独立完整会话共 9 次请求全部符合未完成 1、分组“数据库系统=1”、已截止 0 的直接数据库基准；
  - 隔离副本加入作业 990001 后，直接基准为未完成 2、分组“数据库系统=2”、已截止分组“数据库系统=1”；
  - 边界测试首问 3/3、分组 3/3、截止 0/3，合计 6/9；三条截止 SQL 均使用提交表内连接，实际返回 0；
  - 六条首问/分组 SQL 均使用本人范围 `LEFT JOIN` 且未自动添加截止条件，但仍误把旧 `late` 状态列为未完成；当前数据未触发结果差异。
- 数据与进程边界：
  - 原库测试后仍为会话 30、轮次 64、轨迹 64、动作草稿 32、作业 4155，且不存在作业 990001；
  - 隔离副本为会话 39、轮次 91、轨迹 91、动作草稿 44、作业 4156；会话 31～33是网络阻断证据，34～36是天然验证，37～39是边界验证；
  - 8015 隔离服务已停止；报告与日志不含认证令牌或真实密钥，不纳入项目提交。
- 结论与潜在影响：
  - 首问边界由修正前 1/3 提升为 3/3，说明画像修正产生了实际改善；
  - 截止追问仍 0/3，且 `late` 条件仍错误，因此 ZJH-001 只能从“未整改且已实际复现”更新为“部分整改”；
  - 下一层可能需要在现有 SQL 提示词中加入不可丢失的作业口径，或采用更确定性的业务规则，但两者均未获本批批准。
- 精确回退范围：
  - `data/schema_profiles/teaching.yaml` 中本批新增的两段 `notes`、`status.description` 及 9 个状态映射；
  - `tests/test_retrieval_adaptive.py` 中本批新增的 1 个回归用例；
  - `PROJECT_ISSUES.md`、本文件和 `PROJECT_PROGRESS.md` 中 ZJH-FIX-007 的状态与证据；
  - 仓库外 `D:\NL2SQL2\zjh001-fix-20260727` 可整体弃用；不得回退或覆盖其他批次代码及原数据库。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；修改的是既有 Schema 画像和检索回归文件。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `data/schema_profiles/teaching.yaml` | 业务配置 | `assignment_submission.status` 只列 `submitted/late/missing`，备注未说明无提交行、本人关联或截止追问 | 将状态说明扩充为 9 个旧/新流程状态；为作业和提交表增加本人选课起点、按作业与本人学生双键 `LEFT JOIN`、未完成/有效提交集合及截止条件只按需叠加的说明 |
| `tests/test_retrieval_adaptive.py` | 测试代码 | 未验证精简后的教学 Schema 上下文是否仍保留未完成作业口径 | 新增 `test_compact_teaching_profile_keeps_unfinished_assignment_semantics`，检查裁剪后仍含关键连接、状态和截止语义 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-001 尚无本批画像修正结果 | 增加天然/边界矩阵、改善与剩余内连接/`late` 错误，状态更新为部分整改 |
| `PROJECT_PROGRESS.md` | 记录 | 第三批 B 未记录阶段结论 | 写入固定范围、自动测试、隔离数据和 6/9 边界结果 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-007` 记录及本文件级补充 |

- 关键替换可概括为：`三个旧状态的字段枚举` → `九状态业务解释 + 无提交行/本人 LEFT JOIN/截止叠加的完整生成上下文`。
- 仓库外新增验证辅助文件位于 `D:\NL2SQL2\zjh001-fix-20260727`：
  - `isolated_app.py`：把应用迁移路径指向隔离数据库；
  - `data_sources.isolated.yaml`：让 NL2SQL 数据源读取同一隔离库；
  - `run_review.py`：执行天然数据和已截止无提交边界矩阵；
  - `review-results-*.json`：分别保存网络阻断、天然结果和边界结果；
  - `teaching-isolated.db` 与 `server*.log`：隔离数据和运行日志。
- 上述仓库外文件是可丢弃测试证据，不属于产品源码、依赖或提交内容。

## 记录 ZJH-FIX-008：ZJH-001 独立收口修复

- 日期：2026-07-27
- 对应问题：ZJH-001。
- 用户批准与阶段边界：
  - 用户批准进入一次性封闭的 ZJH-001 收口任务；
  - 范围一次明确为教学 SQL 提示词和最多一次教学作业专用语义保护，不再拆分子阶段；
  - 固定验收为天然数据 9 次、无提交与旧 `late` 综合边界 9 次、定向测试和最终全量测试；
  - ZJH-007、ZJH-008、ZJH-009、ZJH-003 集合查询、前端和原数据库均不在本任务修改范围。
- 修改原因：
  - ZJH-FIX-007 后综合边界仅 6/9，三条截止追问仍使用提交表内连接；
  - 普通 SQL仍误将旧 `late` 算入未完成；
  - 仅增加提示词后，天然结果虽为 9/9，但三条截止 SQL把未完成条件缩成仅 `submission.id IS NULL`，结构仍不合格。
- 修改文件与位置：
  - `prompts/sql_prompt.txt`：教学专用规则新增第 13 条，锁定 `published/closed`、本人关联、状态集合和截止追问继承；
  - `app/service.py`：新增未完成作业主题识别、截止追问信号和 `_unfinished_assignment_semantic_error`；
  - `app/service.py::ask`：通用 `validate_and_fix` 通过后、执行前调用语义保护，错误沿用现有 `repair_sql` 循环；
  - `tests/test_ask_regression.py`：新增提示词组装、截止状态丢失、内连接回修和换话题隔离 4 个用例。
- 调用链变化：
  - `ask → generate_sql → validate_and_fix → _unfinished_assignment_semantic_error → execute`；
  - 语义不合格时为 `... → semantic check → SQLValidationError → repair_sql → validate_and_fix → semantic check → execute`；
  - 不新增 SQL强制改写，不改变最大回修次数，不进入通用 `validator.py`，不扩大角色授权范围。
- 语义保护范围：
  - 仅 `source=teaching`、存在本人 `student_id` 行范围且明确询问未完成作业时启用；
  - 省略式追问只有在包含截止/分组等追问信号且上一轮问题或 SQL仍是作业主题时继承；
  - 换话题后不再受历史未完成作业语义影响。
- 自动验证：
  - 问数、上下文和会话组合 29/29；
  - 安全与角色权限 55/55；
  - 修改前全量基线 270/270；最终新增 4 项后为 274/274，2 条既有弃用警告，最终耗时约 4 分 35 秒。
- 隔离真实验证：
  - 使用 `D:\NL2SQL2\zjh001-closure-20260727\teaching-isolated.db`，原库在线备份完整性为 `ok`；
  - 天然直接基准为未完成 1、课程分组 1、已截止 0；最终会话 37～39共 9 次全部正确；
  - 综合边界作业 990001为已截止无提交，作业 990002具有本人旧 `late` 有效提交；直接基准为未完成 2、课程分组 2、已截止 1；
  - 最终会话 40～42共 9 次全部正确，所有首问、分组和截止 SQL均保留本人 `LEFT JOIN`、`published/closed` 和 `missing/not_submitted/returned`，结果中没有 990002；
  - 会话 31～33是仅提示词检查，会话 34～36因提升权限旧进程未被首次停止而仍访问提示词版服务；两组只作为中间证据，不计入最终验收。
- 数据与进程边界：
  - 原库前后均为会话 30、轮次 64、轨迹 64、草稿 32、作业 4155，合成作业和提交均为 0；
  - 隔离副本最终为会话 42、轮次 100、轨迹 100、草稿 59、作业 4157，合成旧 `late` 提交 1；
  - 8016 服务已停止，仅保留仓库外脱敏 JSON和日志证据。
- 结果与影响：
  - ZJH-001 从“部分整改”更新为“已整改并经实际测试验证”；
  - 保护的失败方式是阻止错误 SQL执行并要求现有模型回修，因此避免以 HTTP success 放行已知错误口径；
  - 对其他角色、其他教学主题和非教学数据源不生效。
- 精确回退范围：
  - `prompts/sql_prompt.txt` 教学专用口径第 13 条；
  - `app/service.py` 中本批新增的常量、两个辅助函数及 `ask` 内语义检查调用；
  - `tests/test_ask_regression.py` 中本批新增的 4 个用例和 `chain/Turn` 导入；
  - `PROJECT_ISSUES.md`、本文件和 `PROJECT_PROGRESS.md` 的 ZJH-FIX-008 记录；
  - 仓库外 `D:\NL2SQL2\zjh001-closure-20260727` 可整体弃用；不得回退 ZJH-FIX-007 Schema 画像或其他已完成批次。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；本批在现有 SQL 提示词、问数服务和回归文件中局部增加教学专用保护。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `prompts/sql_prompt.txt` | 模型提示词 | 通用 SQL 规则没有锁定学生本人未完成作业的发布状态、双键关联、状态集合和省略式追问 | 新增教学专用第 13 条，规定 `published/closed`、本人 `LEFT JOIN`、未完成/有效提交状态和截止追问继承 |
| `app/service.py` | 产品代码 | SQL 通过通用安全校验后直接执行，缺少该教学主题的业务口径检查 | 增加未完成作业主题/追问信号常量与辅助函数；新增 `_unfinished_assignment_semantic_error`，在执行前检查连接、本人范围、状态和不应自动加入的截止条件，错误沿用现有回修循环 |
| `tests/test_ask_regression.py` | 测试代码 | 未覆盖提示词口径、错误内连接、状态丢失和换话题隔离 | 新增提示词组装、截止追问状态保留、内连接回修和历史主题切换 4 个用例，并引入所需 `chain/Turn` 测试对象 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-001 仍为部分整改 | 补充最终天然与综合边界 9/9、保护范围和已验证状态 |
| `PROJECT_PROGRESS.md` | 记录 | ZJH-001 仍可能继续拆分 | 记录一次性收口范围、停止条件、真实结果和不再追加子阶段 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-008` 记录及本文件级补充 |

- 关键替换可概括为：`通用校验通过后直接执行` → `通用校验后增加一次受限于教学学生未完成作业主题的语义检查，不合格则进入现有模型回修`。
- 仓库外新增验证辅助文件位于 `D:\NL2SQL2\zjh001-closure-20260727`：
  - `isolated_app.py`、`data_sources.isolated.yaml`：隔离启动与数据源配置；
  - `run_matrix.py`：执行天然和“无提交 + 旧 late”综合边界；
  - `matrix-natural.json`、`matrix-combined-boundary.json`：最终矩阵证据；
  - `teaching-isolated.db`、`server*.log`：隔离数据和各次启动日志。
- 没有新增通用校验规则、数据库迁移、前端状态或依赖。

## 记录 ZJH-FIX-009：ZJH-007 日期边界查询最小修正

- 日期：2026-07-27
- 对应问题：ZJH-007。
- 用户批准与任务边界：
  - 用户明确批准执行“ZJH-007 日期边界查询最小修正”；
  - 本任务一次性固定为教学 SQL 提示词、现有学生未完成作业语义保护、专项测试、隔离真实模型验证和记录收口；
  - 不修改通用 `validator.py`、前端、原数据库，也不包含 ZJH-008、ZJH-009、ZJH-003 集合查询或其他日期主题。
- 修改前证据：
  - 数据库直接基准为 1 个本人未完成作业，截止时间 `2026-12-31T23:59:00`；
  - 相同问题修正前 2/2 返回 0 行；
  - 错误 SQL 使用 `a.due_time <= '2026-12-31'`，SQLite ISO 时间文本比较排除了截止日当天带时间的记录。
- 修改文件与位置：
  - `prompts/sql_prompt.txt` 教学专用第 13 条：新增 SQLite ISO 时间日期边界规则；
  - `app/service.py`：截止词补充“截至”，新增 `_unfinished_assignment_date_boundary_error`；
  - `app/service.py::_unfinished_assignment_semantic_error`：在确认用户要求截止范围后、检查作业连接与状态前调用日期边界保护；
  - `tests/test_ask_regression.py`：补充提示词断言、错误上界拒绝、合法精确日/截至日表达、跨年内存 SQLite 边界和执行前回修测试；
  - `docs/project_feature_catalog.md`：同步智能问数行为、测试基线、限制和变更记录；
  - `PROJECT_PROGRESS.md`：按用户新要求增加第 14 条进程卡死审查准则。
- 调用链变化：
  - 正常路径仍为 `ask → generate_sql → validate_and_fix → unfinished-assignment semantic check → execute`；
  - 明确日期问题增加局部检查：`... → _unfinished_assignment_date_boundary_error → execute`；
  - 错误日期上界走 `... → date boundary error → SQLValidationError → repair_sql → validate_and_fix → semantic check → execute`；
  - 没有强制改写 SQL，没有改变最大回修次数，也没有扩大任何授权范围。
- 接受的日期表达：
  - “在某日截止”：`due_time >= 当天 AND due_time < 次日`、`date(due_time)=当天` 或当日 ISO 前缀 `LIKE`；
  - “截至某日”：`due_time < 次日` 或 `date(due_time)<=当天`；
  - 原始 `due_time <= 'YYYY-MM-DD'` 不再允许执行。
- 自动验证：
  - 新增断言在旧实现上 4 项失败、14 项通过，证明测试可以捕获原问题；
  - 实现后目标文件 18/18；
  - 安全、角色权限、教学授权与问数 79/79；
  - 助手 API、会话和检索 36/36；
  - 最终全量 278/278，2 条既有弃用警告，耗时 4 分 42 秒。
- 隔离真实模型验证：
  - 使用原库 SQLite 在线备份，源库和隔离库完整性均为 `ok`；
  - 首轮 6 次因沙箱网络 `[WinError 10013]` 在模型阶段快速失败，没有 SQL，不计为业务失败；
  - 准确停止原 8017 测试进程后，以允许千问网络的隔离服务重启；
  - “在2026年12月31日截止”和“截至2026年12月31日”各 3 次，共 6/6 命中唯一数据库基准；
  - 真实 SQL均采用允许的当日 `LIKE` 或 `date(...)` 形式；
  - 仓库外评估器最初因强制要求投影 `due_time` 误报 1 次失败；核对原始行与 SQL后只修正评估标准，对同一批原始响应重算为 6/6，没有重放请求或修改产品测试。
- 进程卡死审查：
  - 中断后检查发现 8017 已健康启动，没有因表面无响应误杀；
  - 首次矩阵在 9.5 秒内明确失败，不是卡死；
  - 全量 pytest 暂无输出期间两次检查子进程，CPU持续增长且 `Responding=True`，最终在既有合理时长内结束；
  - 最终 8017 监听进程已准确停止。
- 数据边界：
  - 原库前后均为会话 30、轮次 64、执行轨迹 64、动作草稿 32、作业 4155；
  - 隔离副本最终为会话 42、轮次 76、执行轨迹 76、动作草稿 38、作业 4155；
  - 证据位于 `D:\NL2SQL2\zjh007-fix-20260727`，不记录令牌或 `.env` 密钥，不纳入产品提交。
- 结果与潜在影响：
  - ZJH-007 更新为“已整改并经实际测试验证”；
  - 保护仅在教学学生本人未完成作业且问题明确包含受支持日期结构时启用；
  - 可能增加一次模型回修；未验证的其他字段、方言或日期表达不能据此写成已修复。
- 精确回退范围：
  - `prompts/sql_prompt.txt` 第 13 条中本批新增的日期边界子项；
  - `app/service.py` 的 `date/timedelta` 导入、“截至”截止词、`_unfinished_assignment_date_boundary_error` 及其调用；
  - `tests/test_ask_regression.py` 的 `sqlite3` 导入、本批新增 4 个测试及 2 条提示词断言；
  - `docs/project_feature_catalog.md`、`PROJECT_ISSUES.md`、本文件和 `PROJECT_PROGRESS.md` 的 ZJH-FIX-009 记录；
  - 仓库外 `D:\NL2SQL2\zjh007-fix-20260727` 可整体弃用；不得回退其他已完成批次或原数据库。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；在 ZJH-FIX-008 的教学专用规则和语义保护内追加日期边界处理。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `prompts/sql_prompt.txt` | 模型提示词 | 第 13 条没有明确 SQLite ISO 时间列与日期字面量的比较边界 | 增加精确日半开区间/`date()`/ISO 前缀和“截至日”次日排他上界规则，明确禁止原始 `due_time <= 'YYYY-MM-DD'` |
| `app/service.py` | 产品代码 | 截止词未含“截至”，语义保护不识别会漏掉日末记录的日期上界 | 增加 `date/timedelta` 使用、补充“截至”信号并新增 `_unfinished_assignment_date_boundary_error`；在未完成作业语义保护内验证精确日和截至日表达 |
| `tests/test_ask_regression.py` | 测试代码 | 缺少日期日末、次日零点和跨年对照 | 增加提示词断言、错误上界拒绝、合法精确日/截至日、跨年内存 SQLite 结果和执行前回修测试 |
| `docs/project_feature_catalog.md` | 功能目录 | 未完成作业只描述一般截止条件 | 增加已支持日期边界、未验证其他日期主题的限制和 278/278 基线 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-007 只有漏数复现 | 增加修改位置、合法表达、自动/真实证据和已验证状态 |
| `PROJECT_PROGRESS.md` | 记录与原则 | 原第 14 条只有定性卡死处理要求 | 追加当时的进程审查准则和本批执行情况；后续量化版本仍在文件开头统一生效 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-009` 记录及本文件级补充 |

- 关键替换可概括为：`due_time <= 当日日期文本` → `精确日使用 [当日, 次日)，截至日使用次日排他上界或日期归一化`。
- 仓库外新增验证辅助文件位于 `D:\NL2SQL2\zjh007-fix-20260727`：
  - `isolated_app.py`、`data_sources.isolated.yaml`：隔离服务入口与数据源；
  - `run_matrix.py`：运行两种日期问法矩阵；
  - `matrix-sandbox-blocked.json`、`matrix-date-boundary.json`：区分环境阻断与最终业务证据；
  - `teaching-isolated.db`、`server*.log`、`server.pid`：隔离数据、日志和准确进程记录。
- 没有替换通用 SQL 校验器、其他日期字段逻辑、前端或原数据库。

## 记录 ZJH-TEST-007：旧管理员教学驾驶舱适用性浏览器收口

- 日期：2026-07-27
- 用户批准与固定范围：
  - 用户批准执行上一轮建议的下一步工作；
  - 本任务只检查当前管理员页面、真实浏览器网络、旧接口权限和待验证项状态；
  - 不修改产品实现，不展开四筛选矩阵，不处理 ZJH-008、ZJH-009、ZJH-003 集合查询或敏感字段响应类型。
- 执行前源码证据：
  - 旧筛选面板在 `index.html` 中默认隐藏；
  - `app.js` 后定义的 `loadDashboard` 调用 `/api/workbench`，覆盖了前部调用旧筛选接口的同名函数；
  - `/api/teaching/dashboard` 的路由注释和既有自动测试均把它定义为管理员专属旧兼容接口。
- 隔离方式：
  - 从原 `data/teaching.db` 在线备份到 `D:\NL2SQL2\dashboard-browser-closeout-20260727\teaching-isolated.db`，备份前源库和副本完整性均为 `ok`；
  - 当前工作区代码独立启动在 8018，不使用仍在 8000 的旧进程；
  - Microsoft Edge 使用仓库外临时用户目录、无头模式和独立 9224 DevTools 端口；验证后删除临时用户目录，避免演示令牌留存；
  - 管理员令牌只在脚本内存和临时浏览器本地存储中使用，报告不记录令牌或 `.env` 密钥。
- 真实浏览器结果：
  - 管理员成功进入“运维首页”，角色标签显示“校级管理员”；
  - 页面实际渲染 3 张摘要卡、1 个工作区段和 2 个快捷操作；
  - 旧筛选面板同时满足 `hidden` 类、`aria-hidden=true` 和 `display:none`；
  - 最终网络请求包含 `/api/workbench`，不包含 `/api/teaching/dashboard`；
  - 本地网络请求无 4xx/5xx，控制台无错误；
  - 截图为 `D:\NL2SQL2\dashboard-browser-closeout-20260727\admin-workbench.png`，结构化报告为同目录 `browser-closeout.json`。
- 旧接口权限验证：
  - 管理员直接访问 `/api/teaching/dashboard` 返回 200，响应仍含卡片与筛选选项目录；
  - 学生访问同一接口返回 403；
  - 该结果只证明后端兼容接口存活和权限正确，不代表前端仍提供旧筛选页面。
- 中断与第 14 条处理：
  - 首次 8018 启动命令被用户中断后，立即检查端口、日志、CPU、响应状态和健康接口；
  - 进程 `20260` 已完成启动、`Responding=True`、健康检查 200，因此确认不是卡死并继续使用，没有盲目重启；
  - Edge 检查在 8.6 秒内完成，没有出现长时间无响应。
- 数据与进程结果：
  - 本任务只有页面与 GET 请求，没有写业务数据；
  - Edge 主进程通过 DevTools `Browser.close` 退出，9224 无监听，临时用户目录已删除；
  - 8018 服务在验证后准确停止，未替换或停止原 8000 服务。
- 结论：
  - `PROJECT_ISSUES.md` 的“待验证 B”更新为 `不再适用`；
  - 当前产品使用角色工作台，旧管理员教学驾驶舱筛选不是当前可见功能，因此不再执行无意义的四筛选浏览器矩阵；
  - 不能把该结论扩大为“全页面浏览器验收全部通过”。
- 产品影响与回退范围：
  - 产品代码、测试、数据库和前端均未修改；
  - 只需回退 `PROJECT_ISSUES.md`、本文件、`PROJECT_PROGRESS.md` 和功能目录中的本次验证记录；
  - 仓库外 `D:\NL2SQL2\dashboard-browser-closeout-20260727` 可整体弃用，不涉及任何产品代码回退。

## 记录 ZJH-TEST-008：敏感字段与缺失字段响应类型专项复现

- 日期：2026-07-27
- 用户批准与固定范围：
  - 用户批准执行“敏感字段响应类型专项复现与产品状态核对”；
  - 范围固定为学生/管理员、四类问法、两次独立会话的 16 次统一助手请求，以及一次学生端真实 Edge 检查；
  - 不修改产品实现、测试、提示词或原数据库，不继续处理 ZJH-003、ZJH-008、ZJH-009。
- 隔离方式：
  - 使用 SQLite 在线备份把原库复制到 `D:\NL2SQL2\sensitive-response-review-20260727\teaching-isolated.db`；
  - 当前工作区代码独立运行在 8018，数据源和助手会话写入均指向隔离副本；
  - Edge 使用独立 9224 端口和仓库外临时用户目录；
  - 测试后准确停止 8018 和 9224，并删除含临时演示令牌的 Edge 用户目录。
- 环境失败与重跑：
  - 首轮 16 次中，2 次学生名单请求不依赖模型并正确拒绝；其余 14 次均因受限执行环境禁止访问 DashScope 而出现 `WinError 10013`；
  - 该轮只作为环境证据，不计入产品结论；
  - 精确停止首轮测试服务后，在允许联网的环境重启同一隔离服务并重新执行完整矩阵。
- 联网矩阵结果：
  - 共 16 次请求，13 次 `success`、2 次 `rejected`、1 次模型超时；
  - 11 次 `success` 实际为 `columns=["error"]` 的“无法回答”占位结果；
  - 身份证号：3 次成功占位、1 次管理员模型超时；
  - 证件号码：4/4 次成功占位；
  - 鞋码：4/4 次成功占位；
  - 学生名单：学生 2/2 次拒绝，管理员 2/2 次按权限成功；
  - 全部响应未检测到 18 位身份证号码模式，没有证据表明返回了真实敏感事实。
- 真实前端结果：
  - 学生实际提问“查询所有学生的身份证号”；
  - API 返回 `success/nl2sql` 和一行 `error`，前端显示 `ANSWER READY / 回答已生成 / 查询完成，共返回 1 行`；
  - 前端卡片 `data-state=success`，并生成未执行的导出待确认动作；
  - 目标 `/api/assistant/query` 返回 200；页面初始化另有若干 401/403/404 控制台资源错误，本任务只如实保存在原始报告中，未将其混入 ZJH-011。
- 源码核对：
  - `service.py` 识别占位列后仅跳过 Judge，仍返回旧成功结构；
  - `assistant_orchestrator.py` 因旧结构没有 `legacy.error` 而转换为统一助手成功响应；
  - `assistant-panel.js` 按 `status=success` 渲染成功卡片。
- 数据与证据：
  - 原库测试前后均为会话 30、轮次 64、执行轨迹 64、动作草稿 32；
  - 隔离副本最终为会话 63、轮次 97、执行轨迹 97、动作草稿 46，包含首轮环境失败、联网矩阵和浏览器代表请求；
  - 结构化报告为 `matrix-report.json`、`browser-report.json`，截图为 `student-sensitive-query.png`，均位于仓库外证据目录；
  - 报告不包含令牌或 `.env` 真实密钥。
- 第 14 条执行复盘：
  - 首次服务启动命令被中断后，立即核查端口、日志和健康接口，确认服务已正常启动后从断点继续；
  - 联网矩阵的启动命令因子进程输出句柄直到批次结束才返回，造成约 99 秒不可见等待；其中一条请求实际在 45 秒达到系统模型超时，矩阵本身最终完整退出；
  - 该现象说明现有第 14 条只有定性要求，仍缺少检查间隔和单次等待上限；后续浏览器检查改为不等待子进程、独立轮询，首次控制连接失败在约 2 秒内被发现并处理；
  - 本轮没有误杀非测试进程，停止时均先按端口核实准确 PID。
- 结论与范围：
  - 待验证 A 已转为 `ZJH-011`，状态为 `未整改且已实际复现`；
  - 问题是响应类型和前端状态错误，不登记为敏感数据泄露；
  - 尚未批准任何修正。
- 精确回退范围：
  - 只需回退 `PROJECT_ISSUES.md`、本文件和 `PROJECT_PROGRESS.md` 的 `ZJH-TEST-008/ZJH-011` 记录；
  - 仓库外 `D:\NL2SQL2\sensitive-response-review-20260727` 可整体弃用；
  - 不涉及任何产品代码、测试、提示词或原数据库回退。

## 记录 ZJH-FIX-010：ZJH-011 敏感身份拒绝与无法回答占位状态最小修正

- 日期：2026-07-27
- 用户批准：先量化第 14 条进程规则，再执行 `ZJH-011` 最小修正。
- 修改前证据：隔离真实模型矩阵中 11 次 `columns=["error"]` 的“无法回答”占位被包装为成功；真实 Edge 将学生身份证号请求显示为成功结果并提供未执行的导出动作。
- 涉及文件：
  - `app/core/business_domains.py`：增加敏感身份标识词识别；
  - `app/service.py`：敏感身份请求在模型前拒绝；严格识别单行 `error` 无法回答占位并转入非数据错误；
  - `app/core/assistant_orchestrator.py`：把该占位错误映射为 `unsupported`，敏感身份错误映射为 `rejected`；
  - `app/static/assistant-panel.js`、`assistant-panel.css`、`index.html`：增加不支持状态和缓存版本；
  - `tests/test_ask_regression.py`、`tests/test_assistant_api.py`、`tests/test_v3_assistant_panel.py`：增加服务、统一接口和前端状态回归；
  - `docs/project_feature_catalog.md`、三份项目记录：同步能力、证据和进度。
- 调用链变化：
  - 敏感身份问法：`assistant/query → _nl2sql → ask → sensitive identity check → format_error → rejected`，不进入模型、SQL执行或动作生成；
  - 普通缺失字段：`模型生成严格 error 占位 → ask 识别 unsupported trace → format_error → _nl2sql 转 unsupported`，不向前端传数据、SQL或动作；
  - 有效查询、有效零行、非严格占位和其他结构化错误保持原链路。
- 自动验证：
  - 修正前目标 7/7 失败；修正后目标 7/7；
  - 相关问数、助手、前端、安全和角色权限 109/109；
  - 全量 283/283，2 条既有弃用警告，耗时 5 分 0.69 秒；
  - 107 个 Python 文件 AST 语法检查通过，目标差异检查无空白错误。
- 隔离真实运行：
  - 原库在线备份后在 8019 运行当前代码，学生/管理员的敏感身份请求均快速拒绝且无数据、SQL、动作和身份证模式；
  - 第一轮管理员鞋码请求达到既有 45 秒模型硬超时；缓存就绪后同一两角色两问题矩阵 4/4 通过，鞋码均为 `unsupported`；
  - 真实 Edge 复核 2/2：敏感身份显示 `ACCESS DENIED`，鞋码显示 `NOT SUPPORTED`，均不渲染表格；无 JavaScript 运行时异常；
  - 学生页面访问管理员数据源接口产生的 401/403 已按 URL 保留为独立资源日志，不写成 ZJH-011 失败。
- 第 14 条执行：
  - 全量测试以独立日志和状态文件运行，10 秒内首查、随后不超过 30 秒轮询并设置 600 秒硬超时，最终正常完成；
  - 真实接口和浏览器任务分别设置 300/180 秒硬超时；出现 45 秒业务超时后检查状态并执行一次固定范围重跑，没有无限追加步骤；
  - 服务停止前核对测试 PID；普通权限终止被拒绝后，按同一准确 PID 提权终止进程树，没有误杀其他项目服务。
- 数据、安全与清理：
  - 原库前后保持动作草稿 32、会话 30、轮次 64、执行轨迹 64；
  - 隔离库最终为动作草稿 32、会话 42、轮次 76、执行轨迹 76；
  - 8019、9225 已停止；两个临时 Edge 用户目录已删除，报告未写入令牌或真实密钥；
  - 证据位于 `D:\NL2SQL2\zjh011-fix-20260727`，不纳入产品提交。
- 潜在影响与未修改范围：
  - 敏感身份关键词采用保守拒绝，普通缺失字段仍可能受模型超时影响；
  - 未修改数据库结构、角色/表/字段/行级权限、Judge、集合查询和子查询范围传播；
  - 未处理本批观察到的学生访问管理员数据源接口 401/403 日志。
- 精确回退范围：
  - `business_domains.py` 的敏感身份词表与识别函数；
  - `service.py` 的占位标记、严格识别函数、敏感身份前置拒绝和 unsupported 返回分支；
  - `assistant_orchestrator.py` 的 `legacy.trace.result_kind == "unsupported"` 分支；
  - 前端三个文件中的 unsupported 状态、样式和 `zjh-011` 缓存版本；
  - 本批新增测试和四份文档中的 ZJH-011/ZJH-FIX-010 记录；
  - 不得回退此前 ZJH-001～010 其他批次或原数据库。

### 文件级修改补充（后补，不替换原记录）

- 仓库内新增文件：无；ZJH-011 使用现有业务词表、服务格式化、统一助手和前端组件扩展状态。

| 修改文件 | 修改性质 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|---|
| `app/core/business_domains.py` | 产品代码 | 通用拒绝词未单独覆盖身份证号、证件号码等身份标识问法 | 新增 `SENSITIVE_IDENTITY_TERMS` 和 `sensitive_identity_question_hit`，集中识别中英文敏感身份字段表达，供教学问数前置拒绝 |
| `app/service.py` | 产品代码 | `columns=["error"]` 只用于跳过 Judge，最终仍调用 `format_success`；敏感身份问题可能先进入模型 | 新增无法回答标记和 `_unanswerable_placeholder_message` 严格识别；在教学策略下先检查敏感身份问法；占位结果写入 `result_kind=unsupported` 并通过 `format_error` 返回，不再进入成功数据链 |
| `app/core/assistant_orchestrator.py` | 产品代码 | 只要旧响应没有 `legacy.error` 就按 NL2SQL 成功转换 | 在错误转换分支读取 `legacy.trace.result_kind`；`unsupported` 返回统一成功外壳和不支持回答，但不附带数据、SQL、错误或动作；敏感请求仍进入拒绝路径 |
| `app/static/assistant-panel.js` | 前端代码 | `stateFrom` 没有不支持状态，模型占位会落入成功卡 | 新增 `answer_type === "unsupported"` 状态；状态标题新增 `NOT SUPPORTED / 当前问题暂无法处理` 和安全提示 |
| `app/static/assistant-panel.css` | 前端样式 | 不支持状态没有视觉样式 | 将 `.is-unsupported` 加入琥珀色非成功状态边框和标签样式，不复用绿色成功状态 |
| `app/static/index.html` | 前端入口 | 浏览器可能继续缓存修正前组件和样式 | 将助手 CSS/JS 资源版本替换为 `?v=zjh-011`，确保真实页面加载本批状态逻辑 |
| `tests/test_ask_regression.py` | 测试代码 | 缺少服务层模型前拒绝和占位转换测试 | 新增所有角色敏感身份词不调用模型、模型单行无法回答占位转换且不产生 Judge 的回归 |
| `tests/test_assistant_api.py` | 测试代码 | 缺少统一助手的 unsupported/rejected 响应契约 | 新增占位结果不含数据/SQL/动作，以及学生/管理员敏感身份请求均在模型前拒绝的端点测试 |
| `tests/test_v3_assistant_panel.py` | 测试代码 | 静态契约不认识 unsupported 和新缓存版本 | 增加状态映射、标题、CSS 类和 `zjh-011` 资源顺序断言 |
| `docs/project_feature_catalog.md` | 功能目录 | 用户可见状态只列成功/空结果/无权限等 | 补充敏感身份前置拒绝、无法回答转不支持、前端 `NOT SUPPORTED`、283/283 基线和变更摘要 |
| `PROJECT_ISSUES.md` | 记录 | ZJH-011 为未整改复现 | 追加获批修正、自动/接口/Edge 证据、数据边界、潜在影响和最终状态 |
| `PROJECT_PROGRESS.md` | 原则与进度 | 第 14 条仍较定性，ZJH-011 尚未收口 | 量化后台/首查/轮询/硬超时/精确终止规则，并增加本批完成情况 |
| `PROJECT_CHANGES.md` | 记录 | 无本批详细修改条目 | 新增当前 `ZJH-FIX-010` 记录及本文件级补充 |

- 关键替换有三处：
  - `敏感身份问法 → 模型尝试回答` 替换为 `敏感身份问法 → 服务层立即拒绝`；
  - `严格 error 占位 → success 数据行 + 导出动作` 替换为 `严格 error 占位 → unsupported 文本，无数据/SQL/动作`；
  - `前端 ANSWER READY 成功卡` 替换为 `NOT SUPPORTED 非成功结果卡`。
- 仓库外新增验证辅助文件位于 `D:\NL2SQL2\zjh011-fix-20260727`：
  - `prepare_isolation.py`、`isolated_app.py`、`data_sources.isolated.yaml`：创建一致副本并让服务只读写隔离数据；
  - `run_pytest.py`、`run_script.py`：分别为 pytest 和真实脚本提供日志、状态文件、硬超时与精确进程终止；
  - `runtime_verify.py`：执行学生/管理员敏感身份与普通缺失字段接口矩阵；
  - `browser_verify.py`：通过 Edge DevTools 验证 `ACCESS DENIED`、`NOT SUPPORTED`、表格和运行时异常；
  - `verify_final_counts.py`：核对原库与隔离库助手相关表计数；
  - `*-report.json`、`*.status.json`、`*.log`、`browser-*.png`：保存原始响应摘要、任务状态、日志和页面截图；
  - `teaching-isolated.db`：承载专项会话和轨迹，避免污染原库。
- 仓库外报告未保存认证令牌或 `.env` 密钥；Edge 临时用户目录已删除，辅助文件不属于产品提交。

## 记录 ZJH-TEST-009：当前累计修改最终演示回归

- 日期：2026-07-27。
- 用户批准：执行“当前累计修改最终演示回归”。
- 固定范围：
  - 复核当前累计修改的全量自动测试、阶段 F 与主要业务流程；
  - 在隔离数据库和独立服务上检查六角色权限、角色切换、真实模型问数和真实 Edge 页面状态；
  - 本批只测试和记录，不修正新发现的产品问题。
- 环境与隔离：
  - `pip check` 返回无依赖冲突；
  - 使用 SQLite 在线备份建立 `D:\NL2SQL2\final-demo-regression-20260727\teaching-isolated.db`；
  - 独立服务使用 8020 端口、隔离数据源配置和当前工作区代码，不替换其他端口服务。
- 自动测试实际结果：
  - 全量 pytest **283/283** 通过，2 条既有弃用警告，耗时 311.00 秒；
  - 阶段 F、六角色导航/权限及演示业务流程定向测试 **22/22** 通过，耗时 32.16 秒；
  - 定向范围覆盖未登录拒绝、六角色导航和允许/拒绝、临时数据库、通知目标，以及课程空间、作业提交/退回/重交/评分/发布、考勤、答疑、辅导员支持、学院与教务流程。
- 隔离真实 HTTP 实际结果：
  - 固定 8 组检查 **8/8** 通过；
  - 学生、教师、辅导员、学院负责人、教务处和管理员各自的允许/拒绝接口符合范围；
  - 未登录访问工作台为 401；教师账号从学院负责人切换为教师后，旧令牌 401，新令牌可读本人教学班且组织单位接口 403。
- 隔离真实模型实际结果：
  - 固定 6 问、每问单次请求且不自动重试，整体 **6/6** 通过；
  - 未完成作业首问 1 行、同会话按课程分组 1 行、同会话只看已截止 0 行；
  - 2026-12-31 明确日期边界返回 1 行；
  - 敏感身份问题返回 `rejected`，不含数据、SQL、动作或身份证模式；
  - 不存在的鞋码字段返回 `success/unsupported`，不含数据、SQL或动作。
- 真实 Edge 实际结果：
  - 成功表格、敏感拒绝、缺失字段不支持三种页面状态 **3/3** 通过；
  - 无 JavaScript 运行时异常，页面未出现身份证号码模式；
  - 普通角色初始化仍请求 `/api/data-access/sources`，未登录为 401、学生登录后为 403；后端权限拒绝正确，但前端无必要请求已登记为 `ZJH-012 未整改且已实际复现`，本批未修正。
- 第 14 条执行：
  - 全量和分组测试均使用独立日志、状态文件和明确硬超时；全量 600 秒、阶段 F 180 秒；
  - 模型矩阵设置单请求 55 秒、整体 360 秒；浏览器与 HTTP 使用有界脚本；
  - 任务结束前核对 8020 服务进程树和 9226 浏览器会话，按准确目标停止，没有盲目等待或误杀其他服务。
- 数据、安全与清理：
  - 原库前后均为动作草稿 32、会话 30、轮次 64、执行轨迹 64、作业 4155、作业提交 328028、审计 0；
  - 隔离库最终为动作草稿 36、会话 37、轮次 73、执行轨迹 73、作业 4155、作业提交 328028、审计 1；
  - 8020、9226 已停止，Edge 临时用户目录已删除；
  - 报告没有保存认证令牌、密码或 `.env` 真实密钥。
- 仓库内本批修改文件：
  - `PROJECT_ISSUES.md`：追加最终回归证据和 ZJH-012；同步纠正 ZJH-007 总览中的过期状态；
  - `PROJECT_CHANGES.md`：新增本条测试记录及证据文件说明；
  - `PROJECT_PROGRESS.md`：更新完成状态、当前边界和下一步；
  - `docs/project_feature_catalog.md`：同步当前质量基线和浏览器边界；
  - 产品代码、提示词、数据库和测试实现均未修改。
- 仓库外新增验证辅助文件及用途：
  - `prepare_isolation.py`、`isolated_app.py`、`data_sources.isolated.yaml`：生成一致数据库副本并让独立服务只使用隔离数据；
  - `run_pytest.py`、`run_script.py`：提供日志、状态、硬超时和可检查运行方式；
  - `http_business_verify.py`：执行六角色、未登录和角色切换真实 HTTP 矩阵；
  - `model_verify.py`：执行固定 6 问真实模型矩阵且禁止自动重试；
  - `browser_verify.py`：通过 Edge DevTools 验证三类页面状态、表格、资源错误和运行时异常；
  - `verify_final_counts.py`：核对原库与隔离库测试前后计数；
  - `*-report.json`、`*.status.json`、`*.log`、`browser-*.png`：保留结构化结论、进程状态、日志和页面截图。
- 结论与边界：
  - 当前演示核心范围回归通过；
  - ZJH-003 集合查询、ZJH-008 Judge、ZJH-009 子查询范围和新登记 ZJH-012 不因本次回归自动视为修复；
  - 本结果不等同于生产环境性能、安全、多浏览器或外部系统验收。
- 精确回退范围：
  - 只需回退上述四份文档中本次 `ZJH-TEST-009`、ZJH-012 和质量基线补充；
  - ZJH-007 总览状态纠正应与其已有详细修复证据保持一致，不属于产品回退；
  - 仓库外 `D:\NL2SQL2\final-demo-regression-20260727` 可整体弃用；
  - 不涉及任何产品实现、既有测试、提示词、原数据库或此前获批修正的回退。

## 记录 ZJH-TEST-010：server 四路检索真实验收

- 日期：2026-07-28。
- 用户批准：执行“server 四路检索真实验收”。
- 固定范围：只验证 server 模式下 Milvus、Elasticsearch、PostgreSQL/pgvector 和图关系检索的真实可用性、命中证据与缓存行为；不修改产品实现。
- 实际环境：
  - Docker Desktop Linux Engine 正常运行；
  - server 依赖容器全部达到健康状态；
  - 实际装载 Milvus 向量 720 条、Elasticsearch 文档 720 条、pgvector 记录 44 条、图检索元数据 68 张表和 98 条关系。
- 实际结果：
  - 向量检索实际使用 Milvus；
  - 关键词检索实际使用 Elasticsearch；
  - 术语/语义检索实际使用 PostgreSQL/pgvector；
  - 关系扩展实际使用图关系数据；
  - 四路均有真实命中，不是仅检查配置文件或容器端口；
  - 首次完整检索耗时 95.646 秒，缓存命中后耗时 4.603 秒。
- 结果边界：
  - 结论只证明当前测试数据和当前 Docker 环境中的四路检索链可运行；
  - 不等同于大规模数据、并发、故障恢复或生产性能验收；
  - BIRD 数据集尚未纳入本条测试。
- 仓库内修改文件：
  - `PROJECT_CHANGES.md`：新增本条真实验收记录；
  - `PROJECT_PROGRESS.md`：同步 server 验收进度；
  - 产品代码、配置、数据库和测试实现均未修改。
- 仓库外证据：
  - `D:\NL2SQL2\zjh-external-acceptance-20260728\evidence\server-four-route-acceptance-20260728.md`；
  - 证据不包含 `.env` 密钥、访问令牌或数据库密码。
- 精确回退范围：只需删除上述两份项目文档中的 ZJH-TEST-010 记录；不涉及产品实现和 Docker 数据的产品回退。

## 记录 ZJH-TEST-011：外部 SQLite 与业务 PostgreSQL 双库接入验收

- 日期：2026-07-28。
- 用户批准：执行“外部 SQLite 与业务 PostgreSQL 双库接入验收”。
- 固定范围：
  - 验证外部 SQLite 上传注册、外部 PostgreSQL 手工配置接入、连接、Schema 扫描、只读限制、固定数据源 NL2SQL 和自动选源；
  - 使用同构图书借阅业务数据对比两个数据库的查询事实；
  - 只测试和登记问题，不修正新发现的产品实现。
- 隔离方式：
  - 使用 `D:\NL2SQL2\zjh-external-acceptance-20260728\runtime-project` 作为运行副本，排除 `.git`、`.venv`、`.env`、缓存目录；
  - SQLite 测试库为 `datasets\library_acceptance.sqlite`；
  - PostgreSQL 使用独立业务库和独立只读角色，与检索库 `nl2sql_retrieval` 分离；
  - 测试凭据只存在隔离运行配置中，未写入项目记录、日志或证据。
- 测试数据：
  - 两库结构一致，共 6 张表、24 列；
  - 数据包含 3 个分馆、4 个分类、10 本图书、6 名读者、12 条借阅、2 条罚款；
  - SQLite 完整性检查为 `ok`。
- 接入与权限实际结果：
  - 学生访问数据接入接口为 403；
  - SQLite 通过 `/api/data-access/sources/db-file` 上传注册成功，扫描得到 6 张表；
  - PostgreSQL 手工配置加载成功；
  - 管理员可在数据源列表同时看到内置教学库、外部 SQLite 和业务 PostgreSQL；
  - 两个外部源连接 2/2 成功，Schema 扫描均得到 6 张表、24 列；
  - SQLite 表预览成功，写入被阻止；数据库层对 SQLite 和 PostgreSQL 的写入均被阻止；
  - 学生直接指定外部源问数被 403 拒绝；
  - PostgreSQL 维护预览接口返回 400，源码与接口行为表明当前维护预览实现仅支持 SQLite。
- 查询事实基线：
  - 各分馆图书数：中央馆 5、东区馆 3、西区馆 2；
  - 2026 年 6 月借阅最多读者：张伟 4 次；
  - 截至 2026-07-01 逾期未归还记录：3 条；
  - 从未被借阅图书：未借阅手册；
  - 分类图书数：计算机 5、文学 3、历史 2、科学 2；
  - 罚款大于 100 元：0 条。
- NL2SQL 实际结果：
  - 两库直接 SQL 基线全部一致；
  - 固定数据源查询：SQLite 5/6、PostgreSQL 5/6；
  - 自动选源查询：SQLite 1/1、PostgreSQL 1/1；
  - 14/14 查询的数值事实正确，但固定源“按分馆统计”两次只返回 `branch_id` 和数量，缺少分馆名称，因此按业务可读性严格口径为 12/14。
- 接口检查原始结果：17/20。
  - 一项为测试断言预期 404、实际返回 405；结合 OpenAPI 可确认不存在 PostgreSQL 注册端点，不能把该项写成接口存在；
  - 两项为外部 SQLite 和 PostgreSQL 的 `/api/schema` 均返回 200 但表、列和 DDL 为空。
- 新登记问题：
  - `ZJH-013`：外部数据源 `/api/schema` 被教学域表白名单过滤为空；
  - `ZJH-014`：PostgreSQL 可通过配置运行，但没有对应前端新增入口和注册 API；
  - `ZJH-015`：固定外部源的分组统计结果只返回内部编号，自动路由同义查询却能返回业务名称。
- 仓库内本批修改文件：
  - `PROJECT_ISSUES.md`：新增 ZJH-013～015 的复现、源码证据和状态；
  - `PROJECT_CHANGES.md`：新增本条测试记录；
  - `PROJECT_PROGRESS.md`：同步外部双库验收结果和下一步；
  - 产品代码、产品配置、原数据库和既有测试实现均未修改。
- 仓库外新增辅助与证据：
  - `prepare_dual_db.py`：建立同构 SQLite/PostgreSQL 数据与只读账号；
  - `run_dual_acceptance.py`：执行接入、权限、直接 SQL、固定源和自动路由矩阵；
  - `inspect_dual_failures.py`：复核失败项、OpenAPI 和外部 Schema 响应；
  - `evidence\dual-db-expected-results.json`：保存人工可核对的查询基线；
  - `evidence\dual-db-acceptance-results.json`：保存结构化实测结果；
  - `evidence\dual-db-acceptance-20260728.md`：保存脱敏验收记录；
  - `logs\dual-db-acceptance*.log`：保存执行输出、错误和进度；
  - 日志和证据扫描未发现凭据或密钥模式。
- 最终结论：外部 SQLite 和独立业务 PostgreSQL 的连接、扫描、只读查询与 NL2SQL 主链已经实际运行；但接口展示、PostgreSQL 自助接入和固定源结果可读性仍有已复现缺口，因此不能写成双库功能完整通过。
- 精确回退范围：
  - 仓库内只回退三份记录文件中的 ZJH-TEST-011、ZJH-013～015 和进度补充；
  - 仓库外隔离目录可独立弃用；
  - PostgreSQL 测试库和只读角色属于本次 Docker 验收环境，不影响项目产品库；
  - 不涉及任何产品实现、组长原配置、原数据库或此前修正的回退。

## 记录 ZJH-TEST-012：BIRD Mini-Dev 固定子集 server 端到端验收

- 日期：2026-07-28。
- 用户批准：开始下一步工作，即执行既定“BIRD Mini-Dev 固定子集 server 端到端验收”。
- 固定范围：
  - 使用一个官方BIRD数据库和15个官方问题；
  - 简单5题、中等7题、困难3题；
  - 每题一次请求、不自动重试；
  - 验证接入、Schema、权限、只读、四路检索、SQL可执行率和Gold执行结果；
  - 不修复本批发现的问题。
- 官方资料与隔离：
  - 从 BIRD 官方 OSS 下载 `minidev.zip`，800,943,648 字节，SHA-256 为 `CC48BA16838204E4E214512030CB572EEB5F7BCDD999BAE4B9B6FF12EC13B92F`；
  - ZIP共122个条目，可正常读取；只提取评测所需数据库、描述和SQLite题集；
  - 使用 `european_football_2`：597,754,880 字节、完整性 `ok`、7表199列31外键；
  - 199列超过 server 默认120列触发阈值；
  - 隔离运行副本排除 `.git`、`.venv`、`.env` 和缓存，不修改原项目配置或数据库。
- 固定评测方式：
  - 官方 `database_description` 生成隔离画像和术语索引；
  - 官方问题 `evidence` 作为当前请求的 `user_glossary`；
  - Gold SQL只在评估器中执行，不进入模型提示词；
  - 服务端模型硬超时55秒，整体任务硬超时1200秒。
- 前置与安全实际结果：
  - Docker五个依赖容器均为 healthy；
  - 学生数据接入403、管理员SQLite路径注册200、Schema扫描7表199列、学生指定外部源问数403、数据库写入阻止；
  - 前置检查6/6通过。
- server 四路实际结果：
  - 预热成功，用时53.359秒；
  - Milvus 206条、Elasticsearch 206条、pgvector术语199条；
  - 代表性检索和15/15题均报告 `vector/keyword/glossary/graph`，`retrieval_status=used`；
  - 没有以容器健康或配置存在代替真实召回。
- 15题原始结果：
  - SQL可执行14/15，达到预设12/15；
  - 严格EX 11/15，达到预设9/15；
  - 简单4/5、中等5/7、困难2/3；
  - 查询总耗时75.594秒，平均5.040秒，最长20.781秒；
  - 所有题 `attempts=1`，没有自动重放。
- 失败项审查：
  - 1145和1040返回正确业务集合但额外返回指标列，并存在未指定并列顺序，严格EX仍保留失败；
  - 1107生成结果为最高分首次日期2013-02-15；数据库确认最高分82出现在2013年三个日期，Gold却返回2016-04-21且当日分数72，记录为Gold与题意/事实冲突，不修改Gold；
  - 1028在四路检索后因DashScope TLS EOF没有生成SQL，按规则未自动重试，只记录为外部环境失败。
- 两种结果边界：
  - 原始严格EX永久保持11/15；
  - 14个得到SQL结果的问题经数据库事实审查为14/14可解释正确；
  - 人工审查不能把未回答的1028写成通过，也不能覆盖原始EX。
- 新问题：
  - `ZJH-016`：外键表名大小写与缺失目标列未规范化，运行时关系图为8节点33关系；
  - `ZJH-017`：两题额外投影未要求指标列，业务集合正确但严格EX失败；
  - 两项均未获批修正。
- 仓库内本批修改文件：
  - `PROJECT_ISSUES.md`：登记 ZJH-016～017；
  - `PROJECT_CHANGES.md`：新增本条测试记录；
  - `PROJECT_PROGRESS.md`：更新BIRD阶段结果和下一步；
  - 产品代码、提示词、产品测试、原配置和原数据库均未修改。
- 仓库外新增内容：
  - `minidev.zip`、`dataset\`：官方包和选择性提取数据；
  - `prepare_bird_acceptance.py`：固定题号、生成画像/词表、执行Gold并生成数据清单；
  - `run_bird_acceptance.py`：执行注册、权限、只读、预热、15题和严格结果比较；
  - `fixtures\`：固定问题、派生画像和术语词表；
  - `evidence\dataset-manifest.json`、`bird-server-acceptance-results.json`、`bird-server-acceptance-20260728.md`：原始和审查证据；
  - `logs\bird-server-acceptance*.log`：进度、标准输出和错误日志；
  - 所有内容位于 `D:\NL2SQL2\bird-mini-dev-acceptance-20260728`，不纳入产品提交。
- 第14条执行：
  - 800MB下载独立后台运行，10～30秒检查文件增长，15分钟硬超时；
  - 首次下载因Windows离线吊销检查立即失败，核对日志后仅对该下载使用 `--ssl-no-revoke` 重试，不修改系统设置；
  - 预热与15题任务后台运行并以进度文件、CPU和日志核查，20分钟整体硬超时；
  - 任务正常结束，没有盲等或误杀其他进程。
- 最终结论：BIRD固定子集 server端到端验收达到预设课程项目标准；该结论不等同于完整500题BIRD排行榜结果，不证明关系图兼容问题已修复。
- 精确回退范围：
  - 仓库内只需回退三份记录文件中的 ZJH-TEST-012、ZJH-016～017 和进度补充；
  - 仓库外隔离目录可独立弃用；
  - Docker内BIRD专用检索索引为验收环境数据；
  - 不涉及产品实现和此前修正的回退。

## 记录 ZJH-FIX-011：外部数据源接入闭环最小修正

- 日期：2026-07-28。
- 用户批准：执行“外部数据源接入闭环最小修正：ZJH-014 + ZJH-013”，保持 `zjh-dev` 现有结构，不整体复制测试分支代码。
- 修改原因与修正前证据：
  - ZJH-014：PostgreSQL 手工配置后能够扫描和问数，但运行时没有注册 API，前端也没有新增入口；
  - ZJH-013：外部 SQLite 与 PostgreSQL 扫描均为 6 表24列，但 `/api/schema` 因套用教学域表白名单返回空结构；
  - 两项均已在修正前真实复现，不把源码推测当作运行结论。
- 仓库内新增文件：

| 新增文件 | 内容介绍 |
|---|---|
| `tests/test_external_data_access.py` | 8 项外部数据接入闭环回归，覆盖权限、密钥外置、失败回滚、脱敏、外部 Schema 与前端接线。 |

- 仓库内修改文件：

| 修改文件 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|
| `app/core/data_sources.py` | 数据源 URL 直接从配置加载，没有独立密码环境变量解析。 | 增加 `password_env` 名称校验和运行时解析；优先读取进程环境，其次读取本地 `.env`，通过 SQLAlchemy URL 注入密码且不回写配置。 |
| `app/core/data_access.py` | 只有 SQLite、CSV、数据库文件注册流程；数据源列表和连接错误缺少统一密码脱敏。 | 增加 PostgreSQL 参数校验、注册前扫描、强制只读、画像/词表生成和失败精确回滚；增加 URL 与连接错误脱敏，列表不返回 `password_env`；PostgreSQL 扫描连接启用默认只读事务。 |
| `app/api/data_access.py` | 没有 PostgreSQL 请求模型和注册路由；连接测试直接返回底层异常文本。 | 增加不含真实密码字段的请求模型和管理员专用 `POST /api/data-access/sources/postgresql`；连接错误改走脱敏输出。 |
| `app/api/routes.py` | `/api/schema` 对所有源统一使用教学域 `allowed_tables`。 | 仅内置 `teaching` 使用教学表白名单；外部源使用其扫描表集合，同时继续应用角色禁用字段与画像屏蔽字段。 |
| `app/static/api.js` | 前端客户端没有 PostgreSQL 注册方法。 | 增加 `registerPostgresqlSource` API 封装。 |
| `app/static/index.html` | 数据接入页没有 PostgreSQL 表单，静态资源仍使用上一批缓存版本。 | 增加 PostgreSQL 页签和主机、端口、库名、用户、密码环境变量名、SSL 模式字段；明确页面不接收真实密码；更新缓存标识。 |
| `app/static/data-access-view.js` | 没有 PostgreSQL 表单切换与提交逻辑。 | 增加页签切换、管理员提交、成功后刷新和表单复位。 |
| `tests/test_v3_assistant_panel.py` | 两处契约仍断言旧 `api.js` 缓存版本。 | 只同步为本批缓存标识，保留资源顺序和旧壳替换等原断言，不放宽测试。 |
| `docs/project_feature_catalog.md` | PostgreSQL 仍描述为仅配置可用，质量基线为 283/283。 | 同步页面、API、配置、安全和只读边界，并更新本批质量基线与变更记录。 |
| `PROJECT_ISSUES.md`、`PROJECT_CHANGES.md`、`PROJECT_PROGRESS.md` | ZJH-013/014 仍为已复现未修正，进度停在修复决策。 | 追加本批实际修改、验证、影响、回退和剩余问题，不删除历史证据。 |

- 调用链变化：
  - PostgreSQL 注册：管理员表单 → 前端 API → 注册路由 → 参数与环境变量名校验 → 运行时解析密码 → 只读扫描 → 写入无密码 URL 和 `password_env` 引用 → 创建画像/词表 → 返回脱敏源信息；
  - 失败路径：任一步失败 → 仅移除本次新增的数据源项、画像和词表 → 返回脱敏错误，不覆盖其他源；
  - 外部 Schema：加载外部完整结构 → 以外部实际表集合为允许表 → 应用角色禁用字段和画像屏蔽字段；
  - 教学 Schema、SQLite/CSV 注册、NL2SQL 路由与数据维护写入权限保持原链路。
- 自动验证：
  - Python 目标文件 AST 和 JavaScript `node --check` 通过；
  - 新增专项 8/8，相关数据接入与安全回归 76/76，缓存契约目标 9/9；
  - 最终全量 291/291，0 失败。
- 隔离真实运行：
  - 使用独立注册表、画像/词表目录、既有真实业务 SQLite 和独立 PostgreSQL 5433 环境，不修改主 `data_sources.yaml`；
  - 学生注册返回 403；缺少密码环境变量返回 400 且未持久化；管理员注册返回 200、扫描 6 表24列；
  - SQLite 扫描及两个源的 `/api/schema` 均为 6表24列；
  - PostgreSQL 维护写入和数据库账号直接写入均被拒绝；
  - SQLite 与 PostgreSQL 各执行一条真实千问 NL2SQL，均返回数据库标准答案标量 3；
  - 固定闭环共 13/13，证据位于 `D:\NL2SQL2\zjh-external-closure-20260728`。
- 第14条执行：
  - Windows 下 pytest 在全部用例执行后曾停留于会话清理；逐项确认通过后，使用仓库外测试插件在既有隔离数据库夹具完成精确清理时记录退出状态，没有修改产品测试或断言；
  - 最终全量进程按约 6 分钟既有基线监控，CPU 持续增长，随后自行结束；没有误判卡死、重复启动或杀死正常进程；
  - 状态文件记录 `exitstatus=0、testscollected=291、testsfailed=0`。
- 安全、影响与边界：
  - 没有输出、记录或提交 `.env` 真实密钥；主数据源配置未被真实验收污染；
  - 页面/API 只传密码环境变量名，真实密码仍由部署者在服务器环境或 `.env` 中准备；
  - PostgreSQL 始终按只读数据源处理，未开放数据维护写入；
  - 这是课程项目单机配置闭环，不代表生产密钥托管、并发、灾备或正式运维能力已验收；
  - ZJH-015～017、ZJH-012 及其他独立问题没有混入本次修正。
- 精确回退范围：
  - 删除 `tests/test_external_data_access.py`；
  - 回退 `data_sources.py` 的 `password_env` 解析、`data_access.py` 的 PostgreSQL 注册/脱敏/精确回滚、API 新路由、外部 Schema 分支和前端 PostgreSQL 表单/客户端方法；
  - 将本批静态资源缓存标识及两处对应测试契约恢复到前一版本；
  - 回退四份文档中仅属于 ZJH-013/014、ZJH-FIX-011 和 291/291 基线的增量；
  - 不得回退此前 ZJH-001～012 的产品修改、测试、记录或原数据库。

### 后续真实浏览器复核补充（不删除原修正记录）

- ZJH-FIX-011 的后端注册、密钥外置、只读、失败回滚和外部 Schema 自动/接口验证结论仍有效。
- 2026-07-28 的真实 Edge 进一步发现：注册 POST 200 后前端空引用误报失败，因此 ZJH-014 从“已整改并经实际测试验证”更新为“部分整改”。
- 该表现另登记为 ZJH-018；本次只测试登记，不修改或回退 ZJH-FIX-011。

## 记录 ZJH-TEST-013：外部数据源前端真实浏览器验收

- 日期：2026-07-28。
- 用户批准与固定范围：执行一次封闭的外部数据源前端真实浏览器验收；覆盖管理员页面注册、Schema、可见问数、学生权限、网络/控制台、安全和清理；只测试登记，不修改产品实现。
- 隔离环境：
  - 当前 `zjh-dev` 代码运行在 8021；
  - 使用原教学库的独立副本、独立 `data_sources.yaml`、独立画像/词表目录和独立 Edge 用户目录；
  - PostgreSQL 使用既有 5433 只读业务测试库，真实密码仅在隔离服务进程环境中解析；
  - 主项目配置和原数据库不承载本次注册、会话或测试文件。
- 管理员页面：
  - “数据维护”真实显示 SQLite、PostgreSQL、CSV 三个入口；
  - PostgreSQL 表单只提供密码环境变量名字段，没有 `type=password` 或真实密码字段；
  - 表单 HTML 校验通过，真实网络请求 `POST /api/data-access/sources/postgresql` 返回 200；
  - 隔离注册表由2个源变为3个源，刷新后“浏览器验收图书库”正常显示。
- 新问题 ZJH-018：
  - 后端成功后，异步事件回调执行 `e.currentTarget.reset()` 时 `currentTarget` 已为 `null`；
  - 页面错误显示 `Cannot read properties of null (reading 'reset')`，把成功注册展示为失败；
  - 原始证据保存在 `registration-ui-defect-evidence.json`，没有用刷新后成功掩盖当轮错误。
- Schema：
  - 首轮点击与页面启动收尾发生竞态，DOM已有6表24字段但视图被切回首页；该轮不写成产品失败；
  - 等待启动稳定后重新执行短检查，真实页面显示 `Schema 画像`、6张表、24字段，6个业务对象均可见；
  - 结构化结果 `schema-visible-results.json` 为通过，截图为 `admin-postgresql-schema-visible.png`。
- 新问题 ZJH-019：
  - 从“问数知识库”选择新注册外部源并点击“问数”，在当前可见统一助手提交借阅逾期统计；
  - 响应为 `success`，但证据源为 `teaching`、路由为 `deterministic_metric`；
  - 页面返回“业务表68、待处理质量项0、数据源状态正常”三行教学工作台统计，没有返回已由后端固定源验证的外部库基准3；
  - 该结果同时由结构化响应和 `admin-external-query.png` 保存。
- 学生权限与既有问题：
  - 学生数据维护导航真实隐藏；
  - 学生直接提交 PostgreSQL 注册请求返回403；
  - 学生页面仍出现2个管理员数据接入接口401/403和对应控制台错误，继续归入既有 ZJH-012，不重复编号。
- 第14条与测试设施复盘：
  - 首次成功注册后，测试脚本等待精确中文提示导致一次超时；后续隔离服务重置注册表但遗漏测试画像/词表，产生一次明确的夹具“配套文件已存在”400；均已与产品结果分开；
  - `Start-Process` 加输出重定向曾使调用约51秒后返回；移除组合等待和重定向后，服务、Edge、脚本启动命令均约1～3秒返回；
  - 一次 Edge 审批工具调用约31秒返回，子进程实际正常；规则14已补充工具调用层超过30秒也必须更换方式；
  - 最终业务矩阵和短 Schema 检查均正常结束，没有继续追加测试阶段或误杀其他进程。
- 仓库内修改：
  - 仅更新 `PROJECT_ISSUES.md`、`PROJECT_CHANGES.md`、`PROJECT_PROGRESS.md` 和 `docs/project_feature_catalog.md` 的真实测试结论；
  - 产品代码、自动测试、提示词、主配置和数据库均未修改。
- 仓库外证据：
  - `registration-ui-defect-evidence.json`：注册200与页面空引用；
  - `browser-resume-results.json`：源列表、问数和学生权限；
  - `schema-visible-results.json`：Schema页面6/24；
  - 四张最终截图和仓库外 CDP/隔离启动脚本；
  - 目录为 `D:\NL2SQL2\zjh-external-browser-20260728`，报告不含令牌或真实密钥。
- 最终结论：
  - 外部数据源页面注册的后端动作、刷新后源列表、Schema和学生权限可用；
  - 页面注册反馈和可见统一助手外部问数未通过，整个前端闭环不能写成验收通过；
  - ZJH-014 更新为部分整改，新增 ZJH-018/019，未经批准不修正。
- 精确回退范围：
  - 仓库内只回退四份文档中的 ZJH-TEST-013、ZJH-018/019、ZJH-014状态补充和第14条本次量化调整；
  - 仓库外证据目录可独立弃用；
  - 不涉及任何产品代码、自动测试或此前修正的回退。

## 记录 ZJH-FIX-012：外部数据源前端接入与统一助手问数闭环修正

- 日期：2026-07-29。
- 用户批准：执行一个封闭批次 ZJH-019 + ZJH-018；先修复阻断外部问数的源上下文链，再修复 PostgreSQL 注册成功后的前端误报，不扩展到 ZJH-012、015～017 或其他独立问题。
- 修改原因：
  - ZJH-018 已真实复现为后端注册 200 后，异步事件回调读取失效的 `e.currentTarget` 并把成功显示为失败；
  - ZJH-019 已真实复现为知识库选中外部源后，可见统一助手仍查询 `teaching` 并命中教学确定性统计；
  - 两项直接阻断“页面接入自己的数据库并完成问数”的课程演示主流程，适合在一次固定范围内最小收口。
- 仓库内新增文件：无。
- 仓库内修改文件：

| 修改文件 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|
| `app/core/assistant_context.py` | 助手页面上下文没有 `source` 字段，也没有外部源鉴权。 | 为助手页加入受格式约束的 `source`；服务端通过数据源注册表解析；只有管理员可使用非 `teaching` 源，普通角色和未知/不可访问源统一拒绝且不枚举源列表。 |
| `app/core/assistant_orchestrator.py` | `_nl2sql` 固定使用 `teaching`；外部业务问题可能先被教学确定性指标/工作台路由截获；会话没有数据源锁定。 | 以已验证上下文源设置 `source/current_source`；显式外部源直接进入 NL2SQL；只有教学源继续注入教学词表、表白名单、行范围和教学确定性路由；复用会话时校验源一致，禁止跨源续问。 |
| `app/static/app.js` | 知识库“问数”只改变旧壳手动源变量，统一助手上下文未携带源；活动重放和收藏样例入口同样丢失源。 | 新增 `sourceAssistantContext(source)`，三个可见入口统一把选中源传给统一助手；保留既有页面打开方式，不恢复隐藏旧问数壳。 |
| `app/static/data-access-view.js` | PostgreSQL 异步提交完成后调用 `e.currentTarget.reset()`，此时 `currentTarget` 已为 `null`。 | 在 `await` 前保存 `const form = e.currentTarget`，成功后调用 `form.reset()`，随后恢复端口/SSL 默认值并刷新源列表。 |
| `app/static/index.html` | `app.js` 和 `data-access-view.js` 使用上一批缓存标识。 | 分别更新为 `zjh-019` 和 `zjh-018`，确保真实浏览器加载本批实现。 |
| `tests/test_assistant_context.py` | 没有外部 `source` 上下文的管理员允许、普通角色拒绝和未知源拒绝断言。 | 增加三类服务端上下文权限回归，验证拒绝状态和不枚举边界。 |
| `tests/test_assistant_api.py` | 没有统一助手显式外部源转发、教学确定性路由绕过及会话跨源拒绝测试。 | 增加管理员外部源直达 NL2SQL、源参数转发、学生拒绝和同会话换源冲突回归。 |
| `tests/test_v3_assistant_panel.py` | 静态契约只覆盖旧缓存标识和通用助手接线。 | 增加知识库外部源上下文传递契约，并同步两个静态资源缓存版本。 |
| `tests/test_external_data_access.py` | 只覆盖 PostgreSQL 后端注册和前端入口，未断言异步事件稳定引用。 | 增加 `form` 稳定引用、移除 `e.currentTarget.reset()` 和缓存标识断言。 |
| `PROJECT_ISSUES.md` | ZJH-014 为部分整改，ZJH-018 为回归，ZJH-019 为未整改。 | 保留原始复现证据并追加修正文件、调用链、自动测试和真实浏览器结果；三项更新为实际验证通过，ZJH-013追加页面证据。 |
| `PROJECT_CHANGES.md` | 记录停在 ZJH-TEST-013。 | 新增本条详细修改、测试、风险和精确回退记录。 |
| `PROJECT_PROGRESS.md` | 下一步仍为修复 ZJH-019/018。 | 更新为本封闭批次完成、296 项基线和真实浏览器闭环结果，并记录第14条进程复盘。 |
| `docs/project_feature_catalog.md` | 外部源统一助手和浏览器闭环仍按未通过边界描述。 | 同步管理员外部源服务端鉴权、会话锁源、页面闭环和 296/296 质量基线。 |

- 调用链变化：
  - 外部知识库问数：管理员选择知识库 → `app.js` 构造含 `source` 的助手上下文 → 统一助手 API → `assistant_context.py` 通过注册表和角色重新鉴权 → 编排器锁定会话源 → 外部源直接进入现有 NL2SQL → 现有 Schema/检索/只读执行 → 响应证据记录真实外部源；
  - 教学问数：未指定源或指定 `teaching` → 继续使用既有教学确定性指标、工作台路由、业务词表、表字段权限和行级范围；
  - 跨源会话：已有会话源与当前已验证源不一致 → 在执行前拒绝，不能把一个库的历史上下文带到另一库；
  - PostgreSQL 页面注册：提交事件先保存表单引用 → 后端完成真实注册和扫描 → 使用稳定引用复位 → 刷新数据源列表并显示成功状态。
- 自动验证：
  - 上下文、数据接入、前端静态契约等目标组分别通过；
  - 包含助手 API、上下文、外部接入和前端契约的相关回归 **55/55**，0 失败；
  - 最终全量 **296/296**，0 失败，JUnit 证据为 `D:\NL2SQL2\zjh-018-019-fix-20260729\full.xml`；
  - 首次全量前台命令在 300 秒被工具硬终止；随后改用逐测试进度文件运行，实际在约 334.7 秒完成 296/296，因此前一次只记测试设施中断，不记产品失败。
- 真实浏览器闭环：
  - 使用 8022 隔离服务、独立教学库副本、独立注册表/画像/词表目录、真实只读 PostgreSQL 5433 业务库和每轮全新 Chromium 用户目录；
  - 首次真实表单提交已完成 POST 200、源数 2→3、目标源可见和表单复位；因后续仅修正验收脚本竞态，最终通过轮复用同一隔离环境中此前真实提交的源，报告明确记录 `mode=reused_after_prior_submit`，没有伪造第二次注册；
  - Schema 页面真实可见 6 张表、24 个字段；
  - 从外部知识库进入当前统一助手，请求体携带目标 `source`；真实 DashScope 调用返回 `success`、路由 `nl2sql`、证据源 `browser_pg_fix_20260729`、结果标量 3；
  - 学生数据维护导航隐藏，直接 PostgreSQL 注册为 403；
  - 报告扫描确认没有真实密码值和 `password` 请求字段；最终九项验收全部为 true；
  - 结构化证据和截图位于 `D:\NL2SQL2\zjh-018-019-browser-20260729`。
- 验收脚本与第14条复盘：
  - 新版浏览器曾因缺少 DevTools 允许来源参数立即返回 403，补齐隔离启动参数后继续；
  - 原验收脚本把 DOM 元素对象直接作为等待值、把成功提示和列表刷新写成 OR 条件，并在前端异步初始化完成前切页；这些均按测试设施问题修正，没有修改产品来迎合测试；
  - Codex 沙箱阻止 DashScope 网络时，页面明确返回 `WinError 10013`；隔离服务获批在沙箱外启动后真实模型查询通过，前一轮不记产品失败；
  - `Start-Process`/部分 PowerShell 文件操作和两次获批启动工具调用出现 30 秒至数分钟的结果回传延迟；均立即核对 PID、端口、进度和日志，改为 Python `Popen` 短启动器与精确状态文件，没有无依据盲等，也没有误杀用户其他 Edge/Chrome 进程。
- 安全、影响与边界：
  - 外部源权限判断在服务端完成，前端 `source` 不能扩大权限；
  - 普通教学角色仍固定教学源；外部源不会继承教学表白名单，但仍使用现有只读 SQL 校验和数据源自身 Schema；
  - 密码仍只由环境变量名引用，未写入项目文件、报告或提交；
  - 本批不处理 ZJH-012 的普通角色预期 401/403 控制台噪声，不处理 ZJH-015～017 的结果/关系图问题，也不扩大到生产并发和密钥托管。
- 精确回退范围：
  - 回退 `assistant_context.py` 的 `source` 字段与服务端鉴权；
  - 回退 `assistant_orchestrator.py` 的会话锁源、外部源直达 NL2SQL 和源转发，同时保留此前教学问数修正；
  - 回退 `app.js` 的 `sourceAssistantContext` 及三个入口传参；
  - 回退 `data-access-view.js` 的稳定表单引用，并恢复 `index.html` 两个缓存标识；
  - 回退四个测试文件中仅属于 ZJH-018/019 的新增断言；
  - 回退四份文档中仅属于 ZJH-FIX-012 和 2026-07-29 验证的增量；
  - 不得回退 ZJH-FIX-011 的 PostgreSQL 后端注册、密钥外置、外部 Schema 修正，也不得回退此前 ZJH-001～013 的独立修改。

## 记录 ZJH-FIX-013：ZJH-016 server 关系图兼容最小修正

- 日期：2026-07-29。
- 用户批准：明确执行“ZJH-016 server 关系图兼容最小修正”；不处理 ZJH-012、015、017、009、集合查询或 Judge，不修改 BIRD 数据库和 server 容器配置。
- 修改原因：
  - 修正前 BIRD `european_football_2` 实际只有7张表、31条外键定义，但关系图构建为8节点、33条关系；
  - SQLite/SQLAlchemy 返回的 `country` 与真实表 `Country` 大小写不一致，旧实现直接创建新节点；
  - Schema画像中的 `Match.league_id -> League.None`、`Match.country_id -> Country.None` 会作为可渲染关系进入 `self.fks`；
  - 问题已有真实复现和源码证据，修正范围可一次固定完成，无需继续拆分审查子阶段。
- 仓库内新增文件：无。
- 仓库内修改文件：

| 修改文件 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|
| `app/core/retrieval/graph.py` | 遍历表时逐个建节点；外键目标表直接使用 inspector 原值；只检查目标表非空；手工关系无条件建表节点并加入边。 | 先以真实表清单建立节点；新增唯一大小写映射；按 inspector 真实列校验两端表/列；无效/不唯一关系告警后跳过；外键和手工关系复用同一添加路径并去除正反向重复关系。 |
| `tests/test_retrieval_adaptive.py` | 只有授权桥接图测试，没有覆盖实际数据库常见的大小写差异、空目标列或画像无效关系。 | 新增2条用例：一条同时覆盖 `country → Country`、空/缺失表列过滤和正常外键保留；一条覆盖合法手工关系大小写映射及与 inspector 外键去重。 |
| `PROJECT_ISSUES.md` | ZJH-016 为未整改；ZJH-017只有两题证据；没有记录题1028的年份证据偏差。 | 保留修正前证据，追加实现、测试和BIRD结果；ZJH-016更新为实际验证通过；ZJH-017补记1078；只登记新问题ZJH-020。 |
| `PROJECT_CHANGES.md` | 修改记录停在 ZJH-FIX-012。 | 新增本条文件级修改、调用链、验证、风险和精确回退记录，并补齐顶部FIX-012/013索引。 |
| `PROJECT_PROGRESS.md` | 下一步仍把ZJH-016列为候选，自动测试基线为296项。 | 记录本封闭批次、BIRD四路/固定题边界、第14条执行和298项新基线，更新后续候选。 |
| `docs/project_feature_catalog.md` | 只说明授权关系图补JOIN，未说明外部方言关系名/列的规范化边界；质量基线为296项。 | 同步关系图只使用真实表/列的实现事实、BIRD实际验证、298项基线和仍未解决的ZJH-017/020边界。 |

- 调用链变化：
  - Schema linking构建 → SQLAlchemy inspector返回真实表清单 → 图只创建这些真实表节点；
  - inspector外键或Schema画像手工关系 → 两端表名按“精确匹配优先、唯一大小写匹配次之”解析 → 两端列按同一原则对真实列清单解析；
  - 任一表/列无法唯一解析 → 写结构化降级告警并跳过，不进入图边、`self.fks`、JOIN文本或外键保留列集合；
  - 合法关系 → 以真实表/列名称加入；已存在正向或反向等价关系不重复记录；
  - 后续 PageRank、授权子图、桥接表选择和JOIN渲染接口未修改。
- 自动验证：
  - 新增回归 2/2通过；
  - 其余5条关系图/授权检索用例分别隔离通过；
  - 首次整文件命令已显示7个用例到100%，但退出阶段超过120秒硬超时，未将其单独记为完整通过；随后新增和既有用例分别隔离验证，最终全量测试提供正式结论；
  - 最终全量 **298/298**、0失败、耗时348.3秒；JUnit 为 `D:\NL2SQL2\zjh-016-fix-20260729\full.xml`，进度为同目录 `full-progress.json`。
- BIRD实际运行：
  - 使用当前工作区代码和原验收598MB只读数据库；测试配置、脚本、索引签名和结果只位于 `D:\NL2SQL2\zjh-016-fix-20260729`；
  - 图结构为7个真实节点、31条有效关系；无小写虚假 `country` 节点，无 `None` 或缺失字段关系；
  - 非沙箱隔离运行中代表问题真实使用 Milvus、Elasticsearch、PostgreSQL/pgvector和graph四路；
  - 固定15题单次真实模型回归为15/15可执行、15/15使用四路、严格EX 10/15；达到课程预设12可执行/9严格EX阈值，但未达到历史原始14可执行/11严格EX计数，未写成整体质量无回归；
  - 1145、1040、1107为历史已知失败；1078额外投影 `birthday` 补充到ZJH-017；1028忽略明确年份证据另登记ZJH-020。两项只登记，没有自动重试或顺带修正。
- 第14条执行：
  - 第一轮四路命令在沙箱中因DashScope `WinError 10013`和检索签名目录权限失败；关系图仍报告7/31，该轮只记环境失败；
  - 获批非沙箱后把签名目录指向仓库外，四路在65.6秒完成；未输出或记录 `.env` 密钥；
  - 固定15题使用PID `31052`、逐题进度和日志，约一分钟正常完成并回收；
  - 全量测试使用PID `26960`、逐用例JSON进度、日志和硬超时审查，298项完成后正常回收；
  - `Start-Process`工具调用本身持续等待，但实际子进程PID、CPU和进度持续变化，因此没有重复启动或误判卡死。
- 潜在影响与明确边界：
  - 不同大小写但唯一对应的数据库标识将统一使用真实Schema名称，改善外部SQLite/PostgreSQL等方言兼容；
  - 若同一Schema确实存在仅大小写不同的多个表/列，非精确引用会因不唯一而安全跳过，不猜测目标；
  - inspector无法读取列清单时，该表关系会降级跳过；查询仍可由内容检索和完整Schema路径继续处理；
  - 本批不修改检索RRF权重、模型提示词、数据权限、外部源会话锁、数据库数据和四个server组件；
  - ZJH-017和ZJH-020仍为独立未整改问题，ZJH-016通过不代表BIRD全部题或完整500题已通过。
- 精确回退范围：
  - 仅回退 `app/core/retrieval/graph.py::RelationGraph.build` 中本批加入的真实表节点初始化、名称解析、列缓存、统一关系添加和降级告警；
  - 仅回退 `tests/test_retrieval_adaptive.py` 中本批新增的两条关系图兼容用例及模块导入；
  - 仅回退四份文档中ZJH-FIX-013、ZJH-016修正补充、ZJH-017补充和ZJH-020登记的增量；
  - 不得回退此前ZJH-FIX-001～012、外部数据源注册/Schema/助手锁源或用户其他工作区修改。

## 记录 ZJH-DOC-004：项目收口审查与交付边界确认

- 日期：2026-07-29。
- 用户批准：同意进入一次性项目收口阶段，要求严格依据事实和14条工作原则，不迎合推测；固定范围是不修改产品实现，完成记录同步和交付边界确认。
- 本次仓库修改：
  - `PROJECT_PROGRESS.md`：更新日期和当前结果；修正ZJH-016状态矛盾；补记ZJH-020；用一次性交付边界替换继续选择修复问题的旧“下一步”；
  - `PROJECT_CHANGES.md`：更新日期、完成项和总览；新增本条文档审查记录；
  - `PROJECT_ISSUES.md`：只更新顶部日期，问题正文和状态不变。
- 只读审查事实：
  - 当前分支 `zjh-dev`，HEAD 与 `origin/zjh-dev` 均为 `e6d4485a5f8611ce1374cfb3ac68f470623f5235`；
  - 工作区有29个已跟踪修改文件和4个未跟踪文件；产品、测试和配置变化均可对应ZJH-FIX-001～013或已批准验收，没有发现来源无法解释的新产品功能；
  - `data/teaching.db` 基线为82,075,648字节、17张表、schema version 0，当前为167,157,760字节、68张表、schema version 3005；内存只读对比确认新增4个作业和10个提交对应 `teaching_migrations.py` 的900001～900004演示种子，应用启动源码会调用 `ensure_mvp_schema()`，当前另有助手会话31、轮次71、动作草稿39等运行态数据；本次没有重新执行基线迁移，仍不据此推断当前二进制应整体提交；
  - `.env`与私有演示账号文件受`.gitignore`保护；本次未读取、输出或记录其中的值。
- 测试边界：本次没有修改产品或测试，也没有重复运行全量、模型、浏览器或server测试；正式结论继续引用当前代码最近一次298/298及既有真实验收证据。
- 交付决定：
  - 已批准产品、配置、提示词、测试和四份项目文档列为交付候选；
  - `data/teaching.db`列为单独待定，不自动提交、清理或回退；
  - `.env`、私有账号、仓库外数据库、日志、截图、PID和索引缓存明确排除。
- 潜在影响：仅改变记录文本和交付说明，不改变运行、API、页面、权限、查询、测试或数据库行为。
- 精确回退范围：只回退三份项目记录中ZJH-DOC-004、日期、ZJH-016/ZJH-020同步和“项目收口与交付边界”段落；不得回退任何产品、测试、配置、数据库或此前记录。

## 记录 ZJH-DOC-005：项目文件分类、职责和测试覆盖目录

- 日期：2026-07-29。
- 用户批准：审查项目文件，按功能和模块分类，说明测试文件与运行功能的边界、测试范围、功能文件作用、文件关系、修改状态和对应问题，并输出为一个条理清晰的文件。
- 新增文件：`docs/project_file_catalog.md`。
- 内容范围：Git跟踪文件、当前明确的4个未跟踪项目文件和目录自身；排除`.env`、虚拟环境、缓存、日志、运行上传和仓库外验收证据。
- 审查方式：Git清单与状态、Python AST顶层类/函数、API/前端/配置/数据/脚本、37个测试模块与238个声明测试函数，以及ZJH-FIX-001～013记录交叉核对。
- 实际修改：只新增目录文档，并在本文件和`PROJECT_PROGRESS.md`登记完成状态；没有向177个既有源码文件批量插入注释，没有修改产品、测试、配置或数据库。
- 验证边界：文件级映射用于维护和交付导航，不替代逐行代码评审、实际运行测试或`docs/project_feature_catalog.md`的功能状态。
- 精确回退范围：删除`docs/project_file_catalog.md`，并删除本文件ZJH-DOC-005总览/记录及`PROJECT_PROGRESS.md`对应一行；不得回退其他文件。

## 记录 ZJH-DOC-006：最终测试向导与可复现结果工作簿

- 日期：2026-07-29。
- 用户批准：参考用户提供的 `testCase.xlsx` 模板，输出可从项目启动一直执行到测试收口的测试向导，并生成能记录和复现结果的测试用例表；范围须覆盖功能、智能问数、SQL安全、内置与导入数据库、server检索和BIRD，并补齐遗漏。
- 新增项目文件：
  - `docs/project_test_guide.md`：11阶段人工测试向导，包含测试目的、模块、资料、命令、步骤、预期、证据、停止条件、问题登记和事实边界；
  - `docs/project_file_catalog.md`：仅同步新增向导的文件职责和当前35项工作区变更映射；
  - `PROJECT_CHANGES.md`、`PROJECT_PROGRESS.md`：增加本次任务总览和完成记录。
- 仓库外最终成果：
  - `D:\NL2SQL2\outputs\final-test-guide-20260729\NL2SQL2项目测试用例与结果表.xlsx`；
  - 工作簿基于模板导入后重建，保留原八列主用例结构和浅蓝表头风格，没有覆盖用户原始模板；
  - 五个工作表为“测试用例”“执行记录”“测试概览”“环境记录”“BIRD结果”。
- 用例范围：
  - 共96条：P0 66条、P1 29条、P2 1条；
  - 覆盖环境启动、确定性自动回归、管理员/教务/学院/教师/学生/辅导员、教学业务、内置库NL2SQL与追问、SQL只读和行级安全、外部SQLite、业务PostgreSQL、server四路、BIRD固定15题、浏览器并发/超时/响应式和测试收口；
  - 所有实际结果初始为“未执行”，没有把历史298/298或历史BIRD结果写成本次结果。
- 可复现设计：
  - 每条用例含唯一ID、前置条件、测试资料、操作步骤和可判定预期；
  - 执行记录支持Run ID、状态、实际观察、证据路径、问题编号、测试者、时间、环境和优先级；
  - 主用例最新状态和概览由公式汇总，状态列提供下拉和条件格式；
  - BIRD页固定15个题号、数据库、难度和问题，记录清单SHA-256及课程内部可执行/EX门槛；向导明确这不是官方完整Dev成绩。
- 安全与事实边界：
  - 没有写入真实API Key、令牌、数据库密码或私有账号密码；
  - README公开五类账号与V3辅导员第六角色的差异已说明；缺少辅导员账号时应记阻塞；
  - 大模型结果按SQL、结果集、范围和错误码复现，不要求文字逐字一致；
  - SQL安全须结合自动测试，BIRD严格EX须自动比较结果集，Docker健康不等于四路检索通过。
- 验证结果：
  - 模板原始结构和样式已先导入、检查并渲染；
  - 最终96条用例全部写入，五张工作表逐张渲染检查；
  - 概览显示用例总数96、P0 66、P1 29、P2 1、未执行96；
  - 公式错误扫描无 `#REF!`、`#DIV/0!`、`#VALUE!`、`#NAME?` 或 `#N/A`；
  - 修正了跨表通配符优先级统计在当前表格引擎中为0的问题，并修正“未达到”被错误着色为绿色的问题；
  - 本次没有运行项目、pytest、模型、浏览器、外部数据库、server或BIRD测试，因此工作簿不是测试通过报告。
- 未修改范围：没有修改 `app/`、`tests/`、`data/`、`prompts/`、`.env`、`data_sources.yaml` 或用户原始 `testCase.xlsx`；没有启动服务或改变 `teaching.db`。
- 潜在影响：仅增加测试交付文档和仓库外空白结果表，不改变产品行为。96条属于完整手工验收集，不要求一次演示全部执行；应先执行P0，再按范围执行P1/P2。
- 精确回退范围：
  - 删除 `docs/project_test_guide.md`；
  - 删除仓库外最终工作簿；
  - 删除本文件ZJH-DOC-006总览/记录、`PROJECT_PROGRESS.md`对应进度行及`docs/project_file_catalog.md`对向导的增量；
  - 不得回退任何产品、测试、配置、数据库或此前项目记录。

## 记录 ZJH-FIX-014：ZJH-021 PostgreSQL 只读表预览最小修正

- 日期：2026-07-29～2026-07-30。
- 用户批准：在已完成修正前审查和真实复现的基础上，执行“浏览外部 PostgreSQL 表数据”的最小修正，并及时更新问题、修改和进度记录；不得影响现有 SQLite 维护、注册、Schema、NL2SQL、server 或其他业务功能。
- 修改原因与修正前证据：
  - 管理员已经能够注册、扫描并问询外部 PostgreSQL，但数据维护页读取 `book` 表时返回 HTTP 400；
  - 同一业务 PostgreSQL 已扫描6表24列，数据库直接查询 `book` 为10行，说明不是连接或空库问题；
  - 调用链最终统一落入只支持 `sqlite:///`、`sqlite3.connect` 和 `PRAGMA table_info` 的 `table_rows`，非 SQLite 被主动拒绝；
  - 外部 SQLite 同接口返回 HTTP 200、`total=10`，用于确认原功能和问题边界。
- 仓库内新增文件：无。
- 仓库内修改文件：

| 修改文件 | 原逻辑或被替换内容 | 新增/替换内容与用途 |
|---|---|---|
| `app/core/data_access.py` | `table_rows` 无条件解析 SQLite 路径，再用 `sqlite3` 查询；PostgreSQL 必然在路径解析处返回400。 | 新增 `_postgresql_table_rows`；仅当 `source.dialect == "postgresql"` 时复用现有 SQLAlchemy 引擎，先从 inspector 表清单验证表名，再用方言引用标识符执行 `COUNT(*)` 和参数化分页 `SELECT`，保持 `columns/rows/total/limit/offset` 响应契约；异常继续脱敏。SQLite 分支内容未改。 |
| `app/static/data-access-view.js` | 所有数据源都按可编辑行渲染输入框、操作列、保存和删除；新增按钮只按 `writable` 置灰，PostgreSQL 页面仍呈现维护语义。 | 按当前源 `dialect` 识别 PostgreSQL 浏览模式：单元格改为纯文本，不生成操作列、输入框、保存或删除按钮；新增记录入口同时设置 `hidden` 与 `display:none`。SQLite 继续走原有可编辑分支。 |
| `app/static/index.html` | 数据维护脚本沿用旧缓存标识。 | 把 `/data-access-view.js` 缓存标识更新为 `zjh-021`，确保浏览器加载本次最小前端修正；未改变页面结构。 |
| `tests/test_external_data_access.py` | 已覆盖 PostgreSQL 注册、密钥外置和 Schema，但没有表预览、页面只读呈现、权限与 SQLite 对照回归。 | 新增 PostgreSQL SQLAlchemy 预览分页及不存在表用例、SQLite 预览响应不变用例、管理员权限用例和 PostgreSQL 禁止写入用例；静态契约检查增加纯文本渲染和显式隐藏新增入口。 |
| `PROJECT_ISSUES.md` | ZJH-021 只有修正前复现和获批范围，状态为未整改。 | 保留修正前证据，追加实际文件、自动/API/Edge证据、首轮视觉复核、边界并更新为实际验证通过。 |
| `PROJECT_CHANGES.md` | 修改总览停在 ZJH-FIX-013。 | 新增本记录及顶部索引，说明文件、调用链、验证、影响和精确回退。 |
| `PROJECT_PROGRESS.md` | 全量基线仍为298项，外部 PostgreSQL 主闭环没有表数据浏览结论。 | 新增本批完成情况，把自动基线更新为302项，并同步当前阻塞、代码冻结例外和交付证据。 |

- 调用链变化：
  - 管理员数据维护页选择 PostgreSQL 表 → `api.tableRows` → 既有管理员路由 `GET /api/data-access/sources/{source}/tables/{table}/rows` → `table_rows`；
  - `table_rows` 读取已注册数据源；SQLite 继续进入原 `sqlite3` 分支，PostgreSQL 才进入 `_postgresql_table_rows`；
  - PostgreSQL 分支复用 `ds_cache.get_engine(source)`，该引擎在既有实现中设置 `default_transaction_read_only=on`；
  - inspector 真实表清单作为表名白名单 → SQLAlchemy 方言引用合法表名 → 同一只读连接执行总数和分页读取 → 返回与前端既有结构相同的数据；
  - 前端根据源方言选择只读或可编辑渲染，不依赖客户端判断扩大后端权限；PostgreSQL 写接口仍由原服务端逻辑拒绝。
- 自动验证：
  - `tests/test_external_data_access.py` 专项 JUnit：12/12，0失败、0错误、0跳过，耗时12.914秒；
  - 最终全量 JUnit：302/302，0失败、0错误、0跳过，测试本体耗时355.063秒；
  - `node --check app/static/data-access-view.js` 与 `git diff --check` 通过；
  - 专项和全量两次 pytest 均在 JUnit 已完整写出后没有自动退出；按第14条验证测试数和最后用例后只终止相应精确 Python/PowerShell PID。该退出阶段挂起作为测试设施事实保留，不能写成命令正常退出，也不改变 JUnit 的逐用例结果。
- 真实 API 验证：
  - 使用仓库外隔离注册表和测试库，不向主 `data_sources.yaml` 写入验收源；
  - PostgreSQL 注册扫描6表24列；预览 `book` 返回200、总数10，`limit=3/offset=1` 返回3行且字段正确；
  - SQLite 对照返回200、总数10、本页3行；
  - 学生预览 PostgreSQL 返回403，不存在表返回400且不含密码；
  - PostgreSQL POST/PATCH/DELETE 均返回400；数据库直接核对总数10且 `default_transaction_read_only=on`；
  - 固定九项检查9/9通过，结构化结果位于仓库外 `D:\NL2SQL2\zjh-021-preview-fix-20260729`。
- 真实 Edge 验证：
  - PostgreSQL `book` 页面显示10行，字段为 `id/title/category_id/branch_id/published_year`，首行为 `101 / Python编程基础 / 1 / 1 / 2024`；
  - PostgreSQL 输入框、保存、删除和新增记录入口均不可见；SQLite 对照仍有操作列、50个字段输入框和可见新增按钮；
  - 两个真实取数网络请求均为200，浏览器控制台错误为0；
  - 首轮浏览器逻辑检查虽得到 `hidden=true`，截图仍看到被CSS覆盖的灰色新增按钮；没有把该轮记为最终通过。只补充显式 `display:none` 后重新执行完整矩阵，最终计算样式为 `none` 且截图确认按钮消失；
  - Edge、隔离服务和测试进程结束后按精确 PID 清理，没有终止用户其他浏览器或项目服务。
- 数据与密钥边界：
  - 没有修改 `data/teaching.db`；本批结束时该文件 SHA-256 为 `1A37F6E70C663BED8C15F0A28A035D2A8757ECCA61B33D4203251AE5B562308C`，仅用于确认测试前后未被本批写入；
  - 没有将 PostgreSQL 密码、`.env` 值、令牌或私有账号写入项目和记录；仓库外结构化结果也不包含真实密码；
  - 没有新增迁移、业务表、演示数据或运行配置。
- 潜在影响与明确边界：
  - PostgreSQL 大表预览仍按现有接口限制最多200行；`COUNT(*)` 在大表上可能比无计数读取慢，但本课程范围不扩展为估算计数、游标分页或性能专项；
  - 当前只处理默认可见表名，不新增 PostgreSQL schema 选择、搜索、排序、导出或编辑；
  - 如果字段包含非 JSON 原生类型，仍沿用 FastAPI/现有响应序列化行为；本次真实业务表和自动用例已通过，不据此宣称所有 PostgreSQL 类型都已验证；
  - ZJH-012、015、017、020及其他既有问题均未顺带修改。
- 精确回退范围：
  - 仅删除 `app/core/data_access.py` 中 `_postgresql_table_rows`，并删除 `table_rows` 开头的 PostgreSQL 方言分支；必须保留ZJH-FIX-011的注册、扫描、密钥外置和错误脱敏代码；
  - 仅回退 `app/static/data-access-view.js` 中 PostgreSQL 浏览模式判断、纯文本行和新增按钮隐藏；必须保留ZJH-FIX-011/012的PostgreSQL表单与稳定引用；
  - 把 `app/static/index.html` 的数据维护脚本缓存标识从 `zjh-021` 恢复为本批前值，不回退其他脚本标识或页面入口；
  - 仅删除 `tests/test_external_data_access.py` 中本批新增的四类预览/权限/写入断言和对应静态断言；
  - 仅回退三份记录文件中ZJH-021修正后、ZJH-FIX-014和302项基线增量；
  - 不得整体回退上述文件，不得覆盖或影响ZJH-FIX-001～013及用户其他未提交修改。

## 记录 ZJH-FIX-015：统一助手结果反馈与安全 Few-shot 闭环修正

- 日期：2026-07-31。
- 用户批准：在确认旧问数反馈后端和治理逻辑仍存在、但 V3 可见统一助手未迁移点赞/点踩且未使用已审核 Few-shot 后，按“统一助手原生反馈 + 服务端安全 Few-shot”方案修正。
- 修改原因：
  - `bda37ff` 引入统一助手时隐藏了包含“结果正确/反馈错误”的旧问数壳，新组件没有反馈入口；
  - 旧 `/api/ask` 由浏览器传入 Few-shot，统一助手 `_nl2sql` 没有加载标准样例，导致管理员采纳后不影响当前可见问数；
  - V3 契约已规划 `/api/assistant/turns/{turn_id}/feedback`，但运行时此前没有实现。
- 主要修改：
  - `app/core/assistant_sessions.py` 增加当前用户、当前工作身份拥有的单轮回答读取；
  - 新增 `app/core/assistant_feedback.py`，只允许对本人已完成的 NL2SQL 回答提交反馈，问题和数据源从服务端轮次恢复；
  - `app/core/feedback.py` 按回答轮次、用户和角色绑定幂等保存反馈；同轮修改反馈复用原 ID；
  - `app/api/assistant.py` 新增 `POST/DELETE /api/assistant/turns/{turn_id}/feedback`，支持提交、更新和取消；
  - `app/static/assistant-panel.js/css` 在包含 SQL 的成功/降级 NL2SQL 卡片显示“结果正确/需要改进”，负向反馈要求类别和说明；
  - `app/static/api.js` 接入轮次反馈接口，`index.html` 更新相关静态资源缓存标识；
  - `app/core/examples.py` 增加启用样例的词项相似度选择，并在注入前复用 SQL 安全校验过滤非只读、未授权表和受限输出字段，最多保留5条；
  - `app/core/assistant_orchestrator.py` 按当前源和授权范围服务端加载 Few-shot；选择失败时记录异常并不带样例继续问数；
  - 新增 `tests/test_assistant_feedback.py`，并扩展统一助手 API 和前端静态契约测试；
  - 同步 `docs/project_feature_catalog.md` 的能力、API、限制、测试和变更记录。
- 权限与安全边界：
  - 跨用户或跨工作身份对回答提交反馈按不存在处理；
  - 客户端不能指定反馈问题或数据源，反馈 SQL 只作为人工待审候选且必须为单条只读 SELECT；
  - 管理员采纳仍复用现有治理动作，样例不会自动执行；
  - Few-shot 只改变模型提示，最终生成 SQL 仍完整经过既有表、字段、行级范围、危险函数、只读和执行限制；
  - 不恢复隐藏旧问数壳，不删除或修改旧 `/api/ask`、`/api/feedback` 和 `/api/examples`。
- 自动验证：
  - Python `compileall`、`node --check app/static/assistant-panel.js` 和 `node --check app/static/api.js` 通过；
  - 反馈、统一助手 API 和前端组件定向 41/41 通过；
  - 安全、角色权限、旧问数、治理、动作草稿、回答证据、助手上下文和会话相关回归 106/106 通过；
  - 当前仓库排除临时测试依赖后全量309/309通过，耗时214.55秒；
  - 两组测试只有2条既有弃用警告，无新增失败。
- 真实浏览器验证：
  - 管理员在当前可见“智能问数”页提问“各学院在读学生人数是多少？”，首轮外部模型请求达到既有超时提示，原问题有界重试后成功返回6行数据库结果；
  - 成功回答显示“结果正确/需要改进”反馈入口，点击“结果正确”后页面提示已进入标准问法复核队列；
  - “质量治理”待审队列显示同一问题、`confirmed_example` 分类和“用例沉淀”，管理员“采纳为标准问法”成功；
  - “问数知识库”随后显示该问题及 SQL，且“用于 few-shot”处于启用状态，证明页面反馈、治理复核和样例沉淀链路贯通；
  - 验收前已备份教学库、样例和治理文件；验收后停止服务并原样恢复，删除本次新建反馈目录，再重启当前代码，未把验收反馈、审核项或重复样例留在演示数据中。
- 数据与运行边界：
  - 自动测试使用临时数据库和临时反馈/治理/样例目录，不修改工作区演示业务数据；
  - 实际页面点击会按产品预期写入 `data/feedback`、治理队列和管理员采纳后的 `data/examples`，浏览器验收后须恢复测试产生的记录；
  - 本批没有修改检索后端配置、SQL Prompt、教学业务表结构或账号权限。
- 潜在影响：
  - Few-shot 会影响模型生成倾向，因此只选择同源、启用、词项相关且授权安全的少量样例；无相关样例时保持原行为；
  - 词项相似度不是语义向量，表达完全不同但含义相同的问法可能不命中样例，这是当前明确限制；
  - 同一轮重复反馈更新现有待审项，避免重复积压；再次点击当前选项可取消反馈并清理关联治理项；已经由管理员完成处理后再修改反馈的并发场景不作为本次演示主链。
- 精确回退范围：
  - 删除 `app/core/assistant_feedback.py` 和 `tests/test_assistant_feedback.py`；
  - 回退 `assistant_sessions.py` 的单轮读取、`feedback.py` 的轮次幂等保存、`assistant.py` 的反馈路由；
  - 回退 `examples.py` 的样例选择和 `assistant_orchestrator.py` 的 Few-shot 参数传递；
  - 回退统一助手 JS/CSS/API 客户端反馈区域及本批缓存标识；
  - 删除本记录和功能目录对应增量；不得整体回退这些文件或影响既有治理、旧问数及其他业务修正。
