-# NL2SQL 智能问数系统 — 回归测试 & 探索测试报告

**测试日期**: 2026-06-02  
**测试环境**: Windows 11, http://127.0.0.1:8000  
**数据库**: SQLite (6表: categories, users, products, orders, reviews, addresses)

---

## 第一部分: 回归测试 (7组)

### 测试1: 每个类目销量第一的商品分别是哪个?

| 项目 | 内容 |
|------|------|
| 输入 | 每个类目销量第一的商品分别是哪个? |
| 生成SQL | 使用 p.name 和 c.name 别名, ROW_NUMBER() OVER 窗口函数, 正确 JOIN 三表 |
| 结果 | 返回7行, 每个类目1条记录 |
| 状态 | ✅ 通过 |

> 之前Bug "no such column: p.name" 已修复。

---

### 测试2: 差评最多的五个商品?

| 项目 | 内容 |
|------|------|
| 输入 | 差评最多的五个商品? |
| 生成SQL | SELECT p.name AS product_name, COUNT(*) AS negative_review_count FROM reviews r JOIN products p ON r.product_id = p.id WHERE r.rating IN (1,2) GROUP BY p.id ORDER BY COUNT(*) DESC LIMIT 5 |
| 结果 | 返回5行, 表头双行显示: product_name/p.name, negative_review_count/COUNT(*) |
| 状态 | ✅ 通过 |

> 之前Bug: 别名无源字段 → 已修复, 双行显示正常。

---

### 测试3: 分别统计2024/2025/2026三年的订单总金额

| 项目 | 内容 |
|------|------|
| 输入 | 分别统计2024/2025/2026三年的订单总金额 |
| 生成SQL | 包含 WHERE o.status = paid AND SUBSTR(o.created_at,1,4) IN (2024,2025,2026) |
| 结果 | 2024: 20711157, 2025: 33040268, 2026: 14313965 |
| 状态 | ✅ 通过 |

> 之前Bug: 缺少 status=paid → 已修复。

---

### 测试4: 去年一共多少订单

| 项目 | 内容 |
|------|------|
| 输入 | 去年一共多少订单 |
| 生成SQL | SELECT COUNT(*) AS order_count FROM orders WHERE status = paid AND created_at LIKE 2025% LIMIT 1 |
| 结果 | 9635 个订单 (2025年) |
| 状态 | ✅ 通过 |

> 之前Bug: 2026年当前, 去年错误输出2024 → 已修复, 正确识别2025。

---

### 测试5: 列出最早注册的1000个用户

| 项目 | 内容 |
|------|------|
| 输入 | 列出最早注册的1000个用户 |
| 生成SQL | ... ORDER BY registered_at ASC LIMIT 200 |
| 结果 | 返回200行, 前端显示 "⚠️ 结果已截断" 提示 |
| 状态 | ✅ 通过 |

> 之前Bug: 未显示截断信息 → 已修复。

---

### 测试6: 最贵的用户是谁?

| 项目 | 内容 |
|------|------|
| 输入 | 最贵的用户是谁? |
| 生成SQL | SELECT u.name, SUM(o.amount) FROM users u JOIN orders o ... GROUP BY u.id ORDER BY SUM(o.amount) DESC LIMIT 1 |
| 结果 | 罗伟峰, 消费总额 208653 元 |
| 状态 | ✅ 通过 |

> 之前Bug: 输出"无法回答" → 已修复, 正确理解为消费总额最高。

---

### 测试7: 多轮叠加追问

| 步骤 | 输入 | 结果 | 状态 |
|------|------|------|------|
| 1 | 2025订单总金额 | 33,040,268 | ✅ 通过 |
| 2 | 按城市拆分 | 返回30个城市 | ✅ 通过 |
| 3 | 只看广东的 | SQL含 a.province=广东 但结果含石家庄/南昌等非广东城市 | ❌ 失败 |
| 4 | 再按月份 | 含上海/北京等非广东城市 | ❌ 失败 |

**失败原因**: 步骤3/4 虽然 SQL 正确添加了 a.province=广东 条件, 但 GROUP BY 按 users.city (注册城市) 分组, 导致有广东省收货地址的用户的所有注册城市都出现在结果中。

---

## 第二部分: 探索测试 (8组)

### 测试8: 安全测试 — 注入非SELECT语句

| 项目 | 内容 |
|------|------|
| 输入 | 删除所有订单 |
| 结果 | 系统拒绝执行, 返回 "无法回答: 仅支持 SELECT 查询, 禁止执行 DELETE/UPDATE/INSERT 等修改操作" |
| 状态 | ✅ 通过 |

---

### 测试9: 字段不存在测试

| 项目 | 内容 |
|------|------|
| 输入 | 谁是体重最重的用户? |
| 结果 | 正确返回 "无法回答: users 表中没有体重(weight)字段" |
| 状态 | ✅ 通过 |

---

### 测试10: 模糊语义理解

| 项目 | 内容 |
|------|------|
| 输入 | 平均评分最高的5个商品 (至少10条评价) |
| 生成SQL | ... GROUP BY p.id HAVING COUNT(*) >= 10 ORDER BY AVG(r.rating) DESC LIMIT 5 |
| 结果 | 正确使用 HAVING 过滤评价数, 返回5个商品 |
| 状态 | ✅ 通过 |

---

### 测试11: 大结果集截断

| 项目 | 内容 |
|------|------|
| 输入 | 列出所有订单 |
| 生成SQL | ... LIMIT 20 |
| 结果 | 自动加 LIMIT 20, 返回20行, 但**未显示截断提示** |
| 状态 | ⚠️ 部分通过 |

> LIMIT 自动添加正确, 但无 "⚠️ 结果已截断" 标记 (与测试5行为不一致)。

---

### 测试12: 多表JOIN

| 项目 | 内容 |
|------|------|
| 输入 | 广东省有多少用户设置了默认地址? |
| 生成SQL | SELECT COUNT(DISTINCT user_id) FROM addresses WHERE province = 广东 AND is_default = 1 LIMIT 100 |
| 结果 | 113 个用户 |
| 状态 | ✅ 通过 |

---

### 测试13: 空输入测试

| 项目 | 内容 |
|------|------|
| 输入 | (空) |
| 结果 | 静默复用上一轮查询结果, 无任何提示 |
| 状态 | ❌ 失败 |

> 应给出 "请输入问题" 等合理提示。

---

### 测试14: 特殊字符输入

| 项目 | 内容 |
|------|------|
| 输入 | 用户 OR 1=1 |
| 结果 | 静默处理为空输入, 复用上次查询结果, 未崩溃 |
| 状态 | ⚠️ 部分通过 |

> 未崩溃, 但无任何错误提示或安全警告。

---

### 测试15: 聚合与分组

| 项目 | 内容 |
|------|------|
| 输入 | 2025年每个月的订单数量, 按月份排序 |
| 生成SQL | SELECT strftime("%Y-%m", o.created_at) AS order_month, COUNT(*) ... GROUP BY order_month ORDER BY order_month LIMIT 12 |
| 结果 | 返回12个月数据, 按月正确排序 |
| 状态 | ✅ 通过 |

---

## 总结

### 通过率统计

| 分类 | 总数 | 通过 | 部分通过 | 失败 | 严格通过率 |
|------|------|------|----------|------|------------|
| 回归测试 | 7 | 5 | 0 | 2 | 71.4% |
| 探索测试 | 8 | 5 | 2 | 1 | 62.5% |
| **合计** | **15** | **10** | **2** | **3** | **66.7%** |

### 已修复的Bug (回归验证通过)

1. ✅ "no such column: p.name" — 正确使用表别名
2. ✅ 别名无源字段 — 表头双行显示
3. ✅ 三年统计缺 status=paid — SQL正确包含
4. ✅ "去年"计算错误 — 2026年识别去年为2025
5. ✅ 截断无提示 — 显示 "⚠️ 结果已截断"
6. ✅ "最贵的用户" 返回"无法回答" — 正确理解为消费总额

### 仍存在的问题

| 编号 | 严重度 | 描述 |
|------|--------|------|
| P1 | 🔴 高 | **多轮追问-广东省筛选失效**: SQL 含 province=广东 但 GROUP BY users.city, 结果含非广东城市 |
| P2 | 🟡 中 | **空输入无提示**: 应提示 "请输入问题" 而非静默复用上次结果 |
| P3 | 🟡 中 | **特殊字符无反馈**: SQL注入类输入应提示 "无法理解" 而非静默处理 |
| P4 | 🟢 低 | **截断提示不一致**: LIMIT 20 时无截断标记, LIMIT 200 时有 |

### 新发现的问题

| 编号 | 严重度 | 描述 |
|------|--------|------|
| N1 | 🟡 中 | **省份筛选与城市字段不匹配**: 多轮追问中按省份过滤时, 仍按 users.city 分组, 建议自动切换到 addresses.city |
*（内容由AI生成，仅供参考）*
