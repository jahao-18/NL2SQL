# NL2SQL 核心测试用例

> 目标: 覆盖项目最容易出错、最能暴露问题的链路。  
> 建议优先级: 先跑 P0，再跑 P1，最后跑 P2 边界和压力用例。

---

## 测试记录建议

每条用例建议记录:

| 字段 | 说明 |
|---|---|
| 测试编号 | 如 TC-01 |
| 测试时间 | 执行时间 |
| 数据源 | 实际使用的数据源 |
| 测试问题 / 操作 | 输入的问题或页面操作 |
| 生成 SQL | 系统生成的 SQL |
| 实际结果 | 页面返回结果 |
| 是否通过 | 通过 / 不通过 |
| 问题归因 | 路由 / Schema / 召回 / JOIN / 指标 / SQL 生成 / 执行 / 前端 |
| 修复建议 | 需要补 Profile、Glossary、关系、指标，还是修代码 |

---

## P0: 必须优先测试

| 编号 | 模块 | 测试点 | 操作 / 问题 | 预期结果 | 重点观察 |
|---|---|---|---|---|---|
| TC-01 | 数据源路由 | 自动选库是否正确 | 自动模式下问: `梅西的惯用脚是哪只？` | 路由到足球相关库，而不是电商/学校库 | source 是否正确，SQL 是否查到球员表 |
| TC-02 | 数据源路由 | 多轮追问是否留在原库 | 先问 `梅西的惯用脚是哪只？`，再问 `他的身高是多少？` | 第二问继续使用同一个足球库 | 是否错误切库 |
| TC-03 | 数据源路由 | 换话题是否切库 | 先问足球问题，再问 `加州学校平均 SAT 成绩最高的是哪所？` | 应切到 `california_schools` | 是否被历史上下文带偏 |
| TC-04 | Schema 解析 | quoted 字段名解析 | 进入 `toxicology` 知识库 | 字段数应显示 11，不是 0 | SQLite 反引号字段是否正常 |
| TC-05 | Schema 知识库 | 字段保存不生成版本 | 在 Schema 配置页编辑字段描述并保存 | 右侧发布与版本不新增快照 | 草稿保存和发布是否分离 |
| TC-06 | Schema 发布 | 发布后即时显示版本 | 编辑字段后点 `发布 Profile`，填版本名和说明 | 第三栏立即出现新版本，无需刷新浏览器 | 前端状态是否刷新 |
| TC-07 | Schema 版本 | 回滚版本 | 修改字段描述并发布；再次修改；回滚到旧版本 | 字段内容恢复到旧版本 | 回滚是否恢复 Profile |
| TC-08 | Schema 版本 | 删除历史版本 | 删除一个历史发布版本 | 版本列表立即移除，刷新后仍不存在 | 快照和 meta 是否一起删除 |
| TC-09 | 字段禁用 | 禁用字段是否拦截 | 将某字段标记 `disabled`，再问一个明显会用到该字段的问题 | SQL 不能使用该字段；若使用应被校验拦截 | validator 是否生效 |
| TC-10 | 默认过滤 | 默认口径是否生效 | 在订单表配置 `status='paid'`，问: `销售额是多少？` | SQL 应包含已支付过滤 | 是否漏掉业务默认过滤 |
| TC-11 | 人工关系 | 无外键 JOIN 是否可用 | 手工添加 `orders.user_id = users.id`，问: `各城市用户的订单金额是多少？` | SQL 使用正确 JOIN | 是否乱连表 |
| TC-12 | 指标口径 | 派生指标是否生效 | 配置 `客单价 = 已支付订单金额求和 / 已支付订单数`，问: `今年客单价是多少？` | SQL 应按公式计算 | 是否直接找不存在的“客单价”字段 |
| TC-13 | Schema 召回 | 宽表字段裁剪 | 问 football 库中涉及 Match 宽表的问题 | 上下文只包含相关列，不应整表 100+ 列全塞 | 召回 trace 是否合理 |
| TC-14 | 四路召回 | 中文问法匹配英文字段 | 问: `惯用脚是左脚的球员有多少？` | 应召回 `preferred_foot` 等字段 | query expansion / glossary 是否有效 |
| TC-15 | SQL 安全 | 写操作拦截 | 输入: `删除所有订单数据` 或 `把所有用户状态改成active` | 系统拒绝，不执行 SQL | 是否生成或执行 DELETE / UPDATE |
| TC-16 | SQL 安全 | 多语句注入 | 输入: `查询所有用户; DROP TABLE users;` | 应拒绝或只允许安全 SELECT | 多语句是否拦截 |
| TC-17 | SQL 安全 | LIMIT 控制 | 问: `列出所有订单` | SQL 自动加 LIMIT 或结果受限 | 是否无限返回 |
| TC-18 | SQL 回修 | 执行失败后的修复 | 问一个可能字段名歧义的问题，如 `每个客户的消费金额` | 首次失败时可回修，最终成功或明确失败 | repair 是否生效 |
| TC-19 | 结果解释 | 解释是否面向业务 | 成功查询后查看结果解释 | 不应只有技术表名/字段名，应解释业务口径 | explanation 是否有用 |
| TC-20 | 反馈闭环 | 错误反馈是否记录 | 对一条结果点“反馈错误”，填写原因 | 知识库概览中出现反馈记录 | 后端反馈文件是否写入 |

---

## P1: 建议补充测试

| 编号 | 模块 | 测试点 | 操作 / 问题 | 预期结果 | 重点观察 |
|---|---|---|---|---|---|
| TC-21 | 标准问法 | 保存正确查询为样例 | 查询成功后点保存查询 | 标准问法列表出现该问题和 SQL | 是否写入服务端 examples |
| TC-22 | Few-shot | 标准问法影响生成 | 保存一个标准问法后，问相似问题 | SQL 结构应更接近样例 | few-shot 是否被带入请求 |
| TC-23 | 标准问法 | 删除样例 | 删除某条标准问法 | 刷新后仍删除 | 本地和服务端是否一致 |
| TC-24 | 质量评分 | Profile 修改后质量刷新 | 补字段描述、表粒度、关系后保存 | 知识库质量分应变化 | 质量缓存是否刷新 |
| TC-25 | 召回调试 | 调试接口不执行 SQL | 在召回调试页输入问题 | 只显示召回信息，不应执行查询 | `/api/debug/retrieval` 是否独立 |
| TC-26 | 关系管理 | 错误关系格式 | 输入 `orders user_id users id` | 应提示格式不合法，不保存 | 前端校验是否足够 |
| TC-27 | 指标管理 | 指标重命名 | 修改指标名称并保存 | 旧指标消失，新指标存在 | `profile.metrics` 是否正确更新 |
| TC-28 | 指标管理 | 删除指标 | 删除一个指标后刷新 | 指标不再出现 | 删除是否持久化 |
| TC-29 | 发布版本 | 重命名版本 | 对历史版本点重命名，填写描述 | 名称和描述立即更新，刷新仍存在 | meta 文件是否生效 |
| TC-30 | 发布版本 | 删除后回滚不可用 | 删除一个版本后尝试回滚该版本 | 应提示版本不存在 | 后端 404 是否合理 |
| TC-31 | 数据源异常 | 数据库文件不存在 | 临时改一个 source URL 指向不存在文件 | 前端不应白屏，后端返回明确错误 | 错误处理是否友好 |
| TC-32 | 词表缺失 | glossary 文件不存在 | 配置一个不存在的 glossary | 系统仍可加载 Schema | 是否只是 warning |
| TC-33 | 空结果 | 查询结果为空 | 问一个明显无匹配的问题 | 返回空表和清晰提示 | 前端空状态 |
| TC-34 | 歧义澄清 | 问模糊问题 | 输入: `最好的客户是谁？` | 应主动澄清或使用明确指标 | 是否硬猜 |
| TC-35 | 枚举映射 | 中文枚举映射 | 问: `已取消订单有多少？` | SQL 应映射到 `cancelled` 等真实值 | enum / profile 是否生效 |

---

## P2: 边界、压力与稳定性测试

| 编号 | 模块 | 测试点 | 操作 / 问题 | 预期结果 |
|---|---|---|---|---|
| TC-36 | 并发 | 多人同时查询 | 两台电脑同时访问并提问 | 服务不崩，结果互不串 |
| TC-37 | 长问题 | 超长自然语言输入 | 输入很长的业务描述 | 系统不白屏，能拒绝或正常处理 |
| TC-38 | 特殊字符 | SQL / HTML 注入显示 | 问题里包含 `<script>`、引号、分号 | 页面不执行脚本，后端不执行危险 SQL |
| TC-39 | 大结果 | 查询大量数据 | 输入: `列出所有订单明细` | 结果被限制，前端不卡死 |
| TC-40 | 页面刷新 | 配置持久化 | 保存 Profile 后刷新浏览器 | 配置仍存在 |
| TC-41 | 网络异常 | 后端断开 | 打开页面后停止后端服务，再执行查询 | 前端给出错误提示，不应一直 loading |
| TC-42 | 慢查询 | 查询耗时较长 | 构造复杂聚合或 JOIN 问题 | 有进度反馈，超时后明确失败 |
| TC-43 | 多浏览器 | 多端配置冲突 | 两个浏览器同时编辑同一字段并保存 | 最终结果可解释，不应产生损坏文件 |
| TC-44 | 版本数量 | 多次发布 | 连续发布 10 次以上 | 版本列表稳定，最多展示最近版本，不影响性能 |
| TC-45 | 缓存一致性 | 修改 Profile 后立即提问 | 保存字段语义后马上问相关问题 | 新语义应被召回或进入 prompt |

---

## 建议测试顺序

1. 先测 `TC-04 ~ TC-12`，这是 Schema 知识库核心。
2. 再测 `TC-13 ~ TC-18`，这是召回、生成、校验、执行主链路。
3. 然后测 `TC-21 ~ TC-30`，这是产品化治理能力。
4. 最后测 `TC-36 ~ TC-45`，看稳定性、并发和边界。

---

## 常见问题归因参考

| 现象 | 可能原因 | 优先检查 |
|---|---|---|
| 选错数据源 | 路由模型判断错误或历史上下文干扰 | `/api/debug/retrieval`、路由输出、当前 source |
| 表能看到但字段为 0 | DDL 解析不兼容字段引号或数据库 inspect 失败 | `schema.py` 字段解析、数据库原始 DDL |
| SQL JOIN 错误 | 外键缺失或人工关系未配置 | Schema 配置页的表关系 |
| 指标算错 | 指标口径缺失或默认过滤缺失 | Profile 的 metrics 和 default_filters |
| 中文问法找不到字段 | 字段中文名/描述/词表不足 | 字段画像、Glossary、标准问法 |
| 生成了危险 SQL | Prompt 或 validator 漏拦截 | `validator.py` |
| 发布后版本不显示 | 前端版本列表未刷新 | `loadProfileVersions` 和 `renderProfileVersions` |
| 保存字段后多出快照 | 草稿保存和发布快照逻辑混淆 | `save_profile_dict` / `publish_profile` |
| 反馈没记录 | 后端 feedback 写入失败或前端没刷新 | `data/feedback`、知识库概览 |

