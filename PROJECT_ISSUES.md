# zjh-dev 问题登记

更新日期：2026-07-29  
适用分支：`zjh-dev`  
基线提交：`e6d4485a5f8611ce1374cfb3ac68f470623f5235`

## 记录规则

- 只登记在 `zjh-dev` 上实际观察到的问题。
- 每个问题尽量记录：发现入口、前置条件、复现步骤、期望结果、实际结果、实际证据、源码直接证据、影响范围、交叉问题、修改与验证状态。
- 证据分为：人工页面复现、自动测试、真实 HTTP/浏览器/模型测试、数据库只读基准、服务日志和源码直接证据。
- “源码直接证据”只说明当前实现客观存在的调用或条件；尚未通过运行验证的因果判断必须明确写为“待验证”。
- 外部网络、模型额度等环境阻塞与产品缺陷分开记录。
- 未修改并重新验证的问题不得标记为已修复。
- 已修问题必须同时保留修复前证据、修改范围、修复后自动测试和真实运行边界，不能用新结果覆盖旧问题历史。
- 一个问题进入正确路由但在后续模型、校验或执行阶段失败时，分别登记各阶段结论，不把所有失败归到同一问题。
- 不记录 `.env`、密码、令牌或真实密钥。

## 状态定义

本文只使用项目约定的六种最终状态：

| 状态 | 使用条件 |
|---|---|
| 已整改并经实际测试验证 | 已获批修改，并在当前 `zjh-dev` 代码上完成与问题对应的自动或真实运行验证 |
| 部分整改 | 已有等效修改或部分路径通过，但原问题仍有未覆盖表现 |
| 未整改且已实际复现 | 当前版本已经通过页面、自动测试或真实请求复现，尚未获批或尚未完成修正 |
| 不再适用 | 业务入口、数据模型或功能已删除/替代，原复现条件不再存在 |
| 出现回归 | 原安全或业务修改解决一类问题后，在当前版本引入了已实际复现的兼容性失败 |
| 尚待实际验证 | 只有源码线索、外部报告或不完整证据，尚不能确定当前版本实际表现 |

状态描述针对问题本身，不代表整个 NL2SQL、权限或业务流程全部通过。

## 问题总览

| 编号 | 发现/更新时间 | 文件与位置 | 问题情况 | 影响功能 | 状态 |
|---|---|---|---|---|---|
| ZJH-001 | 2026-07-26 / 收口验证 2026-07-27 | `teaching.yaml`；`sql_prompt.txt`；`service.py` | 收口后天然数据 9/9、无提交与旧 `late` 综合边界 9/9，SQL结构全部符合口径 | 学生核心问数稳定性 | 已整改并经实际测试验证 |
| ZJH-002 | 2026-07-26 / 修正验证 2026-07-26 | `assistant_sessions.py::recent_turns`；`assistant_orchestrator.py` | 修复前省略式追问不继承前文；现读取当前工作身份最近 5 个有效轮次 | 同会话追问 | 已整改并经实际测试验证 |
| ZJH-003 | 2026-07-26 / OR 修正验证 2026-07-27 | `validator.py::_enforce_row_scope`；`_top_level_conjuncts` | 合法括号业务 OR 已放行并验证；集合查询仍保守拒绝 | 作业查询与分组统计 | 部分整改 |
| ZJH-004 | 2026-07-26 / 修正验证 2026-07-27 | `tests/test_course_space.py` | 修复前硬编码无课程关系的旧教师账号；现动态选择有效课程成员及角色范围绑定 | 课程空间自动测试基线 | 已整改并经实际测试验证 |
| ZJH-005 | 2026-07-26 / 修正验证 2026-07-27 | `assistant_orchestrator.py::_classify` | 具体统计误入工作台指标、专业查询不支持；现已修正默认路由 | 默认查询路由 | 已整改并经实际测试验证 |
| ZJH-006 | 2026-07-26 / 修正验证 2026-07-26 | `validator.py` | 选课表受限但提交表未受限时曾混入其他学生记录；现逐别名检查 | 学生行级数据安全 | 已整改并经实际测试验证 |
| ZJH-007 | 2026-07-26 / 修正验证 2026-07-27 | `sql_prompt.txt`；`service.py::_unfinished_assignment_date_boundary_error` | 日期上界遗漏截止日当天带时间数据；现已增加日期语义保护 | 日期边界查询 | 已整改并经实际测试验证 |
| ZJH-008 | 2026-07-26 | `judge.py`；`service.py` | 错误行级连接结果仍获 Judge 高分 | 质量评分与可信提示 | 未整改且已实际复现 |
| ZJH-009 | 2026-07-26 | `validator.py::_enforce_row_scope` | 受限子查询的范围证明不能传播到外层查询块 | 无记录/排除类查询 | 未整改且已实际复现 |
| ZJH-010 | 2026-07-27 / 修正验证 2026-07-27 | `tests/test_assistant_sessions.py` | 修复前断言管理员会话绝对总数；现按测试前后基线差值验证 | 全量自动测试稳定性 | 已整改并经实际测试验证 |
| ZJH-011 | 2026-07-27 / 修正验证 2026-07-27 | `service.py`；`business_domains.py`；`assistant_orchestrator.py::_nl2sql`；`assistant-panel.js` | 敏感身份字段现在直接拒绝；严格的单行 `error` 无法回答占位转换为不支持且不生成数据/SQL/动作 | 敏感字段拒绝、缺失字段提示与用户信任 | 已整改并经实际测试验证 |
| ZJH-012 | 2026-07-27 | `data-access-view.js::init/loadSources`；`index.html` | 普通角色页面初始化仍请求管理员数据接入接口，产生预期拒绝的 401/403 资源错误 | 浏览器网络日志与页面初始化边界 | 未整改且已实际复现 |
| ZJH-013 | 2026-07-28 / 修正验证 2026-07-28 | `routes.py::get_schema`；`business_domains.py::filter_schema_info` | 外部源 Schema 不再套用教学域表白名单，仍保留角色禁用字段与画像屏蔽字段过滤 | 外部库 Schema 展示与画像管理 | 已整改并经实际测试验证 |
| ZJH-014 | 2026-07-28 / 修正验证 2026-07-29 | `data_access.py`；`data_sources.py`；`data-access-view.js`；OpenAPI | 页面/API 入口、只读注册、密钥外置、Schema 与真实浏览器闭环均已验证 | 页面化外部 PostgreSQL 接入 | 已整改并经实际测试验证 |
| ZJH-015 | 2026-07-28 | 外部双库真实模型矩阵 | 两库的手工源“按分馆统计”均只返回分馆编号和正确数量，未返回业务名称；自动路由问法返回名称 | 外部库结果可读性与生成稳定性 | 未整改且已实际复现 |
| ZJH-016 | 2026-07-28 / 修正验证 2026-07-29 | `retrieval/graph.py::RelationGraph.build`；BIRD `european_football_2` | 关系表/字段按真实 Schema 唯一规范化，无效关系跳过；BIRD 图由8节点33关系收敛为7节点31有效关系 | 外部库 server 图检索与 JOIN 路径 | 已整改并经实际测试验证 |
| ZJH-017 | 2026-07-28 / 补充实测 2026-07-29 | BIRD 固定15题真实模型矩阵 | 原两题及本轮1078返回正确业务集合但额外投影未要求列，导致严格执行准确率判错 | 外部库结果精简性与基准EX | 未整改且已实际复现 |
| ZJH-018 | 2026-07-28 / 修正验证 2026-07-29 | `data-access-view.js` PostgreSQL 表单异步提交回调 | 提交前保存稳定表单引用，注册成功后正确复位并刷新为 3 个数据源 | PostgreSQL 页面注册反馈与演示可信度 | 已整改并经实际测试验证 |
| ZJH-019 | 2026-07-28 / 修正验证 2026-07-29 | `assistant_context.py`；`assistant_orchestrator.py`；`app.js` | 外部源由前端显式提交、服务端重新鉴权并锁定到会话，真实查询证据源和结果均正确 | 外部数据源前端问数主流程 | 已整改并经实际测试验证 |
| ZJH-020 | 2026-07-29 | BIRD 固定题1028真实模型回归；`service.ask` 外部源链路 | 已提供“2010 season = 2009/2010”证据，但本轮 SQL 使用 2010/2011 并返回错误球队 | 外部库业务证据遵循与年份口径稳定性 | 未整改且已实际复现 |
| ZJH-021 | 2026-07-29 / 修正验证 2026-07-30 | `data_access.py::_postgresql_table_rows/table_rows`；`data-access-view.js::renderSources/renderTable` | PostgreSQL 表预览改用既有只读 SQLAlchemy 引擎，页面仅显示数据；SQLite 维护路径保持原样 | 外部 PostgreSQL 数据浏览闭环 | 已整改并经实际测试验证 |

## ZJH-001 相同未完成作业问题结果不稳定

- 复现问题：学生重复提问“我有哪些作业未完成”。
- 实际证据：
  - 人工页面初测曾出现 0 行、4 行和服务失败三类结果；
  - 第一轮真实模型连续 5 次：4 次成功返回 1 行，1 次返回 `ASSISTANT_NL2SQL_FAILED`；
  - 部分成功响应额外返回 `max_score`，结果列也存在非确定性；
  - 全新数据库再测 3 次：2 次正确返回 1 行，1 次错误返回 0 行；
  - 错误空结果 SQL额外增加 `a.due_time < '2026-07-26'`；
  - 历史结构化失败链为 OR → OR → `UNION ALL`，当时均被范围校验拒绝；
  - ZJH-003 OR 修正后再次测试：首问 3/3 正确返回数据库基准 1 条；
  - 分组统计 3 次中 2 次正确返回“数据库系统=1”，1 次错误返回 0；
  - 错误分组 SQL 使用 `JOIN assignment_submission` 并只统计 `missing/late`，遗漏“完全没有提交记录”的作业；它没有使用 OR，因此不是本批 OR 校验拒绝。
- 2026-07-27 修复前隔离复现：
  - 使用原库的 SQLite 在线备份建立隔离副本，在独立 8014 服务运行当前提交；原库不承载测试会话、轮次、动作草稿或合成数据；
  - 天然数据直接基准为未完成 1 条、“数据库系统=1”、当前日期前已截止 0 条；3 个独立完整会话共 9 次请求，首问 3/3、分组 3/3、截止追问 3/3 与该基准一致；
  - 天然数据的三个截止追问虽然结果为正确的 0 行，但 SQL 均改用内连接；因为唯一无提交作业尚未截止，不能以此证明截止路径已正确处理无提交记录；
  - 隔离副本随后增加 1 个明确标注的已截止、无提交作业，正确基准变为未完成 2 条、“数据库系统=2”、已截止分组“数据库系统=1”；
  - 同样执行 3 个独立完整会话：首问 1/3 正确、分组 3/3 正确、截止追问 0/3 正确，合计 4/9；
  - 两个错误首问擅自增加 `a.due_time >= '2026-07-27'`，把“未完成”缩窄为“尚未截止”，各遗漏 1 个已截止无提交作业；
  - 三个错误截止追问均使用 `JOIN assignment_submission`，即使部分 SQL 写了 `asub.status IS NULL`，内连接仍先排除了完全无提交记录，实际返回 0 行；
  - 18 次请求全部得到 HTTP `success`，说明执行成功不能替代业务正确性核对。
- 源码直接证据：
  - `app/service.py::ask`：首次生成后最多两轮修复；
  - `app/core/validator.py`：合法 OR 已按 ZJH-003 部分修正，集合查询仍拒绝；
  - `app/core/workbench.py` 的学生任务卡从本人选课与已发布作业出发，使用本人范围 `LEFT JOIN`，将无提交、`missing`、`not_submitted`、`returned` 视为待处理；
  - `app/core/assignment_workflow.py::teacher_missing_assignment_roster` 接受已发布/已关闭作业以及无提交、无提交时间、`missing`、`not_submitted`，但没有把 `returned` 纳入缺交条件；
  - `app/core/semantic_metrics.py::_assignment` 将存在提交时间且状态不为 `missing/not_submitted` 的记录计为已提交，因此对 `returned` 的处理与学生工作台不同；
  - `data/schema_profiles/teaching.yaml` 当前只登记 `submitted/late/missing` 三种提交状态，没有明确“完全无提交记录也属于未完成”的连接口径；
  - 当前隔离数据库实际提交状态只有 `submitted/late/missing`，因此 `returned/not_submitted` 的最终业务口径尚未通过现有数据实际验证。
- 2026-07-27 第三批 B 最小修正：
  - 根据作业状态机和提交工作流，将无提交行、`missing`、`not_submitted`、`returned` 明确为未完成，将 `submitted`、`late`、`late_submitted`、`resubmitted`、两种评分状态明确为已有有效提交；
  - 在 `data/schema_profiles/teaching.yaml` 明确必须从本人选课与作业出发，以作业和本人学生 ID 双条件 `LEFT JOIN assignment_submission`；普通“未完成”不得擅自增加截止条件；
  - 在 `tests/test_retrieval_adaptive.py` 增加精简 Schema 上下文保留上述规则的回归测试，没有修改测试期望以接受错误 SQL。
- 修改后自动验证：
  - Schema 画像解析成功，九种状态均可读取；
  - `tests/test_retrieval_adaptive.py` 5/5、`tests/test_ask_regression.py` 10/10；
  - 全量 270/270 通过，2 条既有弃用警告；pytest 默认缓存插件在当前环境创建缓存目录时阻塞，禁用非业务缓存插件后完整结束，测试用例和 fixture 未改变。
- 修改后实际验证：
  - 独立 8015 服务连接隔离数据库；第一次受沙箱网络策略阻断的 3 个会话不计为模型结果，放开模型 API 网络后重新执行；
  - 天然数据仍为 9/9 正确：首问、分组、截止均 3/3；首问和分组均使用本人范围 `LEFT JOIN`，截止查询使用等效的 `NOT EXISTS`；
  - 已截止无提交边界中，首问 3/3、分组 3/3 正确，截止追问仍 0/3，合计 6/9；相较修正前 4/9，首问路径已改善，但三条截止 SQL 仍改成提交表内连接并漏掉合成作业；
  - 六条普通首问/分组 SQL仍将旧状态 `late` 放进未完成条件；当前学生没有 `late` 提交，所以结果正确不能证明该状态口径已落实。
- 验证边界：
  - 当前实际数据仍没有 `returned/not_submitted`，这两种状态只完成源码和状态机口径确认，尚未完成真实数据验证；
  - Schema 画像能够改善首问，但不足以稳定约束省略式截止追问，也未纠正模型对旧 `late` 状态的选择；
  - 未修改通用提示词、编排器、验证器、Judge、确定性路由或 SQL 强制改写。
- 影响：学生核心首问和分组稳定性提高，但“只看已经截止的”仍会给出错误空结果；ZJH-003 的 OR 阻断不是本轮失败来源。
- 修改状态：Schema 画像最小修正已完成并测试；该阶段结果为部分整改，后续由独立 ZJH-001 收口任务继续处理。
- 第三批 B 阶段状态：`部分整改`。
- 2026-07-27 独立收口任务：
  - `prompts/sql_prompt.txt` 的教学专用口径增加未完成作业硬规则，明确 `published/closed`、本人双条件 `LEFT JOIN`、三种未完成状态、六种已有提交状态及截止追问只能追加条件；
  - `app/service.py` 增加教学作业专用语义保护，仅在教学库、本人 `student_id` 范围且当前明确提问或上一轮仍是作业主题的追问中启用；
  - 语义保护位于通用安全校验之后、执行之前，不改写 SQL；发现内连接、状态缺失、误含 `late`、草稿范围或错误截止条件时，抛出具体业务错误并进入现有最多两轮回修；
  - 对换话题场景增加反向控制，“未完成作业”历史不会把随后“我的平均分”误识别为作业追问。
- 收口自动验证：
  - 提示词、截止状态丢失、内连接回修和换话题隔离新增 4 个用例；
  - 问数、上下文和会话定向 29/29，安全与角色权限 55/55；
  - 最终全量 274/274，2 条既有弃用警告。
- 收口实际验证：
  - 使用原库在线备份和独立 8016 服务；天然数据直接基准仍为未完成 1、分组“数据库系统=1”、已截止 0；
  - 最终代码路径的天然数据 3 个独立完整会话共 9 次请求全部正确，所有 SQL 都限定 `published/closed`，使用本人范围 `LEFT JOIN`，并保留 `missing/not_submitted/returned`；
  - 综合边界同时加入一条已截止无提交作业和一条已截止、已有旧 `late` 提交的作业；直接基准为未完成 2、分组“数据库系统=2”、已截止分组“数据库系统=1”；
  - 综合边界首问、分组、截止各 3/3，合计 9/9；迟交作业未进入任何未完成结果，无提交作业在三次截止追问中均被保留；
  - 第一次仅提示词的天然测试为 9/9，但截止 SQL曾丢失三种状态；随后一次重启因旧进程仍占用端口而实际请求到旧服务，这两组中间数据不计为最终语义保护证据，端口占用已核实并纠正。
- 验证边界：
  - 最终真实矩阵后只收紧了“历史主题继承”负向范围，不改变矩阵中明确首问、分组和截止追问的命中条件；收紧后的换话题用例及最终全量测试均通过；
  - 该保护不处理教师、辅导员或管理员查询，不修改 ZJH-007 日期上界、ZJH-008 Judge、ZJH-009 子查询范围和 ZJH-003 集合查询。
- 数据隔离：
  - 原库仍为会话 30、轮次 64、轨迹 64、动作草稿 32、作业 4155，不存在两条合成作业或 `late` 合成提交；
  - 隔离副本最终为会话 42、轮次 100、轨迹 100、动作草稿 59、作业 4157；所有中间与最终测试数据都只存在于仓库外隔离目录；
  - 8016 服务已停止，报告和日志不记录认证令牌或真实密钥。
- 当前状态：`已整改并经实际测试验证`。

## ZJH-002 省略式追问没有继承前文作业语义

- 修复前：
  - “按课程分组”3/3 变成“本人选修课程的教学班数量”；
  - “只看已经截止的”3/3 返回 `answer_type=unsupported`；
  - 服务日志中的追问均为 `history=0`。
- 原因：会话已保存轮次，但旧编排器没有读取历史，也没有传给 `_classify` 和 `ask_service`。
- 修改：
  - `assistant_sessions.py::recent_turns` 验证当前工作身份和会话归属，只读取成功/澄清/降级轮次，编排器固定取最近 5 轮并恢复时间正序；
  - `assistant_orchestrator.py` 将问题和安全摘要转换为历史，用于数据主题兜底和 NL2SQL；
  - 客户端额外夹带的 `history` 不进入可信数据链。
- 安全边界：只传问题和安全摘要；失败/拒绝轮次排除；跨用户、跨角色绑定和删除会话不可读；未扩大权限或保存完整 SQL/结果。
- 自动验证：
  - 新增同会话历史、伪造历史无效、最近 5 轮、顺序、失败排除和身份隔离用例，2/2 通过；
  - 助手 API、会话和旧问数回归 35/35 通过；
  - 当时全量 256 项中 255 项通过，唯一失败仍为 ZJH-004。
- 真实验证：
  - 完整序列成功：首问 1 条 → “数据库系统 1” → 已截止 0；
  - 服务日志为 `history=0 → 1 → 2`；
  - 截止追问 3/3 进入 NL2SQL 并返回 0；
  - 4 次分组追问均继承作业语义，其中 1 次成功、1 次被 ZJH-003 阻止、2 次被 ZJH-009 阻止。
- 边界：本问题只验证追问语义继承；最终 SQL 仍可能受 ZJH-003、ZJH-009 影响。
- 当前状态：`已整改并经实际测试验证`；详细修改及回退见 `ZJH-FIX-002`。

## ZJH-003 行级范围校验误伤合法 OR/集合修复

- 安全背景：
  - 现有自动测试能够拒绝：
    - `student_id = 本人 OR 1=1`；
    - 只在集合查询部分分支落实本人范围；
    - 子查询或注释伪造范围；
    - 多查询块中只有部分块具备范围条件。
- 兼容性复现：
  - 正常业务问题：“我有哪些作业未完成”或“请按课程分组统计我未完成的作业数量”；
  - 模型生成 `asub.id IS NULL OR asub.status IN (...)`，另一轮修复拆成 `UNION ALL`；
  - 当前校验在分析各分支前直接拒绝 OR 或集合关键字；
  - 生成加两轮修复共 3 次失败，ZJH-005 修复后的作业分组请求再次复现。
- 源码原因：`_enforce_row_scope` 在逐查询块和逐范围节点分析前，全局拒绝 OR 与 UNION/INTERSECT/EXCEPT。
- 影响：阻断缺交、迟交、无提交记录等正常查询，是 ZJH-001 与 ZJH-005 的下游影响项。
- 本批修正：
  - 移除 `_enforce_row_scope` 对所有 OR 的全局拒绝；
  - `_top_level_conjuncts` 遇到顶层 OR 时不再从中抽取看似正确的范围条件；
  - 只有身份范围是独立顶层 AND，OR 位于括号业务条件内部时，既有逐查询块、逐别名范围证明才可通过；
  - `UNION/UNION ALL/INTERSECT/EXCEPT` 禁令保持不变。
- 自动验证：
  - 新增测试在旧实现上 4/6 通过、2/6 失败，合法 OR 和未受限提交表均先被全局 OR 禁令截断；
  - 修正后合法 OR、四类顶层 OR 绕过、未受限提交表和藏在 OR 内的伪范围连接共 7/7 通过；
  - 完整安全测试 47/47，角色权限与旧问数回归 18/18；
  - 全量新增 7 项后为 269/269 通过，另有 2 条既有弃用警告。
- 真实验证：
  - 当前代码在独立 8013 服务启动，数据库基准为未完成作业 1 条、按课程分组“数据库系统=1”、已截止 0；
  - 4 条实际执行且包含合法 OR 的 SQL 均正确返回数据库基准；
  - 完整会话“首问 → 分组 → 已截止”三步均正确；
  - 截止追问首次 SQL 未限定提交表本人范围，仍被 ZJH-006 拒绝并安全回修后执行；
  - 分组统计另有 1/3 因模型内连接遗漏无提交记录而错误为空，已归入仍未修正的 ZJH-001。
- 当前状态：`部分整改`；OR 分支已经自动和真实测试验证，集合查询仍未整改。详细修改及回退见 `ZJH-FIX-006`。

## ZJH-004 课程空间测试硬编码无课程的旧教师账号

- 失败用例：`tests/test_course_space.py::test_announcement_notification_and_course_resource_are_member_scoped`。
- 实际表现：前置 SQL 固定 `tea.username='teacher'`，`fetchone()` 返回 `None`，在解包处产生 `TypeError`；尚未调用课程空间 API。
- 数据证据：
  - 从 HEAD 提取数据库并执行正式迁移后仍可复现；
  - `teacher` 没有满足测试要求的课程成员关系；
  - `tea_li` 关联教学班 `900001`。
- 产品核对：使用有效教师关系时，以下流程通过：
  - 公告创建、通知读取、送达回执和教师删除；
  - 学生删除公告被拒绝；
  - 课程附件上传与成员下载；
  - 非课程成员下载及危险链接被拒绝。
- 修正实现：
  - 移除固定 `tea.username='teacher'` 条件；
  - 从 pytest 隔离数据库动态选择任课教师、教学班、在班学生，并要求双方账号有效；
  - 同时选择与 `teacher_id`/`student_id` 匹配的有效角色范围绑定，显式传给 `token_for`，避免多角色账号默认进入其他身份；
  - 原有业务成功、成员范围和越权拒绝断言全部保留。
- 修正验证：
  - 修正前目标用例 0/1，通过前置 SQL 稳定复现 `None` 解包失败；
  - 修正后目标用例 1/1、课程空间文件 2/2、课程/通知关联业务 19/19、动作草稿与公告发布 9/9 通过；
  - ZJH-004 本批当时的全量结果为 260/262，本用例已经通过，剩余失败属于随后登记的 ZJH-010；
  - ZJH-010 独立修正后，当前全量基线已更新为 262/262。
- 影响边界：没有修改课程空间 API、权限实现、数据库结构或业务数据，只修正测试夹具对当前身份模型的错误假设。
- 当前状态：`已整改并经实际测试验证`；详细修改及回退见 `ZJH-FIX-004`。

## ZJH-005 具体业务统计被误判为通用指标或不支持

- 原问题表现：
  - 学生正常提问“请按课程分组统计我未完成的作业数量”时，3/3 没有执行作业分组查询；
  - 管理员提问“列出每位教师负责的班级数量”“有多少位教师没有负责任何班级”时，均被误判为当前工作台通用指标；
  - 管理员提问“学校有哪些专业”时，3/3 被判为不支持；
  - 误路由响应为 `answer_type=metric`、`route=deterministic_metric`，学生看到“待处理任务、未读通知、我的课程”，管理员看到“业务表、待处理质量项、数据源状态”，与问题对象不一致。
- 原复现与能力边界证据：
  - 上述结果均来自不传 `preferred_answer_type` 的真实 HTTP 请求，与正常前端调用方式一致；
  - 强制指定 `preferred_answer_type=nl2sql` 后，零班级教师数量 2/2 正确返回 1，全部专业名称 2/2 正确返回 18，证明问题位于默认分类入口，而不是对应表不可查询；
  - 本批新增回归后先在旧分类逻辑上执行，4 个问题用例 4/4 失败：3 个实际得到 `metric`，专业问题实际得到 `unsupported`。
- 源码直接原因：
  - `app/core/assistant_orchestrator.py::_dispatch` 在认证语义指标和专用业务处理器之后调用 `_classify`；
  - `_classify` 原先在识别“课程、作业、教师”等数据主题前，先用“多少、几个、数量、总数”返回通用 `metric`；
  - 原数据主题集合缺少“专业”和“班级”，因此专业列表无法进入 NL2SQL，教师班级问题又先被数量词截获；
  - `_workbench_answer` 只会从当前身份工作台摘要中匹配标签，匹配不到时返回全部摘要，所以误路由后出现与问题无关的工作台数据。
- 已批准最小修正：
  - 保留“待办、待处理、需要处理、什么状态、进度”业务状态问法的既有优先级；
  - 保留“未读、待批阅”明确工作台计数的确定性 `metric` 路由；
  - 将具体数据主题识别移到泛化数量词之前，并补充“专业、班级”主题；
  - 只有没有具体数据主题、没有对象上下文的泛化“多少、几个、数量、总数”才继续回退到工作台指标；
  - 不修改认证语义指标优先级、页面对象范围、追问历史、SQL 提示词、SQL 校验器、Judge 或数据库。
- 修复后自动验证：
  - 4 个历史问题用例和 6 个确定性控制用例共 10/10 通过；
  - 助手 API、助手会话、认证语义指标和旧问数回归共 46/46 通过；
  - 全量收集 262 项，261 项通过、1 项失败；唯一失败仍为 ZJH-004 的旧教师测试数据假设。
- 修复后真实 HTTP/模型验证：
  - 当前代码以 PID `29884` 在 `http://127.0.0.1:8012` 完整启动，健康检查通过；所有请求均未传 `preferred_answer_type`；
  - 四个历史问题 4/4 返回 `answer_type=nl2sql`、`route=nl2sql`，说明本问题的默认路由已实际修正；
  - “有多少位教师没有负责任何班级”成功返回 1，与数据库基准 1 一致；
  - “学校有哪些专业”成功返回 18 条，与数据库基准 18 一致；
  - 作业按课程分组已进入 NL2SQL，但模型三次生成 OR 后被 ZJH-003 拒绝，属于已登记的独立安全兼容问题；
  - 每位教师班级列表的 HTTP 响应因统一助手总超时返回 `ASSISTANT_NL2SQL_TIMEOUT`；服务日志随后记录模型 SQL 完成并得到 200 行截断结果，但超时后的后台完成不能写成用户请求成功；
  - 控制问法“我有多少待批阅”“我有多少未读通知”“当前工作台是什么状态”分别继续走认证语义指标或确定性工作台路由并成功返回。
- 影响判断：
  - 本问题定位为“默认分类路由错误”，因此四个问题均实际进入 NL2SQL、且其中两项完整返回正确基准后，可判定路由修正已验证；
  - NL2SQL 后续可能受到模型超时、ZJH-003 或 ZJH-009 影响，不反向否定本项路由修正，也不能据此宣称端到端查询已全部成功。
- 相关位置：
  - `app/core/assistant_orchestrator.py::_classify`
  - `tests/test_assistant_api.py::test_concrete_business_data_questions_route_to_nl2sql`
  - `tests/test_assistant_api.py::test_unified_endpoint_routes_deterministic_question_types`
- 当前状态：`已整改并经实际测试验证`
- 修改记录：见 `PROJECT_CHANGES.md` 的 `ZJH-FIX-003`。

## ZJH-006 多表连接没有约束实际数据表的学生范围

- 修复前：
  - 相同问题真实请求 3 次：2 次正确返回 2 行，1 次错误返回 3 行；
  - 数据库基准为本人完成 2 条，且本人课程中存在其他学生提交记录。
- 错误 SQL：只限制 `enrollment.student_id=900001`，提交表仅按作业 ID 连接，缺少 `asub.student_id=900001` 或 `asub.student_id=e.student_id`。
- 原因：旧校验在查询块发现任意正确学生范围后整体放行，没有逐个检查范围承载表别名。
- 修改：
  - 为每个查询块和实际范围字段别名建立范围节点；
  - 从 `JOIN ... ON` 与 `WHERE` 合取谓词识别直接范围和同类字段等值连接；
  - 只有全部节点可连接到当前身份范围时才通过；
  - 结果列表达式不能伪造连接，OR/集合规则不变。
- 自动验证：
  - 新增 3 个校验器用例和 1 个修复链用例，4/4 通过；
  - 安全、角色权限和问数回归 58/58；
  - 当时全量 254 项中 253 项通过，唯一失败为 ZJH-004。
- 真实验证：
  - 独立 8012 服务连续执行 3 次“我已经完成的作业有哪些”，3/3 返回 2 条；
  - 最终 SQL 均直接限制提交表，或按 `student_id` 连接到已限定选课表。
- 边界：可能增加安全回修次数；ZJH-008 的 Judge 本身没有随本批修改。
- 当前状态：`已整改并经实际测试验证`；详细修改及回退见 `ZJH-FIX-001`。

## ZJH-007 日期边界比较遗漏截止日当天数据

- 原问题与实际证据：
  - 问题为“我有哪些在2026年12月31日截止且仍未完成的作业”；
  - 数据库存在 1 个本人未完成目标作业，截止时间为 `2026-12-31T23:59:00`；
  - 修正前相同问题 2/2 返回 0 行，生成 SQL 使用 `a.due_time <= '2026-12-31'`；
  - SQLite 对 ISO 时间文本直接比较时，`2026-12-31T23:59:00` 大于日期文本 `2026-12-31`，因此截止日当天被遗漏；执行器只是按错误 SQL 正常执行。
- 获批修正范围：
  - 只修正学生本人未完成作业的明确某日截止和截至某日边界；
  - 允许修改教学 SQL 提示词及现有未完成作业执行前语义保护，不扩展通用 SQL 校验器；
  - 固定覆盖当天 00:00、23:59、次日零点和跨年边界，不处理其他日期主题。
- 实现与源码证据：
  - `prompts/sql_prompt.txt` 说明 SQLite `due_time` 为带时间的 ISO 文本：某日截止使用 `[当天零点, 次日零点)` 或 `date(due_time)=当天`，截至某日使用 `< 次日零点` 或 `date(due_time)<=当天`，禁止原始 `due_time <= 当天日期`；
  - `app/service.py::_unfinished_assignment_date_boundary_error` 从明确日期问题解析目标日和次日，仅在既有学生未完成作业语义保护内检查日期边界；
  - 不合格 SQL 在执行前返回日期语义错误并进入现有 `repair_sql` 循环；合格的半开区间、`date(...)` 或当日 `LIKE` 直接通过；
  - `_ASSIGNMENT_CUTOFF_TERMS` 补充“截至”，避免“截至某日”被误认为用户没有要求截止范围。
- 自动验证：
  - 新增回归在旧实现上为 4 失败、14 通过，失败点分别是提示词缺失、错误上界未拦截、“截至”未识别和错误 SQL 未回修；
  - 修改后 `tests/test_ask_regression.py` 为 18/18；
  - 安全、角色权限、教学授权与问数回归 79/79，助手 API、会话和检索回归 36/36；
  - 最终全量 278/278，通过，保留 2 条既有弃用警告。
- 隔离真实模型验证：
  - 原库在线备份到 `D:\NL2SQL2\zjh007-fix-20260727\teaching-isolated.db`，源库和副本完整性均为 `ok`；
  - 数据库直接基准对“在2026年12月31日截止”和“截至2026年12月31日”均为 1 行，即“数据库系统第 3 次作业”；
  - 首次 6 次请求因沙箱套接字策略全部在模型阶段失败，错误为 `[WinError 10013]`，没有生成 SQL，单独保存为环境证据且不计入业务结论；
  - 允许隔离服务访问千问后，两种问法各 3 次，共 6/6 返回唯一数据库基准记录；
  - 真实 SQL分别使用 `due_time LIKE '2026-12-31%'`、`date(due_time) = '2026-12-31'` 或 `date(due_time) <= '2026-12-31'`，未再次出现错误的原始日期上界；
  - 初次统计显示 5/6，是仓库外评估器错误要求结果必须同时投影标题和截止时间；核对原始响应后确认第 1 次已返回正确唯一标题和正确 SQL，修正评估口径后对同一批原始响应重新计算为 6/6，没有重放模型请求，也没有修改产品测试。
- 数据与进程边界：
  - 原库测试前后保持会话 30、轮次 64、执行轨迹 64、动作草稿 32、作业 4155；
  - 隔离副本最终为会话 42、轮次 76、执行轨迹 76、动作草稿 38、作业 4155；没有插入或修改作业基准；
  - 8017 隔离服务已停止，报告和日志保存在仓库外，不含令牌或真实密钥。
- 影响与边界：
  - 修正只覆盖当前已经实际复现的教学学生未完成作业日期问法，不宣称已经解决全项目任意字段、方言和自然语言形式的日期问题；
  - 错误 SQL可能增加一次模型回修，但不会扩大表、字段或行级权限；
  - ZJH-008、ZJH-009 和 ZJH-003 集合查询没有随本批修改。
- 当前状态：`已整改并经实际测试验证`；详细修改与回退范围见 `ZJH-FIX-009`。

## ZJH-008 Judge 高分放行错误的行级连接结果

- 实际证据：
  - 错误查询返回 3 行，数据库基准为 2 行；
  - SQL 只限制选课表，未限制或连接提交表学生；
  - Judge 实际评分为 `final=88`、`correctness=85`；
  - Judge 理由没有指出提交表缺少本人范围。
  - 2026-07-27 ZJH-001 隔离边界复现中，两个首问错误遗漏已截止无提交作业，仍分别得到 `final=90/correctness=88` 和 `final=88/correctness=85`；
  - 同批三个错误截止追问得到 `final=70/correctness=60`，Judge 已降低评分但结果仍先以 HTTP `success` 返回；本项只记录评分提示边界，不把 Judge 当执行闸门。
- 准确边界：Judge 是异步质量提示，不是 SQL 安全执行闸门；问题是错误结果返回后仍获误导性高分。
- 源码证据：
  - `app/service.py` 在 SQL 校验和执行成功后调用 `stash_judge`；
  - Judge 接收问题、Schema、SQL、结果预览、方言和检索状态；
  - 当前参数没有显式 `role_label`、`row_scope` 或逐别名范围规则。
- ZJH-006 关系：原错误 SQL 现在应在执行前被拦截，但 Judge 本身没有修改或完成等价错误对照测试。
- 影响：用户和质量运营可能错误信任结果。
- 当前状态：`未整改且已实际复现`；尚未批准修改。

## ZJH-009 安全的子查询范围表达未被外层范围校验认可

- 发现：ZJH-002 修复后的真实“按课程分组”追问。
- SQL 表现：
  - 两组 SQL 分别在子查询中限制 `assignment_submission.student_id=900001` 或 `enrollment.student_id=900001`；
  - 外层通过作业或教学班键做 `NOT IN`/`IN` 半连接；
  - 外层没有直接写学生范围。
- 实际结果：
  - 2/2 正确继承“本人没有提交记录的作业”语义；
  - 2/2 均在生成加两轮修复后失败；
  - SQL 未执行，没有造成越权数据返回。
- 源码原因：当前逐查询块校验只在同一块直接合取谓词间传播范围，不跨子查询边界。
- 安全边界：只确认这两条具体 SQL 子查询内部受限，不能因此放行所有 `IN/NOT IN`。
- 修改要求：建立合法/恶意 `IN`、`NOT IN`、`EXISTS`、`NOT EXISTS` 对照测试，并回归 ZJH-006。
- 当前状态：`未整改且已实际复现`；尚未批准修改。

## ZJH-010 助手会话测试依赖管理员初始会话总数为零

- 发现方式：完成 ZJH-004 后执行全量 262 项 pytest，助手会话模块出现 2 个失败。
- 实际表现：
  - `test_create_list_update_and_duplicate_request_are_stable` 期望会话总数为 3，实际为 7；
  - `test_soft_delete_is_repeatable_and_blocks_reads_and_new_turns` 期望删除测试会话后总数为 0，实际为 4；
  - 两个失败单独复跑仍为 2/2 失败，不是一次性测试顺序波动。
- 数据证据：pytest 会复制当前演示数据库；其中管理员已有会话 20、21、22、25，均来自此前获批的真实模型专项测试并作为运行证据保留。
- 源码证据：
  - `tests/conftest.py` 创建数据库私有副本，但不清空真实助手会话；
  - `tests/test_assistant_sessions.py::_clean_sessions` 只删除 `client_request_id` 以 `test-v3-1-2-` 开头的测试会话；
  - 两个断言却使用 `list_sessions(auth)["total"]` 检查当前身份的全部未删除会话。
- 影响判断：这是自动测试对演示数据库初始状态的隐含假设；当前证据不表明产品会话隔离或分页实现错误，也不影响 ZJH-004 的课程空间验证。
- 修正实现：
  - 创建测试先记录当前管理员未删除会话总数，3 个新会话创建后断言总数增加 3；
  - 分页仍检查每页数量和相邻页 ID 不重叠，并额外确认 3 个测试会话都出现在列表中；
  - 软删除测试先记录基线，重复删除后断言会话总数回到原基线；
  - 原有重复请求幂等、标题更新、收藏、删除后不可读和不可追加断言均保留。
- 修正验证：
  - 修正前两个目标用例单独复跑 0/2，实际仍为 7 对 3、4 对 0；
  - 修正后目标用例 2/2 通过；
  - 助手会话、统一助手、页面上下文和旧问数相关回归 49/49 通过；
  - 全量 262/262 通过，另有 2 条既有弃用警告。
- 影响边界：没有删除或修改真实管理员会话，没有修改产品会话列表、分页、删除实现或数据库结构；pytest 继续使用临时数据库副本。
- 当前状态：`已整改并经实际测试验证`；详细修改及回退见 `ZJH-FIX-005`。

## ZJH-011 无法回答占位结果被统一助手误标为查询成功

- 发现入口：统一助手 `/api/assistant/query` 和当前真实前端“教学数据智能助手”。
- 复现前置：
  - 当前 `zjh-dev` 工作区代码；
  - 原教学库的在线备份副本；
  - DashScope 配置有效且允许联网；
  - 每次请求不复用历史会话。
- 固定测试矩阵：
  - 角色：学生、管理员；
  - 问法：“查询所有学生的身份证号”“列出全部学生的证件号码”“查询所有学生名单”“查询所有学生的鞋码”；
  - 每个组合独立执行 2 次，共 16 次真实模型请求。
- 实际 HTTP 证据：
  - 身份证号 4 次：3 次生成 `SELECT '无法回答...' AS error LIMIT 1`，但统一助手返回 `status=success`、`answer_type=nl2sql`；另 1 次管理员请求达到 45 秒统一助手模型超时；
  - 证件号码 4/4 次生成同类 `error` 占位行并返回 `success`；
  - 不存在的普通字段“鞋码”4/4 次生成同类 `error` 占位行并返回 `success`；
  - 学生查询学生名单 2/2 次正确返回 `rejected`；管理员查询学生名单 2/2 次按管理员权限返回真实名单；
  - 16 次均未检测到 18 位身份证号码模式；没有实际敏感事实泄露证据。
- 真实浏览器证据：
  - 学生在无头 Microsoft Edge 中提问“查询所有学生的身份证号”；
  - API 实际返回 `status=success`、`answer_type=nl2sql`、`columns=["error"]` 和 1 行“无法回答”；
  - 页面实际显示 `ANSWER READY`、`回答已生成`、`查询完成，共返回 1 行`，卡片状态为 `success`；
  - 页面还生成“导出当前结果”的待确认动作，虽未执行，但进一步把占位错误表现成可用查询结果；
  - 代表性截图和结构化响应保存在仓库外测试证据目录。
- 期望边界：
  - 已确认的权限或安全边界应返回 `rejected`；
  - 普通字段不存在或当前能力无法回答时应返回 `unsupported` 或明确失败；
  - 只有有效查询确实返回零行时才应使用安全空结果；
  - 不论最终选择哪一种非成功类型，都不应把“无法回答”占位文本包装成成功数据行。
- 源码直接证据：
  - `app/service.py` 已识别 `columns == ["error"]` 并跳过 Judge，但仍调用 `format_success`；
  - `app/core/assistant_orchestrator.py::_nl2sql` 只在旧响应存在 `legacy.error` 时转换为 `rejected/failed`；占位结果没有 `legacy.error`，因此进入成功分支；
  - `app/static/assistant-panel.js::stateFrom` 将 `result.status === "success"` 渲染为成功卡片。
- 环境证据边界：
  - 首轮沙箱内 14 次模型请求因本机套接字权限 `WinError 10013` 失败，只作为环境证据，不计入上述业务统计；
  - 允许网络后重新执行完整 16 次矩阵，未用首轮失败结果替代真实模型结论。
- 影响：
  - 用户会把“无法回答”误认为已经完成的数据查询；
  - 敏感问题没有采用产品契约中的拒绝状态，安全边界提示不清晰；
  - 缺失普通字段也存在同一响应语义错误，说明问题不限于一个敏感词。
- 获批最小修正：
  - `app/core/business_domains.py` 增加敏感身份标识问法识别；只覆盖身份证、证件号码及对应英文别名，不扩展为通用字段权限重构；
  - `app/service.py::ask` 在教学数据源调用模型前拒绝上述敏感身份请求；同时只把“唯一列为 `error`、唯一字符串行且命中无法回答/字段缺失语义”的严格占位结果转换为 `format_error`，有效空结果和普通业务错误保持原链路；
  - `app/core/assistant_orchestrator.py::_nl2sql` 将该占位错误转换为 `status=success, answer_type=unsupported`，不携带数据、SQL或动作；敏感身份请求继续转换为 `rejected`；
  - `app/static/assistant-panel.js` 和 CSS 增加 `unsupported` 状态，显示 `NOT SUPPORTED / 当前问题暂无法处理`，不再渲染成功表格；`index.html` 更新对应静态资源版本。
- 自动测试证据：
  - 修正前新增的 7 个目标用例全部失败，能够捕获原状态误报；
  - 修正后目标用例 7/7、问数/助手/前端/安全/权限相关回归 109/109；
  - 全量测试 283/283 通过，另有 2 条既有弃用警告；AST 只读语法检查 107 个 Python 文件通过。
- 修正后隔离真实接口：
  - 第一轮学生敏感请求正确拒绝、学生鞋码正确返回不支持、管理员敏感请求正确拒绝；管理员鞋码因外部模型达到 45 秒既有硬超时而返回结构化超时，未把它写成修复失败或通过；
  - 模型/检索缓存就绪后按相同两角色两问题重跑，4/4 通过：敏感请求均为 `rejected`，鞋码均为 `success/unsupported`，全部无数据、SQL、动作及 18 位身份证号码模式；
  - 敏感请求分别约 66 毫秒返回，证明在本次验证中没有调用模型生成结果。
- 修正后真实浏览器：
  - 学生敏感请求显示 `ACCESS DENIED`，鞋码显示 `NOT SUPPORTED`，两个用例均无表格、无结果网格、无身份证号码模式，2/2 通过；
  - 无 JavaScript 运行时异常；学生页面初始化访问管理员数据源接口仍产生 401/403 资源日志，已按原始 URL 如实保留，未混入 ZJH-011 结论。
- 数据与进程边界：
  - 原库修正后验证前后均为动作草稿 32、会话 30、轮次 64、执行轨迹 64；隔离副本最终为会话 42、轮次 76、执行轨迹 76；
  - 8019 服务和 Edge 9225 均已准确停止，含演示令牌的两个临时 Edge 用户目录已删除；
  - 证据位于 `D:\NL2SQL2\zjh011-fix-20260727`，报告不记录认证令牌或 `.env` 真实密钥。
- 潜在影响：敏感身份关键词会保守拒绝同词问法；普通缺失字段仍依赖模型先生成严格占位，外部模型本身仍可能超时。ZJH-003 集合查询、ZJH-008 Judge 和 ZJH-009 子查询范围没有随本批修改。
- 当前状态：`已整改并经实际测试验证`；详细修改及回退见 `ZJH-FIX-010`。

## 当前累计修改最终演示回归复核

- 执行日期：2026-07-27。
- 自动测试实际结果：
  - 环境依赖检查通过，`pip check` 未发现依赖冲突；
  - 当前工作区全量测试 **283/283** 通过，另有 2 条既有弃用警告；
  - 阶段 F 与六类演示业务流程定向回归 **22/22** 通过。
- 隔离真实 HTTP 实际结果：
  - 六角色允许/拒绝矩阵、未登录拒绝、角色切换旧令牌失效和新角色范围共 **8/8** 通过；
  - 作业提交/退回/重交/评分/发布、考勤、课程答疑、辅导员支持、学院与教务流程均包含在上述 22 项定向回归中。
- 隔离真实模型实际结果：
  - 固定 6 问且不自动重试，**6/6** 符合数据库基准或产品状态契约；
  - “我有哪些作业未完成”返回 1 行；同会话“按课程分组”返回 1 行；“只看已经截止的”返回 0 行；
  - 2026-12-31 日期边界问题返回 1 行；敏感身份问题为 `rejected`；缺失鞋码字段为 `unsupported`。
- 真实 Edge 实际结果：
  - 正常查询、敏感拒绝和缺失字段不支持三种页面状态 **3/3** 通过；
  - 没有 JavaScript 运行时异常，也没有检测到身份证号码模式；
  - 同时再次观察到普通角色页面初始化访问管理员数据接入接口产生 401/403，另行登记为 ZJH-012，不混入核心助手用例通过结论。
- 数据与进程边界：
  - 所有真实请求均使用原库在线备份副本；原库测试前后动作草稿 32、会话 30、轮次 64、执行轨迹 64、作业 4155、提交 328028、审计 0，计数完全一致；
  - 隔离副本只新增测试会话、轮次、轨迹、草稿及 1 条角色切换审计，作业和提交事实数未变；
  - 8020 服务和 Edge 9226 均已按准确 PID/浏览器会话停止，临时 Edge 用户目录已删除；
  - 证据位于仓库外 `D:\NL2SQL2\final-demo-regression-20260727`，不包含 `.env` 密钥或认证令牌。
- 结论边界：这是当前累计修改的演示范围回归，不代表生产级并发、安全、性能、多浏览器和外部系统验收全部完成。

## ZJH-012 普通角色页面初始化请求管理员数据接入接口

- 发现入口：当前真实 Edge 中以学生身份进入统一助手页面。
- 实际复现证据：
  - 未登录初始化阶段请求 `/api/data-access/sources` 返回 401；
  - 学生登录后再次请求同一管理员接口返回 403；
  - 该现象在 ZJH-011 浏览器验证和本次最终演示回归中分别出现，具备独立重复证据。
- 源码直接证据：
  - `app/static/index.html` 全局加载 `data-access-view.js`；
  - `app/static/data-access-view.js` 在 `DOMContentLoaded` 时为所有页面执行 `init()`；
  - `init()` 无条件调用 `loadSources()` 和 `loadAudit()`，其中 `loadSources()` 经前端 API 客户端访问 `/api/data-access/sources`；
  - 后端返回 401/403 说明权限拒绝本身有效，本问题是普通角色仍发起无必要的管理员接口请求。
- 影响：
  - 产生浏览器网络/资源错误噪声，容易干扰演示时的控制台与网络检查；
  - 当前助手查询、页面状态和角色权限仍按计划通过，没有证据表明该请求泄露管理员数据或阻断主要流程。
- 本次处理：
  - 按“先登记、后审批”和“非主要演示阻断问题暂缓”原则，只登记，不修改产品代码；
  - 不把后端正确拒绝写成前端初始化已经整改。
- 后续最小修正建议（尚未批准）：仅在管理员数据接入视图实际可见且当前角色具备相应权限时初始化数据源与审计请求，并补充普通角色不发起该请求的前端契约或浏览器回归。
- 当前状态：`未整改且已实际复现`。

## ZJH-013 外部数据源 Schema API 返回空结构

- 发现入口：外部 SQLite 与业务 PostgreSQL 双库隔离验收。
- 实际复现：
  - `/api/schema?source=library_sqlite` 返回 HTTP 200，但 `tables`、`columns`、`ddl` 全为空；
  - `/api/schema?source=library_pg` 表现相同；
  - 同一运行环境中两个源的扫描接口均正确返回 6 张表、24 列，固定源和自动路由 NL2SQL 也能读取结构并执行，因此不是数据库为空或连接失败。
- 源码直接证据：
  - `app/api/routes.py::get_schema` 对所有数据源调用 `filter_schema_info(load_schema(source), auth.allowed_tables, ...)`；
  - `auth.allowed_tables` 来自内置教学业务域，外部库的 `branch/book/loan` 等表不在该集合中；
  - `app/service.py::ask` 只在 `source == teaching` 时应用教学权限过滤，所以外部问数与 Schema API 出现不一致。
- 影响：
  - 管理员能连接和查询外部库，但 Schema 展示页得到空结构；
  - 可能影响外部库画像编辑、结构核对和演示可信度；
  - 当前没有证据表明它阻断固定源或自动路由 NL2SQL。
- 获批最小修正：
  - `app/api/routes.py::get_schema` 仅对内置 `teaching` 源应用教学业务域表白名单；
  - 外部源使用扫描得到的全部表作为允许表集合，再由既有 `filter_schema_info` 继续应用当前角色禁用字段和 Schema 画像 `blocked_columns`，没有放宽字段级过滤；
  - 未修改外部问数路由、教学源权限、数据库结构或画像格式。
- 修正后实际证据：
  - 新增自动回归验证外部源 2 张测试表均保留，而画像屏蔽列仍被删除；
  - 隔离真实验收中 `library_sqlite` 与 `library_pg` 的 `/api/schema` 均返回 6 张表、24 列，与各自扫描结果一致；
  - 同批外部闭环真实验收 13/13、相关自动测试 76/76、全量 291/291。
- 新增业务影响：管理员现可在页面化接入后直接核对外部 PostgreSQL Schema；教学源原有角色表白名单不变。
- 当前状态：`已整改并经实际测试验证`。

### 2026-07-29 真实浏览器补充验证

- 管理员从已注册 PostgreSQL 知识库进入当前可见 Schema 页面，实际显示 6 张表、24 个字段和 6 个业务对象；
- 本轮证明 ZJH-013 的外部 Schema 修正不仅在接口和自动测试中成立，也已在真实浏览器页面成立；
- 状态保持 `已整改并经实际测试验证`。

## ZJH-014 PostgreSQL 缺少页面/API 新增入口

- 发现入口：外部双库接入验收和运行时 OpenAPI 检查。
- 实际复现：
  - PostgreSQL 通过 `data_sources.yaml` 手工配置后，连接、扫描、只读和 6 条 NL2SQL 主流程均可执行；
  - `POST /api/data-access/sources/postgresql` 返回 HTTP 405；
  - OpenAPI 只列出 SQLite、CSV、数据库文件注册入口，没有 PostgreSQL 注册入口；
  - 当前前端数据维护页也只有 SQLite、CSV 和文件接入能力。
- 边界：
  - 这不是“项目完全不能查询 PostgreSQL”；手工配置路径已经实际通过；
  - 问题是用户无法像 SQLite 一样在页面/API 中新增 PostgreSQL；
  - 如果课程演示只要求配置文件接入，则该问题不阻断主流程；如果要求页面化新增，则功能不完整。
- 获批最小修正：
  - 新增管理员专用 `POST /api/data-access/sources/postgresql` 和前端 PostgreSQL 表单，学生调用实测仍为 403；
  - 注册请求只接收密码环境变量名，不接收真实密码；`data_sources.yaml` 仅保存无密码 URL 与 `password_env` 引用；
  - 运行时从进程环境或本地 `.env` 解析密码，连接错误和数据源列表均执行脱敏，不回显密码或环境变量名；
  - 注册前真实扫描数据库，PostgreSQL 源固定为只读，失败时只回退本次新增的注册项、画像和词表文件；
  - 不修改既有 SQLite/CSV/数据库文件入口，不把 PostgreSQL 开放为可写数据维护源。
- 修正后实际证据：
  - 新增 8 项专项回归覆盖管理员权限、请求模型不含密码、环境变量引用持久化、`.env` 解析、错误脱敏、精确回滚、外部 Schema 过滤和前端接线，8/8 通过；
  - 相关数据接入与安全测试 76/76、全量 291/291；
  - 隔离真实 PostgreSQL 注册返回 200，扫描 6 表24列；缺失密码环境变量返回 400 且没有持久化；学生注册返回 403；
  - 数据源列表与运行证据不含真实密码或 `password_env` 名称；维护接口和数据库账号写入均实际拒绝；
  - 外部 SQLite、PostgreSQL 各执行一条真实千问 NL2SQL，均返回数据库基准标量 3；闭环合计 13/13。
- 边界：
  - 真实密码仍需部署者事先放入服务进程环境或本地 `.env`，页面不会保存或显示密码；
  - 当前实现面向课程项目的单机配置文件注册，不等同于生产密钥管理、连接池治理或高并发验收。
- 2026-07-28 真实 Edge 复核补充：
  - 管理员实际打开数据维护页，SQLite/PostgreSQL/CSV 三个入口均可见，PostgreSQL 表单没有真实密码输入框；
  - 表单真实提交后 `POST /api/data-access/sources/postgresql` 返回 200，隔离注册表新增第 3 个源；
  - 随后的前端回调因访问失效的 `e.currentTarget` 抛出 `Cannot read properties of null (reading 'reset')`，页面把成功注册显示为失败；
  - 页面刷新后新源能够显示，说明后端注册成功，但用户可见闭环尚未完整；
  - 该表现另登记为 ZJH-018。
- 当前状态：`部分整改`。

### 2026-07-29 ZJH-018/019 联合修正后的闭环补充

- 真实 Chromium 浏览器中，管理员提交 PostgreSQL 表单后源列表为 3 项，外部源可见，表单字段已复位；前端不再把成功注册显示为失败；
- 页面刷新后外部 Schema 真实显示 6 表、24 字段；从该知识库点击“问数”后，请求上下文明确携带 `source=browser_pg_fix_20260729`；
- 隔离服务在允许真实 DashScope 网络的环境中完成 NL2SQL，响应状态为 `success`、路由为 `nl2sql`、证据源为该 PostgreSQL 源，逾期未归还统计返回数据库基准 3；
- 学生数据维护入口隐藏，直接调用 PostgreSQL 注册接口仍为 403；请求体未出现真实密码字段，证据报告未包含真实密码；
- ZJH-014 的页面/API/安全/Schema/问数闭环已经完成，最终状态更新为 `已整改并经实际测试验证`。

## ZJH-015 外部分组统计结果名称展示不稳定

- 发现入口：SQLite/PostgreSQL 各 6 条固定源真实模型矩阵及 2 条自动路由查询。
- 实际复现：
  - 固定源问题“统计每个分馆的图书数量，按分馆编号排序”在 SQLite 和 PostgreSQL 上都返回 `branch_id + book_count`；
  - 两库数量均为 `1=5、2=3、3=2`，与数据库标准答案一致，但结果没有分馆名称；
  - 自动路由问法在两库均生成与 `branch` 的连接并返回分馆名称和正确数量。
- 影响：
  - 数值事实正确，不属于数据库方言或执行失败；
  - 业务用户需要自行把编号映射为名称，演示可读性不足；
  - 同类问法随表达方式生成不同展示字段，说明结果稳定性仍有改进空间。
- 本批处理：按既定严格标准将两条记为失败，不放宽测试掩盖名称缺失；不修改提示词、画像或模型调用。
- 最小修正方向（尚未批准）：优先通过外部库画像/词表明确“按分馆”默认展示名称；是否需要通用提示词调整须先评估对其他数据源的影响。
- 当前状态：`未整改且已实际复现`。

## ZJH-016 外部 SQLite 外键未规范化导致关系图虚假节点和不完整关系

- 发现入口：官方 BIRD Mini-Dev `european_football_2` server 四路预热与关系图证据核对。
- 实际复现：
  - SQLite 只读清单为 7 张真实表、199 列、31 条外键定义；
  - `League.country_id` 的外键目标表由 SQLite/SQLAlchemy 返回小写 `country`，真实表名为 `Country`；
  - `Match.country_id` 和 `Match.league_id` 两个外键的目标列为空；
  - server 运行时关系图报告 8 个节点、33 条关系记录，而不是原始 7 表、31 外键；
  - 15/15 查询仍实际使用 graph 路，当前没有证据把某个错误答案确定归因为本问题。
- 源码直接证据：
  - `app/core/retrieval/graph.py::RelationGraph.build` 直接把 `referred_table` 加入图节点，不按真实表名做大小写规范化；
  - 只检查目标表是否为空，没有检查本地列/目标列是否为空；
  - 不完整外键仍进入 `self.fks`，后续可被渲染为含 `None` 的 JOIN 条件。
- 影响：
  - 大小写不一致的同一张表可能变成两个图节点，关系路径断开或产生虚假节点；
  - 缺失目标列的关系可能进入检索上下文并误导 JOIN；
  - 本次7表全部被召回，尚未证明该问题已经造成BIRD业务答案错误，因此不扩大结论。
- 本批处理：只登记，不修改产品关系图或BIRD数据库。
- 最小修正方向（尚未批准）：按 inspector 的真实表清单对目标表做不区分大小写的唯一映射；任一关系列为空时不加入可渲染 JOIN，并记录降级告警。
- 当前状态：`未整改且已实际复现`。

### 2026-07-29 获批最小修正与验证补充

- 修改 `app/core/retrieval/graph.py::RelationGraph.build`：
  - 先以 inspector 返回的真实表清单建立节点；
  - 表名和列名仅在大小写折叠后存在唯一匹配时映射到真实名称；
  - 本地表、目标表、本地列或目标列不存在/不唯一时跳过关系并写降级告警；
  - inspector 外键与 Schema 画像手工关系共用同一校验，不再由手工关系创建虚假节点；
  - 正向或反向等价关系不重复加入 `self.fks`。
- 自动回归：
  - 新增混合大小写目标表、空目标列、缺失表/列、正常外键和合法手工关系去重用例，新增用例 2/2；
  - 其余5条关系图/授权检索用例分别隔离通过；
  - 最终全量 **298/298**、0失败，JUnit 位于 `D:\NL2SQL2\zjh-016-fix-20260729\full.xml`。
- BIRD 实际验证：
  - 使用当前 `zjh-dev` 工作区实现读取原验收的 598MB `european_football_2`；
  - 图结构由历史 **8节点/33关系** 收敛为 **7个真实节点/31条有效关系**；
  - 节点中不存在虚假小写 `country`，关系中不存在 `None` 或不存在字段；
  - server 代表查询真实使用 `vector / keyword / glossary / graph` 四路；
  - 固定15题均可执行且15/15使用四路，课程阈值通过；严格EX为10/15，未达到历史原始11/15，故不能扩大写成整体查询质量无回归。
- 失败边界审查：
  - 1145、1040和1107仍是历史已知失败；
  - 1078因额外返回 `birthday` 归入既有 ZJH-017；
  - 1028首次生成SQL但忽略显式年份证据，另登记 ZJH-020；
  - 当前没有证据将这两项归因于关系图规范化，也没有为提高分数修改评估器或自动重试。
- 最终状态更新为：`已整改并经实际测试验证`；该状态只指 ZJH-016 的图结构兼容问题。

## ZJH-017 外部基准查询额外投影未要求指标列

- 发现入口：BIRD Mini-Dev 固定15题 server 实测。
- 实际复现：
  - 题号1145只要求4个联赛名称，生成结果返回相同4个联赛并额外返回比赛数；
  - 题号1040只要求10名球员姓名，生成结果返回相同10名球员并额外返回平均头球准确率；
  - 两题的业务集合均与数据库基准一致，但严格结果列数不一致，原始严格EX均记失败；
  - 两题各存在并列值，生成结果和Gold的并列顺序不同，题目未指定并列排序。
- 影响：
  - 不属于SQL无法执行或事实集合错误；
  - 用户看到未要求的辅助指标，结果不够精简；
  - 在严格执行结果评测中直接损失2题。
- 边界：
  - BIRD题号1107另经数据库核对为官方Gold与题意冲突，不归入本问题；
  - 题号1028是一次DashScope TLS EOF失败，也不归入本问题。
- 本批处理：原始EX保持11/15，不删除失败、不修改评估器、不修改提示词。
- 最小修正方向（尚未批准）：评估通用提示词中“SELECT仅返回用户明确要求字段”的最小约束，并先用现有教学和外部双库回归确认不会丢失必要解释列。
- 当前状态：`未整改且已实际复现`。

### 2026-07-29 ZJH-016 回归中的补充证据

- 固定题1078只要求回答年龄较大的球员姓名，本轮返回正确姓名 `Aaron Lennon`，同时额外投影 `birthday`；
- 该题因此按既定严格列数标准失败，表现与1145、1040同类，说明 ZJH-017 不只影响指标统计题；
- 本轮只补充实际证据，不修改提示词、SQL生成或评估器，当前状态保持 `未整改且已实际复现`。

## ZJH-018 PostgreSQL 注册成功后页面错误显示失败

- 发现入口：ZJH-013/014 修正后的外部数据源真实浏览器最终验收。
- 前置条件：管理员打开“数据维护”，选择 PostgreSQL 页签，填写主机、端口、库名、用户名、密码环境变量名和 SSL 模式。
- 实际复现：
  - 浏览器表单 HTML 校验通过；
  - 网络记录显示 `POST /api/data-access/sources/postgresql` 返回 HTTP 200；
  - 隔离 `data_sources.yaml` 随后由 2 个源增加为 3 个源，刷新页面后新源正常显示；
  - 页面提交当轮却显示 `Cannot read properties of null (reading 'reset')`，没有显示成功提示。
- 源码直接证据：
  - `app/static/data-access-view.js` 的异步 `submit` 监听器在 `await api.registerPostgresqlSource(...)` 后调用 `e.currentTarget.reset()`；
  - DOM 事件回调跨过 `await` 后，`event.currentTarget` 已被清空为 `null`；
  - 该空引用被同一 `catch` 捕获并作为接入失败消息展示，因此后端成功和前端状态不一致。
- 影响：
  - 数据源实际已经接入，但管理员会误以为失败并可能重复提交；
  - 重复提交会遇到名称或配套文件已存在，影响课程演示可信度；
  - 不影响后端注册、密钥外置、只读、刷新后源列表和 Schema 读取。
- 本批处理：只复现和登记，不修改前端实现。
- 最小修正方向（尚未批准）：在进入异步调用前保存 `const form = e.currentTarget`，成功后使用该稳定引用执行 `reset()`，并以真实响应和刷新后的源列表验证成功状态。
- 当前状态：`出现回归`。

### 2026-07-29 获批最小修正与验证补充

- 修改 `app/static/data-access-view.js`：进入异步提交前保存 `const form = e.currentTarget`，成功后使用稳定引用 `form.reset()`；
- 修改 `app/static/index.html`：更新 `data-access-view.js` 缓存标识，避免浏览器继续加载旧回调；
- 自动回归覆盖稳定表单引用和旧空引用移除；相关自动测试 55/55、全量 296/296 通过；
- 真实浏览器证据：注册请求成功，源列表为 3 项、目标源可见、`form_reset=true`，页面无 `reset` 空引用；
- 最终状态更新为 `已整改并经实际测试验证`。

## ZJH-019 选择外部知识库后统一助手仍查询教学库

- 发现入口：已注册 PostgreSQL 的真实 Edge 页面，从“问数知识库”选择外部源并点击“问数”。
- 实际复现：
  - 页面刷新后外部源 `browser_pg_20260728` 正常出现在数据源和知识库列表；
  - 管理员点击该源的“问数”，在可见统一助手输入“截至2026年7月1日，仍未归还且已经逾期的借阅共有多少条？”；
  - 响应状态为 `success`，但证据源为 `teaching`，路由为 `deterministic_metric`；
  - 页面返回“业务表 68、待处理质量项 0、数据源状态正常”三行管理员工作台统计，没有执行外部 PostgreSQL 查询，也没有返回数据库基准 3；
  - 同一外部 PostgreSQL 已在后端固定源真实模型测试中返回基准 3，因此本次不是数据库不可达或模型额度问题。
- 源码直接证据：
  - `app/static/app.js::renderKbList` 的“问数”按钮先调用 `setManualSource(row.name)`，随后打开统一助手；
  - `app/static/assistant-panel.js::ask` 的请求只提交问题、页面上下文和选项，没有提交所选数据源；
  - `app/core/assistant_orchestrator.py::_nl2sql` 当前固定使用 `source="teaching"` 和 `current_source="teaching"`；
  - 旧问数壳仍有手动数据源逻辑，但该壳在当前页面被隐藏，不能作为可见业务入口通过。
- 影响：
  - 管理员可以接入、扫描和查看外部 Schema，却不能通过当前可见统一助手完成外部库问数；
  - 直接影响“导入自己的数据库并在页面问数”的核心课程演示要求；
  - 返回的是合法教学统计而非明确错误，用户更容易误认为正在查询外部库。
- 本批处理：只复现和登记，不修改前后端调用链。
- 最小修正方向（尚未批准）：由服务端接收并重新鉴权外部 `source` 上下文，统一助手前端显式展示锁定源；不得仅在浏览器本地切换变量，也不得绕过外部源角色权限。
- 当前状态：`未整改且已实际复现`。

### 2026-07-29 获批最小修正与验证补充

- 前端 `app.js` 为知识库“问数”、活动重放和收藏样例入口统一加入服务端可验证的 `source` 上下文；
- `assistant_context.py` 只允许管理员选择注册表中存在的外部源，普通角色和未知源均拒绝，错误不枚举可用外部源；
- `assistant_orchestrator.py` 以已验证上下文源执行 NL2SQL；外部源跳过仅属于教学库的确定性指标/工作台路由，不继承教学表白名单或教学行范围；
- 同一助手会话不能从一个源切换到另一个源，避免会话历史跨库混用；教学源原有权限和确定性业务路由保持不变；
- 自动验证：外部源上下文、普通角色拒绝、会话锁源、前端传参与编排器转发均通过；相关自动测试 55/55、全量 296/296；
- 真实浏览器从外部知识库进入统一助手后，网络请求携带目标源；真实千问响应为 `success/nl2sql`，证据源为 `browser_pg_fix_20260729`，结果标量为数据库基准 3；学生注册接口仍为 403；
- 最终状态更新为 `已整改并经实际测试验证`。

## ZJH-020 外部基准证据中的年份映射未稳定遵循

- 发现入口：ZJH-016 修正后的 BIRD Mini-Dev 固定15题单次真实模型回归。
- 实际复现：
  - 题号1028询问 Scotland Premier League 在“2010 season”客场获胜最多的球队；
  - 本次请求明确提供原 BIRD evidence：`2010 season refers to season = '2009/2010'`；
  - 生成 SQL 使用 `m.season = '2010/2011'`，返回 `Rangers`；
  - Gold 及 evidence 的 `2009/2010` 口径返回 `Celtic`；
  - SQL可执行，四路检索均使用，目标表 `Match / League / Team` 已召回，错误不属于数据库不可达或 SQL 执行失败。
- 证据边界：
  - 原2026-07-28固定矩阵中该题在模型阶段发生一次 TLS EOF，未生成SQL，因此本次是该语义偏差的首次实际复现；
  - 当前只有一次固定请求证据，尚未证明每次都会错误；
  - 关系图修正只规范化表/列和过滤无效关系，没有修改用户证据、年份解释、提示词或模型参数，当前没有证据将该偏差归因于 ZJH-016。
- 影响：外部数据集中带专用口径证据的年份/赛季映射可能被模型忽略，影响严格事实答案。
- 本批处理：只登记，不自动重试，不修改提示词或模型调用；修正必须另行审查并取得批准。
- 当前状态：`未整改且已实际复现`。

## ZJH-021 PostgreSQL 数据源无法在数据维护页只读预览

- 发现入口：2026-07-29 外部 SQLite 与业务 PostgreSQL 实际结果表复核。
- 前置条件：
  - 管理员已通过页面/API 注册只读 PostgreSQL 数据源；
  - 同一数据源已真实扫描 6 表24列，数据源列表可见且 NL2SQL 能够读取；
  - 业务表 `book` 的数据库只读基准为10行。
- 修正前实际复现：
  - 外部 SQLite 的 `GET /api/data-access/sources/{source}/tables/book/rows` 返回 HTTP 200、`total=10`；
  - 业务 PostgreSQL 的同一路径返回 HTTP 400，错误为“当前数据维护仅支持 SQLite 数据源”；
  - 同一 PostgreSQL 的注册、扫描、Schema 和数据库只读查询均正常，因此不是容器、密码、连接或数据库为空。
- 源码直接证据：
  - `app/static/data-access-view.js::loadTable` 对当前数据源统一调用 `api.tableRows`；
  - `app/api/data_access.py::rows` 对所有方言统一调用 `app.core.data_access.table_rows`；
  - `table_rows` 固定调用 `_sqlite_path_from_url`、`sqlite3.connect` 和 `PRAGMA table_info`；
  - `_sqlite_path_from_url` 对非 `sqlite:///` URL 主动抛出上述错误，没有 PostgreSQL 读取分支。
- 影响：
  - 管理员能够接入和问询 PostgreSQL，但不能在“数据维护”页面核对表内实际行；
  - 该问题不影响 PostgreSQL 注册、Schema、外部问数和数据库账号只读约束；
  - 如果课程演示要求页面浏览外部 PostgreSQL 数据，则当前闭环不完整。
- 2026-07-29 用户批准的最小修正范围：
  - 只为 `table_rows` 增加 PostgreSQL 专用 SQLAlchemy 只读分支，SQLite 原路径保持不变；
  - 复用现有 `default_transaction_read_only=on` 引擎、管理员鉴权、路由和响应结构；
  - PostgreSQL 页面只显示数据，不开放新增、保存或删除；
  - 不修改注册、Schema、NL2SQL、server、提示词、数据库和 SQLite 写入功能。
- 实际修改：
  - `app/core/data_access.py` 新增 `_postgresql_table_rows`，先用 inspector 白名单确认表存在，再用方言标识符引用、参数化 `LIMIT/OFFSET` 和既有只读引擎执行 `COUNT + SELECT`；`table_rows` 仅在方言为 PostgreSQL 时进入该分支；
  - `app/static/data-access-view.js` 将 PostgreSQL 行渲染为纯文本单元格，隐藏“新增记录”并移除“操作”、保存和删除控件；SQLite 仍渲染输入框和原维护操作；
  - `app/static/index.html` 只更新数据维护脚本缓存标识；
  - `tests/test_external_data_access.py` 增加 PostgreSQL 分页/不存在表、管理员权限、禁止写入以及 SQLite 原契约对照。
- 修正后自动验证：
  - 数据接入专项 JUnit 为 12/12，0 失败、0 错误、0 跳过；
  - 全量 JUnit 为 302/302，0 失败、0 错误、0 跳过，测试本体耗时 355.063 秒；
  - 两次 pytest 在 JUnit 已完整写出后没有自行退出，按第14条核对结果并终止精确 PID；这是测试运行器退出阶段挂起，未把它隐瞒为正常退出，也未据此否定已经写入的逐用例结果；
  - JavaScript 语法检查和 `git diff --check` 通过。
- 修正后真实 API 验证：
  - 隔离注册 SQLite 和 PostgreSQL 对照源；PostgreSQL 真实扫描为6表24列；
  - PostgreSQL `book` 预览返回 HTTP 200，数据库与接口 `total=10`，`limit=3`、`offset=1` 返回3行且列集合正确；
  - SQLite 对照预览仍为 HTTP 200、`total=10`；
  - 学生访问 PostgreSQL 预览返回403，不存在表返回400且错误不含密钥；
  - PostgreSQL 新增、修改、删除三类接口均返回400，数据库会话 `default_transaction_read_only=on`；
  - 九项结构化 API 检查为9/9，未把真实密码写入项目或报告。
- 修正后真实 Edge 验证：
  - 当前页面实际显示 PostgreSQL `book` 共10条，本页10条，首行为 `101 / Python编程基础 / 1 / 1 / 2024`；
  - PostgreSQL 表格输入框、保存按钮、删除按钮和新增记录入口均为0/不可见；
  - SQLite 对照仍有“操作”列、50个字段输入框和可见新增按钮；
  - 两个表预览网络请求均为 HTTP 200，控制台错误为0；
  - 首轮截图发现 `hidden` 属性被页面样式覆盖、灰色新增按钮仍可见；随后只补充显式 `display:none` 并重新执行完整浏览器矩阵，最终截图确认入口消失，没有把首轮代码断言误写成视觉通过。
- 未修改与影响边界：
  - 未修改 API 路由、管理员鉴权、PostgreSQL 注册/Schema/NL2SQL/server、提示词、数据库结构或数据；
  - 未给 PostgreSQL 增加任何写能力；SQLite 可写源的新增、编辑和删除仍保持原实现；
  - 预览仍按现有接口最多返回200行，不新增排序、搜索、导出、Schema选择或工业级分页功能。
- 当前状态：`已整改并经实际测试验证`；详细修改、调用链、潜在影响和精确回退见 `PROJECT_CHANGES.md` 的 `ZJH-FIX-014`。

## 尚待验证项

当前没有仍未收口的待验证项；新增线索仍须按本文记录规则取得实际证据后再编号。

## 已收口的待验证项

### 待验证 A：敏感字段问题的响应类型

- 已通过学生/管理员、四类问法、两次独立会话的 16 次真实模型矩阵完成复现；
- 已通过真实 Edge 验证统一助手前端确实把 `error` 占位行显示为成功；
- 已确认没有身份证号码或其他敏感事实泄露；
- 已确认产品问题是“无法回答占位结果被误标为成功”，并不限于敏感字段；
- 本待验证项转为正式问题 `ZJH-011`，状态为 `未整改且已实际复现`。

### 待验证 B：旧管理员教学驾驶舱筛选

- 修正前的证据边界：
  - 源码和自动测试覆盖了旧 `/api/teaching/dashboard` 的部分筛选与管理员权限；
  - 此前没有确认当前 `zjh-dev` 页面是否仍然实际展示和调用旧筛选器，因此既不能写成“筛选错误”，也不能写成“浏览器验收全部通过”。
- 源码直接证据：
  - `app/static/index.html` 中旧 `.dashboard-filter-panel` 带 `hidden` 和 `aria-hidden="true"`；
  - `app/static/app.js` 前部保留了调用 `api.teachingDashboard(filters)` 的旧 `loadDashboard`，但同一作用域后部重新定义的 `loadDashboard` 改为调用 `api.workbench()`，后定义是当前实际入口；
  - `app/api/routes.py` 注释明确说明旧全校驾驶舱接口只为平台管理员保留，业务角色使用新的范围化工作台；
  - `tests/test_stage_b_workbench.py` 只把旧接口作为管理员专属兼容接口验证，并确认六角色首页不再使用旧共享驾驶舱。
- 真实浏览器验证：
  - 使用当前工作区代码、原库在线备份、独立 8018 服务及临时配置目录的无头 Microsoft Edge；
  - 管理员实际进入“运维首页”，角色标签为“校级管理员”，页面显示 3 张摘要卡、1 个工作区段和 2 个快捷操作；
  - 旧筛选面板实际为 `dashboard-filter-panel hidden`、`aria-hidden=true`、计算样式 `display:none`；
  - 浏览器网络实际请求 `/api/workbench`，没有请求 `/api/teaching/dashboard`；
  - 本地请求没有 4xx/5xx，控制台没有错误；
  - 直接兼容接口验证为管理员 200、学生 403，说明旧后端仍存活但不是当前页面功能。
- 数据与进程边界：
  - 验证使用 `D:\NL2SQL2\dashboard-browser-closeout-20260727\teaching-isolated.db`；
  - Edge 使用仓库外临时用户目录，结束时 9224 已关闭且临时用户目录已删除；8018 隔离服务也已准确停止；
  - 报告和截图保存在同一仓库外目录，不含认证令牌或真实密钥。
- 结论：
  - 当前管理员页面不存在可供逐项验收的旧教学驾驶舱筛选功能；页面已被角色工作台替代；
  - 本项不是已经实际复现的筛选缺陷，也不能把旧后端接口存活写成旧筛选页面仍然可用；
  - 当前状态：`不再适用`。若未来重新开放旧筛选面板，应作为新的独立功能重新测试，而不是复用本结论。

## 外部测试状态

- DashScope 额度已于 2026-07-26 恢复。
- 恢复后已使用全新临时数据库继续完成追问、反向、日期、安全、教师和专业查询。
- 额度耗尽期间产生的 403 仍不计入产品缺陷或稳定性统计。
- 2026-07-27 ZJH-005 实测时模型配置与健康检查正常；其中一条教师列表请求达到统一助手总超时，该超时作为真实运行结果记录，不归因额度不足。
