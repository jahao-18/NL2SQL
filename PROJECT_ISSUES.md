# 项目问题记录

> 仅记录已发现且需要跟踪的问题。问题变化时更新“更新时间、状态、说明”。

| 编号 | 发现/更新时间 | 文件与位置 | 问题情况 | 影响功能 | 状态 |
|---|---|---|---|---|---|
| ISSUE-001 | 2026-07-13 / 更新 2026-07-14 | `.env`、`.venv`、`requirements.txt` | `.venv` 依赖、`.env` 和 DashScope Key 均已补齐；依赖、应用导入、服务启动和模型调用通过 | 原环境阻塞已解除 | 已解决 |
| ISSUE-002 | 2026-07-13 / 修正并验证 2026-07-14 | `tests/test_governance.py::test_publish_review_is_approved_before_publish`；`app/core/governance.py::accept_review_item` | 测试虚构 `demo` 数据源并替换画像读取和发布，却未隔离接受审查时真实发生的画像保存，因而触发未知数据源。现已增加保存替身，并断言正确快照按 `save → publish` 顺序处理 | 治理测试现在隔离外部数据源，同时实际验证接受审查的保存与发布顺序 | 已修正；治理专项3/3、完整 pytest 64/64通过，未修改治理实现 |
| ISSUE-003 | 2026-07-13 / 修正并验证 2026-07-14 | `scripts/eval.py::load_cases/_normalize_cell/run_case`；`tests/teaching_eval_cases.yaml`；`tests/test_eval_runner.py` | 修改前教学集使用不受脚本支持的 `contains_sql`，顶层 `source` 未继承，且比较器实际把 `1` 与 `1.0` 判为不等。现已改为8条经真实数据库验证的标准 SQL 结果比较，继承顶层数据源、精确规整数值并检查200行截断状态 | 教学评测现可自动运行，并能发现关键词检查遗漏的空结果、口径、连接方式和截断问题 | 已修正；标准 SQL 8/8 可执行，受控测试 6/6，通过真实模型 7/8；剩余真实查询失败见 ISSUE-014 |
| ISSUE-004 | 2026-07-13 | `tests/eval_cases.yaml`；`scripts/seed_db.py`；`data_sources.yaml` | 通用评测面向电商库，生成脚本输出 `data/app.db`，但当前数据库不存在且未明确注册 `demo_sqlite` | 通用查询评测可能使用错误数据源或无法执行 | 待确认原评测方案 |
| ISSUE-005 | 2026-07-13 | `app/main.py`；`app/core/chain.py`；测试依赖 | 存在 FastAPI `on_event`、LangChain Community、TestClient/httpx 弃用警告 | 当前不阻塞运行，未来升级可能出现兼容问题 | 低优先级观察 |
| ISSUE-006 | 2026-07-13 22:09 / 更新 2026-07-14 | `app/core/business_domains.py::AuthenticationError/user_from_token`；`app/main.py::_authentication_error_handler`；`tests/test_authentication.py` | 修改前实测缺失和未知令牌的 session 均返回 admin；现已删除管理员回退并统一映射为 HTTP 401。修改后通过单元、TestClient 和临时 Uvicorn 真实 HTTP 复测 | 未登录和未知令牌不再获得管理员身份；合法角色及正常问数保持可用 | 已修正并通过实际运行验证；演示令牌边界见 CHG-20260714-03 |
| ISSUE-007 | 2026-07-13 22:09 / 更新 2026-07-14 19:11 | `app/core/business_domains.py::require_feature`；`app/main.py::_authorization_error_handler`；`app/api/routes.py`；`app/api/governance.py`；`tests/test_authentication.py` | 007A 已修正 Profile、版本、质量和治理接口。007B 修改前受控 TestClient 实测无令牌和学生对反馈、示例六类读写请求均返回 200 并进入内存替身。现已为反馈创建/删除要求 `ask`、反馈列表要求 `knowledge`、示例读取要求 `ask`、示例新增/删除要求 `knowledge`；授权发生在存储调用之前 | 匿名用户不能再读写反馈或示例；普通问数角色保留反馈提交/撤销和示例读取，知识管理角色保留列表及示例维护；标准示例不能再被匿名或学生改写后影响 SQL 生成 | 已按本地演示项目边界解决并通过实际回归；严格反馈创建者所有权未扩展，见 CHG-20260714-06 边界说明 |
| ISSUE-008 | 2026-07-13 22:32 / 实测 2026-07-14 19:49 / 修正 2026-07-14 | `app/api/data_access.py`；`app/core/data_access.py::table_rows/insert_row/update_row/delete_row`；`tests/test_data_access_permissions.py` | 修改前教师可取得 12 个数据源、非空 URL、教学库全部 17 张表，扫描/读取 `student` 时暴露身份字段和 8926 行总数；内存替身证明教师增删改学生记录及读取身份审计均为 200。现已过滤数据源、扫描、行和审计返回，并在 SQL/事务内强制允许表、禁用字段及 `teacher_id/college_id` 直接范围；无直接范围字段的单表保守拒绝 | 教师不再绕过问数权限读取学生身份或跨范围维护；学院只得到本学院直接范围，教务禁用字段被移除，非管理员 URL 被隐藏，管理员保持完整访问 | 已按本地项目边界修正；专项、完整 pytest、临时 Uvicorn 权限矩阵和合法学生问数均已实际回归 |
| ISSUE-009 | 2026-07-13 22:20 / 更新 2026-07-14 13:01 | `app/core/business_domains.py::row_scope_context`；`app/api/routes.py::ask`；`app/service.py`；`app/core/validator.py` | 已增加生成后、执行前的行级范围校验。真实回归中首轮全局平均 SQL 被拒绝，现有修复链补为 `enrollment.student_id = 1` 后执行 | 学生模糊问法不再执行全局统计；查询执行层风险已解除 | 已修正并通过回归；裁判理解偏差另见 ISSUE-013 |
| ISSUE-010 | 2026-07-13 22:32 / 浏览器实测更新 2026-07-15 17:45 后 | `app/api/routes.py::list_sources/get_schema`；`app/static/app.js::loadAllSchemas`；`data_sources.yaml`；`scripts/eval_bird.py`；`data/bird/` | 数据源列表包含未下载的 BIRD 库；五角色浏览器登录时，前端实际并发请求11个 BIRD Schema 并全部返回500，服务端同时记录 `unable to open database file`；教学源 Schema、驾驶舱和问数请求仍返回200。项目已有 BIRD 适配脚本，但本地无 `data/bird/`、`dev.json` 和 BIRD 数据库，尚不具备真实运行验证条件 | 不阻塞教学库主流程，但登录 Network 和服务日志持续出现失败请求；BIRD 本身尚不可查询 | 已建立 P-12 必做标记；取得数据后只执行单库5～10题，未测试前不得写成已接入或已通过 |
| ISSUE-011 | 2026-07-13 | `app/static/data-access-view.js` 表格加载及增删改事件 | 部分异步操作没有统一 `try/catch`；请求失败时可能出现未处理 Promise 异常 | 数据维护失败时页面缺少明确反馈；不影响智能问数主流程 | 中低优先级待处理 |
| ISSUE-012 | 2026-07-14 / 更新 2026-07-14 13:01 | `app/core/retrieval/atoms.py::_ENUM_MAX`；`tests/test_retrieval_context.py`；`tests/teaching_eval_cases.yaml::teaching_003` | 根因为完整 Schema 枚举上限 20、检索原子上限 12 不一致，18 个专业名称在检索模式被全部丢弃；已将检索上限对齐为 20 | 检索上下文现包含“软件工程”，真实回归正确过滤并返回 5 行 | 已修正并通过单元及真实问数回归 |
| ISSUE-013 | 2026-07-14 13:01 / 修正并验证 2026-07-14 | `app/core/judge.py::_scope_context/judge/stash`；`app/service.py::ask`；`prompts/judge_prompt.txt`；`tests/test_judge_scope.py` | 修改前裁判没有收到 `role_label/row_scope`，把学生、教师的强制本人范围误判为多余过滤；学生示例总分56、正确性40。现已把可信权限范围作为独立上下文传给裁判，并要求只豁免系统确认的范围条件 | 正确范围查询不再显示错误低可信徽章或制造虚假低可信治理任务；查询执行行为不变 | 已最小修正；受控测试4/4，真实学生可信度95/正确性95，教师可信度93/正确性92，完整 pytest 68/68通过 |
| ISSUE-014 | 2026-07-14 / 修正并验证 2026-07-14 | `prompts/sql_prompt.txt` 教学口径第6条；`tests/teaching_eval_cases.yaml::teaching_004` | 修改前真实模型两轮均为“每位教师”生成 `JOIN teaching_class`；数据库实测240名教师中有1名教师（ID 94）没有授课班，内连接会将其遗漏。现已明确要求以教师为主表左连接授课班和选课，使用明细主键计数并保留零记录教师 | “统计每位教师”现能包含没有授课班的教师，并保持200行安全截断提示 | 已最小修正；第4题两次独立真实模型测试及一次8题回归中的第4题均通过，完整教学评测8/8 |
| ISSUE-015 | 2026-07-15 16:25 / 学院修正 16:54 / 专业口径二次实测并透明化 / 第三次浏览器验收通过 2026-07-15 | `app/core/teaching_dashboard.py::_apply_filters/teaching_dashboard`；`app/static/app.js::renderDashboard`；教学库 `course/teaching_class/enrollment/student` | 学院开课数原错误按学生学院统计，已最小修正为课程所属学院。二次浏览器测试发现固定学期和学院后，切换任一专业的学期开课数仍等于全部专业。随后真实API矩阵验证：2025春季6学院18专业、2025秋季6学院18专业，共36/36组专业开课数均等于学院开课数，但专业选课数均随专业变化。数据库直接证据显示 `course` 只有 `college_id`、`teaching_class` 只有 `course_id`，不存在专业—课程/教学班归属关系；现有专业条件只能按选课学生的 `student.major_id` 间接限定，而数据中每个专业均覆盖所属学院全部教学班。现已在专业筛选时将卡片标为“学院开课”（未选学院时为“专业参与开课”），并提示按专业学生选课覆盖统计、不是课程归属 | 学院口径正确；页面不再把当前库无法提供的“课程所属专业开课数”伪装成独立统计。选课、成绩等有学生粒度的指标仍按专业过滤 | 已解决：学院测试修改前2失败、修改后2通过；专业矩阵36/36确认模型限制；静态入口1/1、完整pytest 97/97、JS语法和真实HTTP通过；用户第三次浏览器复验确认专业口径提示正常 |
| ISSUE-016 | 2026-07-15 浏览器实测 / 二次失败 / 缓存版本修正 / 第三次浏览器验收通过 2026-07-15 | `app/static/app.js::resetAssistantSession`；`app/static/index.html` 的 `app.js`版本参数；`tests/test_authentication.py::test_frontend_entrypoint_serves_current_role_isolation_script` | 首次修改已新增统一清理函数，但二次浏览器测试中切换管理员到学生后，上一角色最后一条输入和结果仍显示。服务器真实HTTP读取确认脚本已包含清理函数，而首页仍引用修改前后的同一 `/app.js?v=p0-product-3`；表现与旧脚本一致。现将入口更新为 `/app.js?v=p0-product-4`，强制浏览器取得包含清理函数的新资源；不归因后端历史（学生新请求仍为 `history=[]`） | 新角色页面与上一角色的可见问数状态隔离；后端认证、模型历史和行级范围未改 | 已解决：入口测试及真实HTTP确认p4和清理函数，完整pytest 97/97、JS语法通过；用户第三次浏览器复验确认切换角色后上一角色查询内容和结果不再显示 |
| ISSUE-017 | 2026-07-15 学生浏览器实测 / 二次失败 / 缓存版本修正 / 第三次浏览器验收通过 2026-07-15 | `app/static/app.js::loadSchema`；`app/static/index.html` 的 `app.js`版本参数；`tests/test_authentication.py::test_frontend_entrypoint_serves_current_role_isolation_script`；`app/api/routes.py::get_quality/get_profile_versions` | 首次修改已将质量/Profile版本加载限制在 `hasFeature("knowledge")`，但二次浏览器仍出现原两个403。真实登录确认学生features仅有 `ask/domain_settings/role_management`、不含knowledge；首页仍引用p3，故证据指向旧脚本缓存。现更新为p4，入口测试同时验证新脚本包含knowledge分支；后端403拒绝保持不变 | 学生前端不再主动请求无权知识管理资源；未通过放宽后端权限消除红色请求 | 已解决：入口测试及真实HTTP确认p4与权限分支，完整pytest 97/97、JS语法通过；用户第三次浏览器复验确认学生问数不再出现原quality/profile versions两个403 |
| ISSUE-018 | 2026-07-15 19:19 首次浏览器发现 / 同题三轮与冷门题复核 2026-07-15 | `app/core/judge.py::judge/_result_preview`；`app/service.py::ask` 裁判入参；检索上下文；`prompts/judge_prompt.txt` | 首次询问“软件工程课程有多少名同学满分”时，正确SQL和结果0曾被裁判误评为`final=33/retrieval=40/correctness=30`，并产生错误低可信治理任务。随后管理员空历史、同数据源连续三轮真实复测：SQL完全一致、结果均为0，裁判分别为93/92/92，召回均95、正确性92/90/90；独立SQLite确认课程存在且正确答案为0。Debug检索实际命中`course/score/enrollment/teaching_class`等表，前2000字符已包含`score.final_score`，排除字段缺失和4000字符截断。另测冷门问题“2025年春季尚未解除、风险分不低于80的考勤风险预警数”，结果104与独立SQL一致，裁判96；生成SQL额外限制在读学生，但数据库中被该条件排除的记录为0，未造成当前结果错误 | 证明裁判曾真实发生一次假阴性，但后续四次不同/相同问题均高分，未形成稳定可复现的实现缺陷；查询执行始终正确。额外在读条件是跨数据集需关注的潜在口径风险，当前没有实际功能影响 | 已按模型偶发波动、非阻塞项收束，暂不修改检索、Prompt或裁判代码；若以后在上下文完整时连续稳定低分，或附加在读条件实际改变结果，再重新打开并申请最小修正 |
