# NL2SQL 智能问数项目汇报

> 汇报重点: 项目整体能力建设 + Schema 知识库构建细化  
> 当前阶段: 核心问数链路已跑通，知识库治理、召回检索、评测闭环和前端产品化能力持续完善中

---

## 一、当前项目总体进展

当前项目已经完成从“单次 NL2SQL Demo”到“可配置、可治理、可评测的智能问数系统”的阶段性升级。

目前系统已经具备以下主链路能力:

1. 多数据源注册与自动路由
2. Schema 自动读取与结构化知识库构建
3. 四路 Schema 召回与上下文裁剪
4. 大模型 SQL 生成
5. SQL 安全校验与失败回修
6. 查询执行与结果格式化
7. 结果解释与可信度评估
8. 标准问法沉淀与用户反馈闭环
9. 前端知识库工作台配置与发布

项目现在不是单纯把自然语言转成 SQL，而是在围绕“问数准确率、可维护性、可解释性、安全性、可评测性”做完整工程化闭环。

---

## 二、一次问数的完整流程

当前一次用户提问的执行链路如下:

```text
用户输入自然语言问题
  ↓
前端携带当前数据源、历史对话、业务术语、few-shot 样例
  ↓
后端判断数据源
  ↓
加载数据库 Schema + Schema Profile + 业务词表
  ↓
判断是否需要走 Schema 检索
  ↓
四路召回相关表、字段、关系和业务规则
  ↓
生成精简 Schema 上下文
  ↓
大模型生成 SQL
  ↓
SQL 安全校验
  ↓
只读连接执行 SQL
  ↓
失败则携带错误信息回修 SQL
  ↓
返回结果表格、SQL、解释、追踪信息
  ↓
后台异步评估可信度
  ↓
用户确认正确或反馈错误，进入知识库治理闭环
```

这个流程里，Schema 知识库不是单独存在的配置文件，而是贯穿数据源路由、Schema 检索、SQL 生成、安全校验、解释和反馈的核心上下文。

---

## 三、核心模块设计

### 3.1 多数据源注册与自动路由

系统通过 `data_sources.yaml` 注册多个数据源，每个数据源包含:

- 数据源名称
- 展示标签
- 数据库连接 URL
- 业务词表路径
- Schema Profile 路径

后端启动后按数据源名称统一管理连接。当前支持 SQLite 和 PostgreSQL 类型的数据源。

在自动模式下，用户不需要先选择数据库。系统会根据:

- 当前问题
- 最近对话历史
- 当前会话数据源
- 数据源描述

自动判断应该查询哪个库。

同时保留手动锁定数据源能力，适合测试、调试和业务人员明确知道要查哪个库的场景。

### 3.2 Schema 加载

Schema 加载模块负责读取数据库结构，并产出两类内容:

1. 给模型使用的 Schema 上下文
2. 给前端展示和配置使用的结构化字段元数据

SQLite 场景下读取 `sqlite_master` 中的建表语句。  
PostgreSQL 等通用数据库场景下使用 SQLAlchemy inspect 读取表、字段、类型、主键、外键和注释。

近期还修复了 SQLite DDL 字段解析兼容问题，支持反引号、双引号、方括号字段名，例如:

```sql
`atom_id` TEXT
"atom_id" TEXT
[atom_id] TEXT
atom_id TEXT
```

这保证类似 BIRD toxicology 这类数据集能够正确解析字段，不会出现“有表但字段数为 0”的问题。

### 3.3 SQL 生成

SQL 生成由大模型完成，但不是直接把用户问题交给模型，而是将以下上下文一起注入:

- 数据库方言
- 当前日期
- 精简后的相关 Schema
- 表与字段业务语义
- 默认过滤规则
- 指标口径
- 人工 JOIN 关系
- 用户自定义术语
- few-shot 标准问法
- 多轮历史

模型输出后，系统会提取 SQL 并进入安全校验。

### 3.4 SQL 安全校验

SQL 校验器负责拦截高风险或不符合约束的 SQL。当前主要包括:

- 只允许 SELECT
- 拦截 INSERT、UPDATE、DELETE、DROP 等写操作
- 单条 SQL 限制
- 表白名单校验
- 禁用字段校验
- 强制 LIMIT
- 查询超时保护

这部分保证系统即使接入真实业务库，也不会执行危险操作。

### 3.5 执行与失败回修

SQL 通过只读连接执行。  
如果执行失败，系统会将:

- 用户问题
- 上一次 SQL
- 数据库报错信息
- Schema 上下文

重新交给模型进行 SQL 修复，最多尝试有限轮次，避免无限重试。

---

## 四、Schema 知识库构建设计

Schema 知识库是本阶段重点建设内容。它的目标不是简单展示数据库表结构，而是把数据库结构转化为业务可理解、可治理、可发布、可回滚的知识资产。

当前 Schema 知识库由以下几类资产组成:

| 资产 | 作用 |
|---|---|
| 数据库原始 Schema | 自动读取表、字段、类型、主键、外键 |
| Schema Profile | 维护表画像、字段语义、默认过滤、字段可用性 |
| Glossary 业务词表 | 维护业务术语、取值解释、自然语言映射 |
| 人工关系 | 补充缺失 JOIN 关系 |
| 指标口径 | 沉淀 GMV、客单价、复购等业务指标 |
| 标准问法 | 将正确问法和 SQL 作为 few-shot 样例 |
| 反馈记录 | 沉淀错误案例，形成治理线索 |
| 发布版本 | 支持发布、回滚、重命名、删除 |

### 4.1 表级画像

表级画像用于描述一张表在业务上的含义。

示例:

```yaml
tables:
  orders:
    business_name: 订单
    description: 记录用户购买商品的订单明细
    grain: 一行代表一笔订单
    default_time_column: created_at
    default_filters:
      - status = 'paid'
```

表级画像当前支持:

- 表业务名
- 表描述
- 表粒度
- 默认时间字段
- 默认过滤条件

这部分主要解决模型不知道“一行代表什么”的问题。  
例如订单表是一行一笔订单，订单明细表可能是一行一个商品项，地址表可能是一行一个用户地址。粒度不清楚时，聚合、去重和 JOIN 都容易出错。

### 4.2 字段级画像

字段级画像用于解释字段在业务中的作用。

示例:

```yaml
columns:
  orders:
    amount:
      business_name: 订单金额
      description: 用户实际支付的订单金额
      semantic_type: metric
      default_aggregation: sum
      unit: 元

    status:
      business_name: 订单状态
      semantic_type: dimension
      enum_values:
        - pending=待支付
        - paid=已支付
        - cancelled=已取消
      default_filter: status = 'paid'
```

字段级画像当前支持:

- 中文名 / 别名
- 字段业务描述
- 枚举值解释
- 示例值
- 单位
- 默认过滤
- 语义类型
- 默认聚合方式
- 敏感、废弃、禁用标记

这些信息会进入三处:

1. 前端 Schema 配置页
2. Schema 检索索引
3. 模型生成 SQL 的上下文

### 4.3 字段安全与可用性

字段可以被标记为:

```yaml
sensitive: true
deprecated: true
enabled: false
```

含义分别是:

- `sensitive`: 敏感字段，不应被主动召回
- `deprecated`: 废弃字段，降低使用优先级
- `enabled: false`: 禁用字段，SQL 校验阶段直接拦截

这使 Schema 知识库具备“治理”和“安全约束”能力，而不仅仅是提示词增强。

### 4.4 人工 JOIN 关系

生产库中经常没有完整外键，所以系统支持人工维护关系:

```yaml
relations:
  - left: orders.user_id
    right: users.id
    type: many_to_one
    description: 每笔订单属于一个用户
```

人工关系会进入:

- 关系图谱召回
- JOIN 路径提示
- SQL 生成上下文
- 前端 Schema 配置页面

这可以明显降低多表查询时 JOIN 错误的概率。

### 4.5 指标口径

指标口径是 Schema 知识库的重要组成部分。  
很多业务问题不是直接查字段，而是在问指标，例如:

- GMV
- 客单价
- 件单价
- 复购用户数
- 转化率

当前支持结构化维护指标:

```yaml
metrics:
  客单价:
    formula: 已支付订单金额求和 / 已支付订单数
    default_filters:
      - orders.status = 'paid'
    default_time_column: orders.created_at
    description: 平均每笔订单金额
```

指标口径的价值在于:

- 业务口径不再散落在 prompt 中
- 可以被前端配置和治理
- 可以进入检索和 SQL 生成上下文
- 后续可以做版本管理和评测回归

### 4.6 草稿、发布与版本

当前 Schema Profile 区分草稿和发布版本。

字段、表画像、关系、指标的保存只是保存草稿，不会生成快照。  
只有点击“发布 Profile”时，才会生成发布快照。

发布时可以填写:

- 版本名称
- 发布说明

历史版本支持:

- 查看版本名称和描述
- 回滚
- 重命名
- 删除

这样可以避免每次字段保存都生成大量无意义快照，同时保留正式发布节点的可追溯性。

---

## 五、四路 Schema 召回设计

大库场景下，完整 DDL 不能直接全部喂给模型。因此系统实现了 Schema Linking 检索流程，只召回和当前问题相关的表、字段、关系和业务规则。

当前采用四路召回:

| 召回路 | 主要信号 | 作用 |
|---|---|---|
| 向量召回 | 字段名、中文名、描述的语义相似度 | 解决自然语言和字段名不一致 |
| 关键词召回 | 表名、字段名、枚举值、业务词命中 | 解决精确词匹配 |
| 业务术语召回 | Glossary 中的术语、取值映射、口径说明 | 补充业务规则 |
| 关系图谱召回 | 外键、人工关系、表连接路径 | 补充结构相关表 |

### 5.1 向量召回

系统会把表和字段拆成可检索的 Schema Atom。  
字段的中文名、描述、枚举值、语义类型都会进入向量文本。

当用户问“已支付订单金额”时，即使真实字段是 `amount`，只要字段画像里维护了“订单金额”“已支付”等语义，就能被召回。

### 5.2 关键词召回

关键词召回用于处理精确匹配，例如:

- 用户直接提到字段名
- 用户提到枚举值
- 用户提到业务词表里的固定术语

关键词召回对英文缩写、ID 类字段、状态值等非常重要。

### 5.3 业务术语召回

业务术语来自 Glossary 和用户配置。  
它可以补充:

- 业务别名
- 枚举值含义
- 统计口径
- 特殊过滤规则

例如:

```text
有效订单 = status = 'paid'
最近一年 = 当前日期向前 12 个月
```

### 5.4 关系图谱召回

关系图谱把数据库外键和人工关系合并，形成表之间的连接网络。

它的作用是:

- 当某张表被召回时，补充与它强相关的连接表
- 为 SQL 生成提供 JOIN 路径提示
- 避免模型漏掉桥接表

### 5.5 召回融合与裁剪

多路召回后，系统会进行融合排序，再决定最终进入上下文的表和字段。

当前设计重点:

- 多路召回结果融合，避免单一路径误判
- 宽表只保留命中列、主键、外键和必要补充列
- 小库可以直接喂完整 DDL
- 大库自动切换到精简上下文

这样既节省 token，也减少无关字段干扰。

---

## 六、标准问法、反馈与治理闭环

### 6.1 标准问法

当用户确认某次查询结果正确后，可以将问题和 SQL 保存为标准样例。

示例:

```text
问题: 最近 30 天销售额是多少？
SQL: SELECT SUM(amount) FROM orders WHERE status = 'paid' ...
```

这些样例会作为 few-shot 进入后续问数请求，提高同类问题的稳定性。

### 6.2 用户反馈

用户可以对结果进行正确或错误反馈。反馈记录包括:

- 问题
- SQL
- 数据源
- 错误原因
- 错误分类
- 查询解释

这些反馈可以反向指导知识库治理。例如:

- 字段召回错了，补字段画像
- JOIN 错了，补人工关系
- 口径错了，补指标定义或默认过滤
- 枚举理解错了，补枚举解释

### 6.3 质量评分

系统当前已经具备知识库质量评分雏形，会从多个维度评估当前知识库建设情况:

- 表粒度覆盖
- 字段语义覆盖
- 关系治理
- 指标口径
- 安全标记

后续可以把质量评分作为发布前检查项。

---

## 七、评测机制

项目中不只依赖人工试用，也建设了评测机制，用于衡量系统准确率和改动风险。

### 7.1 BIRD 基准库接入

当前已接入多个 BIRD dev 基准库，包括:

- california_schools
- card_games
- codebase_community
- debit_card_specializing
- european_football_2
- financial
- formula_1
- student_club
- superhero
- thrombosis_prediction
- toxicology

这些库用于验证系统在不同领域、不同表结构、不同字段命名下的泛化能力。

### 7.2 批量评测脚本

项目提供批量评测脚本，可以按数据集运行标准问题，记录:

- 生成 SQL
- 执行结果
- 是否成功
- 错误原因
- 召回上下文
- 耗时

这用于判断某次代码或 prompt 调整是否提升或降低整体效果。

### 7.3 裁判模型评估

查询成功后，系统可以使用独立裁判模型对结果进行评估。

评估输入包括:

- 用户问题
- Schema 上下文
- 生成 SQL
- 查询结果预览

裁判模型输出:

- 数据完整度评分
- 结果匹配度评分
- 综合可信度
- 面向业务用户的解释

裁判评估是异步执行的，不阻塞用户看到结果。

### 7.4 高风险用例回归

当前项目也在沉淀高风险回归用例，重点覆盖:

- 多表 JOIN
- 去重统计
- 默认过滤
- 枚举值映射
- 中文问题匹配英文字段
- 宽表字段裁剪
- 多轮追问
- 自动路由切库
- SQL 失败回修

这些用例用于保障后续迭代不破坏已有能力。

---

## 八、前端产品化能力

当前前端不只是一个问答输入框，而是围绕“智能问数 + 知识库治理”做了工作台化设计。

### 8.1 问数助手

问数助手支持:

- 自然语言提问
- 自动或手动选择数据源
- 多轮追问
- 查询进度展示
- SQL 展示
- 表格结果
- 结果解释
- 可信度展示
- 保存标准问法
- 正确/错误反馈

### 8.2 知识库列表

知识库列表用于展示所有数据源，包括:

- 数据源名称
- 表数量
- 字段数量
- 质量状态
- 操作入口

### 8.3 知识库概览

概览页用于展示当前知识库治理情况:

- 表数量
- 字段数量
- 关系数量
- 指标数量
- 反馈数量
- 健康度评分
- 治理建议
- 标准问法
- 反馈记录

### 8.4 Schema 配置页

Schema 配置页采用三栏布局:

- 左侧: 表列表
- 中间: 表画像和字段画像编辑
- 右侧: 发布版本、表关系、指标、术语

这个页面是 Schema 知识库构建的主要入口。

### 8.5 召回调试页

召回调试页用于输入一个问题后，只看 Schema 召回情况，不一定执行完整 SQL。  
这可以帮助开发和知识库维护人员判断:

- 哪些表被召回
- 哪些字段被召回
- 召回是否命中正确业务语义
- 是否需要补充 Profile 或 Glossary

---

## 九、当前技术实现概览

| 模块 | 主要职责 |
|---|---|
| `app/api/routes.py` | 对外 API，包括问数、Schema、Profile、反馈、样例、调试 |
| `app/service.py` | 问数主编排，串联路由、Schema、检索、生成、校验、执行 |
| `app/core/schema.py` | 读取数据库 Schema，合并 Profile 和 Glossary |
| `app/core/schema_profile.py` | 管理结构化 Profile、版本、发布、质量报告 |
| `app/core/retrieval/` | 四路 Schema 召回、融合、裁剪、渲染上下文 |
| `app/core/validator.py` | SQL 安全校验 |
| `app/core/executor.py` | 只读执行 SQL |
| `app/core/judge.py` | 结果可信度评估 |
| `app/core/examples.py` | 标准问法样例存储 |
| `app/core/feedback.py` | 用户反馈存储 |
| `app/static/` | 前端单页工作台 |

---

## 十、已完成重点能力

目前已完成:

1. 多数据源配置与自动路由
2. SQLite / PostgreSQL Schema 读取
3. Schema Profile 结构化知识库
4. 表级画像和字段级画像
5. 人工 JOIN 关系
6. 指标口径维护
7. 字段敏感、废弃、禁用治理
8. 四路 Schema 召回
9. 宽表字段裁剪
10. 标准问法 few-shot 沉淀
11. 用户反馈记录
12. Profile 发布、版本、回滚、重命名、删除
13. SQL 安全校验
14. SQL 失败回修
15. 裁判模型可信度评估
16. BIRD 基准库接入
17. 前端知识库工作台
18. 召回调试能力

---

## 十一、当前阶段价值

当前阶段的主要价值体现在四个方面。

### 1. 从“模型猜 SQL”变成“基于知识库生成 SQL”

模型不再只依赖原始 DDL，而是基于业务语义、指标口径、关系图谱和标准问法生成 SQL。

### 2. 从“不可控结果”变成“可治理链路”

字段可用性、默认过滤、指标口径、JOIN 关系都可以被配置和治理。

### 3. 从“人工试错”变成“可评测迭代”

通过 BIRD 基准库、裁判模型、高风险用例和反馈记录，可以持续评估系统效果。

### 4. 从“开发工具”变成“产品化工作台”

前端已经具备知识库列表、概览、Schema 配置、版本发布、召回调试等产品化能力。

---

## 十二、后续规划

### 1. Profile 自动生成草稿

根据字段名、字段类型、样例值、词表和历史 SQL，自动生成:

- 字段中文名
- 字段描述
- 语义类型
- 默认聚合
- 枚举解释

人工主要负责审核和修正。

### 2. 发布审批流程

当前已支持草稿和发布版本。后续可以增加:

- 待审核
- 审核通过
- 审核驳回
- 发布人和发布时间

适合多人协作维护知识库。

### 3. 质量评分增强

继续细化知识库质量评分:

- 核心表覆盖率
- 核心字段覆盖率
- 指标口径完整度
- 关系完整度
- 标准问法覆盖率
- 反馈关闭率

### 4. 错误归因自动化

将错误反馈自动归类为:

- 选库错误
- Schema 召回错误
- JOIN 错误
- 指标口径错误
- SQL 生成错误
- 数据本身缺失

这样可以更快定位应该补知识库还是调模型。

### 5. 标准问法评测集

将已确认正确的标准问法沉淀成自动化评测集。  
每次修改 Profile、Prompt 或召回策略后，自动跑一遍回归，评估准确率变化。

### 6. 企业级部署

后续可以补充:

- 用户权限
- 数据源权限
- 审计日志
- HTTPS
- Docker 部署
- 内网访问控制

---

## 十三、总结

当前项目已经完成智能问数主链路，并围绕 Schema 知识库、四路召回、SQL 安全、评测机制和前端治理工作台进行了系统化建设。

其中 Schema 知识库是提升准确率和可维护性的核心。它把数据库里的技术结构转化为业务可理解、可配置、可发布、可回滚的知识资产，并参与 Schema 召回、SQL 生成、安全校验、结果解释和反馈治理全过程。

下一阶段重点建议放在三个方向:

1. 继续完善 Schema Profile 自动化生成和质量评分
2. 建立标准问法评测集，形成稳定回归机制
3. 补齐权限、审批和部署能力，推动真实业务库试点
