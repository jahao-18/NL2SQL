# NL2SQL 原始项目评价与工作责任边界

> 评价日期：2026-07-14  
> 评价对象：本轮测试、优化和记录工作开始前的 Git 原始基线，以及首次接收项目时的本地运行条件。  
> 目的：说明原始项目做了什么、解决什么问题、哪些能力实际可以运行、首次运行和查询中实际出现了什么问题，并将原始交付、环境准备和本轮优化工作分开记录。

## 一、评价原则与证据边界

本评价遵守实事求是原则，采用以下证据等级：

1. **实际运行测试**：真实执行依赖安装、应用导入、pytest、Uvicorn、HTTP API、SQLite 查询或 Qwen 问数后得到的结果。
2. **源码直接证据**：从原始 Git 基线代码、README、配置和测试文件中可以直接确认的实现，但没有把它描述成已经发生的运行结果。
3. **未验证内容**：没有执行过的浏览器操作、生产部署、并发压力、长期稳定性和外部 BIRD 全量评测，不作肯定结论。

本文所称“原始项目”是当前仓库 `HEAD` 中已有的跟踪文件，不包含本轮未提交的修改。责任边界按文件和实际变更划分，不对具体个人的主观能力或工作态度作判断。

## 二、原始项目的内容

### 2.1 项目定位

原始 README 将项目定义为“NL2SQL 教学数据智能问数平台”。它使用 FastAPI、LangChain、通义千问 Qwen 和 SQLite/PostgreSQL，让用户用中文提问，由系统生成只读 SQL、执行查询并展示结果。

它不是单一的 SQL 生成脚本，而是包含以下模块的完整 Web 原型：

- 后端 API 和静态前端工作台。
- 中文自然语言转 SQL。
- 自动数据源路由和手动数据源选择。
- Schema 读取、枚举发现、业务词表和 Schema 画像。
- 本地向量、BM25 和关系图谱组成的 Schema 检索。
- 多轮追问、歧义澄清和 SQL 执行失败修复。
- 单条只读 SQL 校验、表白名单、字段限制、行数上限和执行超时。
- 查询结果异步可信度评估。
- 管理员、教务、学院、教师、学生五类演示角色。
- Schema 画像编辑、版本管理、反馈、示例和治理复核队列。
- SQLite/CSV/DB 文件接入、表数据维护和审计。

### 2.2 默认教学数据

原始项目自带 `data/teaching.db`，实际检查可读取 17 张教学业务表，覆盖：

- 学院、专业、行政班和学生。
- 教师、课程、开课班和选课。
- 成绩、评教、作业、作业提交和考勤。
- 学习活动、奖学金和学业预警。

同时提供：

- `data/glossaries/teaching.md`：教学术语、连接规则和指标口径。
- `data/schema_profiles/teaching.yaml`：表粒度、默认过滤、字段语义和人工关系。
- `data/examples/teaching.json`：教学 Few-shot 示例。
- `tests/teaching_eval_cases.yaml`：8 条教学问题及 SQL 片段检查。

### 2.3 原始问数链路

根据源码和实际调用，原始核心链路为：

1. 接收自然语言问题和历史对话。
2. 自动选择或使用指定数据源。
3. 根据角色过滤可见表和字段。
4. 检索相关 Schema、词表、画像、关系和指标。
5. 调用 Qwen 生成 SQL或澄清问题。
6. 校验单条 SELECT、危险关键字、表和输出字段。
7. 使用只读连接执行 SQL，并限制结果行数和执行时间。
8. 执行失败时把 SQL 和错误交给 Qwen 修复，最多使用原项目规定的修复轮次。
9. 返回结果，并异步提交可信度裁判。

## 三、项目面向的问题

原始项目主要面向以下教学管理问题：

- 业务人员不会编写 SQL，但需要查询教学数据库。
- 教学库表多、连接关系复杂，直接把完整 Schema 全部交给模型会增加干扰。
- “本学期”“挂科率”“在读学生”等业务表达需要统一口径。
- 学生、教师、学院和校级人员的数据访问范围不同。
- LLM 生成 SQL 存在语法错误、表列选择错误、枚举猜错和危险操作风险。
- 查询结果需要可解释、可回看，并通过反馈、示例和 Schema 画像持续治理。

从功能设计看，原项目对 NL2SQL 的主要环节考虑较全面，尤其包含检索、修复、只读执行和治理工作台，不是只有一个 Prompt 的简单示例。

## 四、首次运行条件与实际结果

### 4.1 首次接收时的环境状态

| 项目 | 首次实际状态 | 判断 |
|---|---|---|
| `.venv` | 尚未安装项目依赖 | 属于本地环境准备，不直接认定为原代码缺陷 |
| `.env` | DashScope Key 尚未配置 | README 明确要求用户配置，属于部署前置条件 |
| `data/teaching.db` | 文件存在并可读取 | 原项目已提供默认演示数据 |
| BIRD 数据库 | 11 个配置源对应的数据库未下载 | README 已声明 BIRD 不随仓库提供；不影响默认教学库，但原前端/API 仍会展示这些不可用源 |

### 4.2 环境补齐后的实际启动结果

在按原 `requirements.txt` 安装依赖、由用户配置 `.env` 后，实际结果如下：

- `pip check`：没有损坏依赖。
- 关键依赖导入：通过。
- `import app.main`：通过。
- Uvicorn 临时服务：能够启动。
- `/api/health`：返回 200。
- DashScope/Qwen：能够真实调用。
- 本地检索模式：能够建立或读取教学库检索索引。

因此，原始项目的默认教学主链路在环境正确时可以启动，不能评价为“完全跑不通”。

### 4.3 原始 pytest 基线

首次在项目虚拟环境运行原始测试时，实际结果为：

- 7 项通过。
- 1 项失败。
- 失败项：`tests/test_governance.py::test_publish_review_is_approved_before_publish`。
- 实际错误：接受发布审核时调用 `save_profile_dict("demo", ...)`，而 `demo` 未注册为数据源，抛出“未知数据源: demo”。

这一失败在本轮新增测试后仍保持相同调用栈，说明它是原始基线问题，不是本轮认证、范围或检索修改造成的。

## 五、首次真实教学查询结果

### 5.1 八条教学评测问题

首次使用真实 Qwen、`teaching` 数据源执行 `tests/teaching_eval_cases.yaml` 的 8 条问题时，实际观察如下：

| 用例 | 问题 | 首次实际结果 |
|---|---|---|
| teaching_001 | 各学院学生人数是多少？ | SQL 片段检查通过，执行成功，未发现明显语义错误 |
| teaching_002 | 2025 年春季学期挂科率最高的 5 门课程是什么？ | 学期、挂科和 LIMIT 片段检查通过，执行成功 |
| teaching_003 | 软件工程专业各年级学生人数是多少？ | 首次生成 `major.name = '软件工程专业'`，数据库真实枚举为“软件工程”，结果返回 0 行；属于实际查询错误 |
| teaching_004 | 统计每位教师的授课班级数和选课学生人次 | SQL 执行成功；数据库教师数为 240，而结果上限为 200，响应标记 `truncated=True`，属于安全上限造成的完整性边界 |
| teaching_005 | 哪些课程的平均分低于 70？ | SQL 片段检查通过，执行成功，未发现明显语义错误 |
| teaching_006 | 教学评价分最高的 10 位教师是谁？ | SQL 片段检查通过，执行成功 |
| teaching_007 | 本学期开设了多少门课程？ | 2025、spring 和开课班条件检查通过，执行成功 |
| teaching_008 | 各学院平均成绩和挂科率是多少？ | 学院、成绩和 CASE 片段检查通过，执行成功 |

需要注意：“SQL 片段通过”不等于有标准答案证明完全正确。原始评测没有逐行标准结果，因此本文只把 teaching_003 的错误枚举和 0 行结果记录为确定错误；其他用例表述为“未发现明显错误”，不宣称已经形式化证明正确。

### 5.2 角色查询实际结果

首次角色回归中实际观察到：

- 学生问“我的平均成绩”：生成 SQL 正确限定 `student_id = 1`。
- 学生模糊问“平均成绩是多少”：生成全校平均 SQL，没有学生范围，实际返回约 `72.7488`；属于行级范围遗漏。
- 同一全局平均结果提交原始裁判后得到 92 分，说明裁判没有识别角色范围错误。
- 学生请求列出所有学生姓名和成绩：被敏感词/权限规则拒绝。
- 教师问“我的课程平均成绩”：正确限定 `teacher_id = 37`。
- 教师请求学生姓名：被拒绝。
- 学院角色查询本学院平均成绩和挂科率：能生成 `college_id = 1` 范围。

这些结果说明原始 Prompt 对显式“我的/本学院”有一定效果，但范围依赖模型遵守提示，没有形成执行前的强制保证。

### 5.3 多轮、澄清和修复链实际结果

以下原始能力经过真实 Qwen 测试并成功：

- 四轮追问：从 2025 春季学院平均分，继续限定计算机学院、增加挂科率、再按课程拆分，历史条件能够保留。
- 歧义澄清：“哪个学院最好？”会追问评价指标；用户回答“按平均成绩”后能够生成排序 SQL。
- 受控 SQL 修复：人为让首轮生成 `SELECT missing_column FROM student`，执行失败后原始 `repair_sql` 能重新生成可执行查询并返回结果。

因此，多轮、澄清和修复并非仅存在于源码，已经实际运行成功。

## 六、首次运行和查询中确认存在的问题

下表只列实际运行复现的问题，或以实际响应为主要证据的问题。

| 原始问题 | 实际证据 | 影响 | 本轮开始前状态 |
|---|---|---|---|
| 治理 pytest 失败 | 原始 pytest 为 7 通过、1 失败；`demo` 数据源不存在 | 无法获得全绿原始回归基线 | 未解决 |
| 教学评测脚本与用例格式不一致 | 教学 YAML 使用 `contains_sql`，现有评测脚本不能直接完成该组验收，只能另行手工执行 | 自动评测不能可靠发现空结果和错误枚举 | 未解决 |
| 通用评测数据源缺失 | `tests/eval_cases.yaml` 面向电商库，生成脚本目标为 `data/app.db`，实际文件和明确注册源均不存在 | 通用评测无法按现状直接执行 | 未解决 |
| 缺失/未知令牌成为管理员 | 无令牌和未知令牌请求 session 均实际返回 admin；无令牌数据维护曾返回 200 | 未登录请求获得管理员身份 | 未解决 |
| 管理接口缺少后端角色校验 | Profile、治理等接口无令牌实际能够返回 200 | 可绕过前端隐藏直接调用管理功能 | 未解决 |
| 数据维护权限未落实到表、字段和行 | 教师令牌实际读取 student 表并得到姓名、学号等字段 | 绕过问数限制并暴露身份字段 | 未解决 |
| 学生模糊查询越过本人范围 | “平均成绩是多少”实际执行全校平均，SQL 无 `student_id` | 低权限用户获得超范围统计 | 未解决 |
| 裁判给越范围查询高分 | 上述全校平均结果实际得到 92 分 | 可信度展示误导用户 | 未解决 |
| 专业枚举映射不稳定 | teaching_003 首次使用“软件工程专业”并返回 0 行；重试曾正确使用“软件工程”并返回 5 行 | 相同问题结果不稳定，片段检查不能发现 | 未解决 |
| 不可用 BIRD 源仍由 API 返回 | `/api/sources` 实际列出 12 个源；请求缺失的 `california_schools` Schema 实际返回 500 | 用户可选择不可用源并遇到失败 | 未解决 |

## 七、实际通过的安全与稳定性检查

为了避免只记录缺陷，以下原始能力也经过了实际测试：

- `DELETE`、`PRAGMA`、多语句查询被 SQL 校验器拒绝。
- 嵌套引用未授权表被拒绝。
- 包含受限输出字段的 `SELECT *` 被拒绝。
- 只读 CTE 可以通过。
- SQLite 连接保持只读，写操作无法执行。
- 执行器最多返回 200 行，并标记结果被截断。
- 将超时配置临时设为 1 秒后，递归查询约 1.04 秒被中断。
- 5 个前端 JavaScript 文件通过 `node --check`。
- HTML 中检查到的 176 个 ID 没有重复，本地静态资源存在。

因此，原项目已有基础 SQL 防护和执行保护，但这些能力不能替代身份认证、接口授权和行级数据权限。

## 八、仅由源码审查发现、尚未作为浏览器故障实测的事项

以下结论来自源码，不写成已发生的浏览器运行故障：

- `app/static/app.js::loadAllSchemas` 会尝试加载数据源 Schema；结合缺失 BIRD 源，存在产生多个失败请求的路径，但本轮没有把完整浏览器网络面板结果作为实测证据。
- `app/static/data-access-view.js` 的部分异步增删改流程没有统一 `try/catch`，理论上请求失败时可能出现未处理 Promise；尚未用浏览器逐项触发验证。
- FastAPI `on_event`、LangChain Community 和 TestClient/httpx 出现弃用警告；警告已实际输出，但未来具体在哪个版本失效尚未验证。

## 九、对原始项目的综合评价

| 维度 | 评价 | 实际依据 |
|---|---|---|
| 核心可运行性 | 良好 | 环境补齐后可启动服务、调用 Qwen、执行教学查询 |
| 功能覆盖 | 良好 | 包含问数、检索、修复、角色、治理、数据接入和前端工作台 |
| 基础 SQL 安全 | 良好 | 危险语句、只读、行数上限和超时均通过实际测试 |
| 查询准确性 | 一般 | 多数教学问题可执行，但存在实际枚举错误、范围遗漏和 LLM 不稳定 |
| 身份与接口授权 | 不足 | 实际存在无效令牌成为管理员、管理接口无后端授权、数据维护越权 |
| 自动化测试成熟度 | 不足 | 原始测试仅 8 项且有 1 项失败；教学评测与脚本不兼容 |
| 数据源可用性体验 | 一般 | 默认 teaching 可用，但 API 同时暴露 11 个未安装 BIRD 源 |
| 可信度评估 | 不足 | 实际对越范围查询给出高分，无法作为可靠权限或正确性保证 |
| 生产可用性 | 不足 | README 本身也声明演示账号不能用于生产，仍需认证、授权、脱敏和部署加固 |

综合结论：**原始项目是核心链路能够跑通、功能面较丰富的教学原型，但不是已经完成安全闭环和质量验收的生产级系统。** 原项目具备继续优化的基础，不需要推倒重写；但身份认证、接口授权、数据维护范围、自动评测和可信度评价需要补齐。

## 十、工作内容与责任范围划分

### 10.1 原始交付基线范围

以下内容在本轮开始前已经存在，应归入原始项目或上一阶段交付范围：

- `app/` 下的后端、前端、检索、问数、治理和数据维护实现。
- `prompts/`、`data/teaching.db`、教学词表、画像和示例。
- 原始 `requirements.txt`、README、`data_sources.yaml` 和 Docker 配置。
- 原始 `tests/test_governance.py`、`tests/test_role_permissions.py` 和两套 YAML 评测集。
- 原始项目中已经存在的 ISSUE-002 至 ISSUE-013 所描述的实现或测试缺口。

这里的“归入原始范围”表示问题存在于本轮修改前的文件或运行结果，不表示对某一名同学作主观过错判断。

### 10.2 环境与用户配合范围

- `.venv` 依赖由本轮按原 `requirements.txt` 安装，属于环境准备，不是业务代码开发。
- `.env` 和 DashScope Key 由用户配置；密钥未写入记录或提交。
- BIRD 数据库未由本轮下载。README 已明确它们是可选的大文件数据集。

### 10.3 本轮测试与优化工作范围

本轮新增或完成的工作包括：

- 创建 `PROJECT_ISSUES.md`、`PROJECT_CHANGES.md`、`PROJECT_PROGRESS.md`，建立修改前申请、问题跟踪、代码级记录和实事求是验证准则。
- 安装项目虚拟环境依赖，验证应用导入、服务启动、模型调用和教学数据库。
- 执行原始 pytest、教学真实问数、角色测试、多轮、澄清、修复链、SQL 安全、超时、前端静态检查和 API 越权探测。
- CHG-20260714-01：修复检索枚举阈值不一致，新增 `tests/test_retrieval_context.py`。
- CHG-20260714-02：增加学生、教师、学院行级范围执行前校验，扩展 `tests/test_role_permissions.py`。
- CHG-20260714-03：删除缺失/未知令牌回退管理员，新增 `tests/test_authentication.py` 和统一 HTTP 401 处理。
- 修改后实际回归达到 25 项通过、1 个原始治理测试失败；临时 Uvicorn HTTP 和合法学生 Qwen 查询通过。

### 10.4 当前仍未完成、不得计入本轮已完成成果的事项

- ISSUE-002：治理测试 `demo` 数据源失败。
- ISSUE-003：教学评测自动化与结果语义校验。
- ISSUE-004：通用电商评测的数据源边界。
- ISSUE-007：画像、治理、反馈、示例等管理接口后端授权。
- ISSUE-008：数据维护接口表、字段和行级范围。
- ISSUE-010：缺失 BIRD 数据源的可用性展示。
- ISSUE-011：数据维护前端统一错误处理。
- ISSUE-013：可信度裁判理解角色范围。
- 浏览器端完整交互验收、生产部署、并发与压力测试。

## 十一、责任认定时应避免的误区

- 首次没有 `.env` 或 API Key 是部署条件未完成，不能直接写成上一名同学的代码错误。
- BIRD 未下载是 README 已声明的可选数据缺失；真正需要评价的是不可用源仍被用户界面/API 暴露的体验问题。
- LLM 输出具有随机性，不能把一次错误全部归因于代码；但检索上下文丢失真实枚举、缺少确定性校验和评测无法发现错误，属于可以通过工程手段改进的部分。
- 本轮修复暴露出的下游问题，例如裁判不理解强制角色范围，应记录为原始裁判能力与新安全规则之间的不一致，不能隐瞒，也不能在未测试前宣称已解决。
- 只有在原始基线实际复现的问题，才能列入“原始运行或查询错误”；源码推断必须单独标识。

## 十二、最终结论

原始 NL2SQL 项目已经完成了一个可启动、可连接真实模型、可查询教学数据库的综合教学原型，核心问数链路和基础 SQL 安全具有实际可用性。与此同时，首次真实测试确认其认证、接口授权、数据范围、评测和可信度机制尚未完善。

因此，原始项目责任范围可概括为：**完成主体功能和演示框架，但留下若干经实际测试确认的安全与质量缺口。** 本轮工作责任范围则是：**建立可追溯测试基线，实际复现问题，按批准范围进行最小修复，并用单元、API、临时服务和真实 Qwen 查询验证修复结果。**

后续继续处理 ISSUE-007、008、013 等问题时，应保持该边界，不把原始缺陷记成本轮新增问题，也不把尚未完成的修复计入本轮成果。
